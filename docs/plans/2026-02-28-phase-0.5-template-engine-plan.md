---
title: "Phase 0.5: Template Engine"
type: feat
date: 2026-02-28
status: in-progress
parent: docs/plans/2026-02-26-feat-jasonette-revival-roadmap-plan.md
milestone: 2
branch: feat/phase-0.5-template-engine
---

# Phase 0.5: Template Engine

## Goal

Build a standalone TypeScript template engine that transforms `$jason` documents by evaluating template expressions and structural directives (`{{#each}}`, `{{#if}}`). This is the core runtime that all platforms (web, iOS, Android) will share.

## Deliverables

1. **`packages/template-engine/`** — TypeScript package with Vite (library mode) + Vitest
2. **Template parser** — Handles `{{var}}`, `{{#each}}`, `{{#if}}/{{#elseif}}/{{#else}}`
3. **Expression evaluator** — JSEP + custom AST walker with security layers
4. **Mixin resolver** — `@` mixin operator with recursion limits
5. **Conformance test results** — Expected output for Phase 0 fixtures
6. **npm package** — `@jasonette/template-engine`

## Tasks

### 0.5.1 — Project Setup

- [ ] `packages/template-engine/package.json` — name: `@jasonette/template-engine`
- [ ] `packages/template-engine/tsconfig.json` — strict TypeScript
- [ ] `packages/template-engine/vite.config.ts` — library mode, ESM + CJS output
- [ ] `packages/template-engine/vitest.config.ts`
- [ ] `packages/template-engine/src/index.ts` — main entry
- [ ] Install dependencies: `jsep`, `vitest`, `vite`, `typescript`

### 0.5.2 — Expression Evaluator

TDD: write tests first for each expression type.

- [ ] JSEP parsing integration
- [ ] AST walker for:
  - Identifier resolution (`$jason`, `$get`, `$params`, `$root`, `$index`, `$env`, `$cache`, `$keys`)
  - Literal values (string, number, boolean, null)
  - MemberExpression (dot and bracket access)
  - BinaryExpression (`+`, `-`, `*`, `/`, `%`, `==`, `!=`, `===`, `!==`, `<`, `>`, `<=`, `>=`, `&&`, `||`)
  - UnaryExpression (`!`, `-`, `+`)
  - ConditionalExpression (ternary `? :`)
  - CallExpression with function allowlist
- [ ] Security layers:
  - Property blocklist (`__proto__`, `constructor`, `prototype`) on MemberExpression
  - AST validation: reject assignment, statements, unsupported node types
  - Complexity limits: max depth 20, max 50 nodes
  - Timeout: 10ms per expression evaluation
  - Function allowlist: `Math.*`, `JSON.stringify`, `JSON.parse`, `parseInt`, `parseFloat`, `String`, `Number`, `encodeURIComponent`, `decodeURIComponent`
- [ ] Expression cache (LRU, 1000 entries for parsed ASTs)

### 0.5.3 — Template Transformer

TDD: test structural transformations.

- [ ] String interpolation: `"Hello {{name}}"` → `"Hello World"`
- [ ] `{{#each expr}}` — iterate array, set context + `$root` + `$index`
- [ ] `{{#if expr}}` / `{{#elseif expr}}` / `{{#else}}` — conditional inclusion
- [ ] Deep object traversal: recursively process all string values and structural keys
- [ ] Array template support: `[{"{{#if}}": ...}, {"{{#else}}": ...}]`

### 0.5.4 — Mixin Resolver

- [ ] `@` key detection in objects
- [ ] Form A: `$document.key` local reference resolution
- [ ] Form B: remote URL fetch (HTTPS only)
- [ ] Form C: `key@url` selector (stub — Tier 2)
- [ ] `+` merge semantics
- [ ] Recursion depth limit: 5
- [ ] Size limit: 1MB
- [ ] URL validation (AD-10 policy)
- [ ] In-memory cache for fetched mixins

### 0.5.5 — Integration & Conformance

- [ ] `render(document, context)` main API
- [ ] Generate expected output for Phase 0 conformance fixtures
- [ ] Update `spec/conformance/*/expected.json`
- [ ] Run all conformance tests
- [ ] Run adversarial negative tests (must reject/handle gracefully)

## Success Criteria

- [ ] All conformance test fixtures produce correct expected output
- [ ] All adversarial negative tests are handled (rejected or safe output)
- [ ] 100% of expression types from spec covered by tests
- [ ] Security: prototype pollution, complexity bomb, and timeout tests pass
- [ ] Package builds cleanly with Vite (ESM + CJS)
- [ ] CI passes

## Task Ordering

```
0.5.1 Project Setup
 ↓
0.5.2 Expression Evaluator (TDD)
 ↓
0.5.3 Template Transformer (TDD)
 ↓
0.5.4 Mixin Resolver
 ↓
0.5.5 Integration & Conformance
```
