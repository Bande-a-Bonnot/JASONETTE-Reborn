{
  "schema_version": "foundry.prompt-envelope.v1",
  "run_id": "019f4bec-43dc-742b-80b0-e42b7d46ebdd",
  "phase": "phase1b",
  "recipient": "cucumber-reviewer-round3",
  "prompt": "Final Gherkin/Vitest quality review. Verify mutations/render actions are When steps, public behavior wording, split inherited cases, concrete full background matrix examples, valid syntax, independent scenarios, and executable correspondence. Run coverage verifier. Return PASS only with no P0/P1. Do not modify files.",
  "visible_context": [
    {
      "label": "reviewed_nlspec",
      "kind": "nlspec",
      "sha256": "a285312d21d90156b46607f9b758182c3c7d24227fab17479168eea34a449692",
      "content": "---\ndate: 2026-07-10\ntopic: Web HTML template and iframe safety boundary\nsource_spec: docs/specs/2026-07-10-web-html-component-safety-spec.md\nstatus: reviewed\n---\n\n# Web HTML Template and Iframe Safety Boundary NLSpec\n\nThis NLSpec defines body-scoped raw-HTML preservation, safe resolved-key output,\nand sandboxed Web HTML component/background iframes. Independent Foundry red and\ngreen teams must be able to test and implement it without shared hidden context.\n\n## Table of Contents\n\n- [1. Why](#1-why)\n- [2. What](#2-what)\n- [3. How](#3-how)\n- [4. Out of Scope](#4-out-of-scope)\n- [5. Design Decision Rationale](#5-design-decision-rationale)\n- [6. Definition of Done](#6-definition-of-done)\n\n---\n\n## 1. Why\n\n### 1.1 Problem Statement\n\nThe Web renderer evaluates strings inside privileged `html.text`, can mutate an\noutput object's prototype through resolved `__proto__` keys, and creates both\nHTML component and background iframes without a sandbox. It must preserve raw\nHTML only during body-template transformation, produce safe own data properties,\nand install an opaque script-capable sandbox before assigning any HTML iframe\nsource.\n\n### 1.2 Design Principles\n\n**Raw HTML is opaque in body mode.** A final shape of exact own\n`type === \"html\"` retains resolved own `text` values without evaluation.\n\n**Generic transforms remain generic.** The option defaults off. Only body\nrendering opts in, including initial rendering and `$render`.\n\n**Classification is deterministic.** All keys resolve once before any type value\nor ordinary value transforms. Final type and insertion-order collisions are\ntherefore stable.\n\n**Resolved keys are inert data.** Dangerous names become ordinary own data\nproperties and cannot mutate prototypes or become inherited component fields.\n\n**Equivalent HTML paths receive equal privilege.** HTML component and HTML\nbackground iframes both receive exact `allow-scripts` without same-origin.\n\n**Policy exists before content.** Sandbox assignment is the first\nsecurity-observed iframe operation after creation; retained final state remains\nexact through renderer disconnection or test completion.\n\n**Evidence matches the test platform.** jsdom proves transformation and emitted\nDOM policy, not browser sandbox enforcement.\n\n### 1.3 Layering and Scope\n\nThe generic TypeScript template engine owns the opt-in option, resolved-key\nordering, classification, recursive transformation, and safe output properties.\nThe Web renderer enables the option for selected body templates. HTML component\nand background renderers own source selection and sandbox/order/attribute\nbehavior. Tests cover each layer and the complete `$render` path.\n\n---\n\n## 2. What\n\n### 2.1 Data Model\n\n```text\nRECORD RenderOptions:\n    preserve_html_text : Boolean OPTIONAL = false\n    -- TypeScript property name: preserveHtmlText\n    -- Existing option fields remain unchanged.\n\nRECORD ResolvedEntry:\n    index : Integer\n    authored_key : String\n    resolved_key : String\n    authored_value : Any\n    transformed_type_value : Any OPTIONAL\n\nENUM HtmlSourceKind:\n    INLINE\n    URL\n    NONE\n\nRECORD HtmlSourceSelection:\n    kind : HtmlSourceKind\n    value : Any OPTIONAL\n\nENUM IframeTraceEventKind:\n    CREATE\n    SANDBOX\n    SOURCE\n    APPEND\n    RETURN\n\nCONSTANT HTML_TYPE = \"html\"\nCONSTANT HTML_TEXT_KEY = \"text\"\nCONSTANT HTML_SANDBOX = \"allow-scripts\"\n```\n\n### 2.2 Architecture\n\n```text\ninitial render or $render\n    -> renderSync(body_template, context, preserve_html_text = true)\n        -> transform recursively\n            -> resolve all object keys\n            -> transform final type candidates\n            -> protect raw text under final HTML shape\n            -> define safe own output properties\n    -> render body\n        -> HTML component or HTML background\n            -> own-field source selection\n            -> sandboxed iframe creation\n```\n\nThe mode is shape-based throughout the entire body tree: roots, headers,\nfooters, sections, layouts, layers, backgrounds, nested arrays, directive\nresults, and nested action/options/payload objects. `executeAction()` performs a\nseparate generic options/continuation transform with the option omitted, as do\ndirect standalone `transform()` calls. Safe own-property definition applies to\nboth modes and to `$set`, `$cache.set`, `$global.set`, and session-state sinks.\n\n### 2.3 Vocabulary\n\n- **Body mode:** `preserve_html_text === true`.\n- **Resolved key:** an authored key after existing key interpolation.\n- **Final resolved type:** the last insertion-ordered transformed value whose\n  resolved key is `type`.\n- **Protected raw text:** an original value whose resolved key is `text` under\n  exact final resolved type string `html` in body mode.\n- **Safe own property:** enumerable, writable, configurable data property that\n  does not invoke legacy `__proto__` mutation behavior.\n- **Own source field:** `type`, `text`, `url`, or `css` obtained only when\n  `Object.hasOwn()` is true.\n- **Opaque script sandbox:** exact attribute `sandbox=\"allow-scripts\"`.\n- **Renderer-owned observation window:** iframe creation through the containing\n  component/background render function's return; endpoint state is separately\n  checked after initial render and `$render`, without a continuous-monitor claim.\n- **Security observer:** the finite iframe-event observer defined in section 3.4.\n\n---\n\n## 3. How\n\n### 3.1 Enable and Propagate Body Mode\n\n```text\nFUNCTION render_selected_body(template, context) -> Body:\n    options = RenderOptions(preserve_html_text = true)\n    RETURN render_sync(template, context, options)\n```\n\nBoth initial `renderDocument()` body-template selection and `$render` body\nre-rendering use this function. Recursive calls for arrays, directives, and\nnested objects pass the same `RenderOptions` unchanged.\n\n`executeAction()` option and continuation normalization calls `transform()` with\nno body-mode option. Direct standalone `transform()` calls do the same. Both\ntherefore retain existing generic interpolation and per-entry evaluation order.\nPassing an unrelated `RenderOptions` object without `preserveHtmlText` also means\nfalse. An embedded action shape may be raw immediately after body transformation;\nwhen that action executes, its options undergo the existing second generic\ntransformation and may interpolate against the action-time context.\n\n### 3.2 Transform a Body-Tree Object Safely\n\nDirective objects retain existing selection behavior. Objects/arrays emitted by\n`#if`, `#elseif`, `#else`, or `#each` recursively receive the same options.\nRegular-object enumeration uses ECMAScript `Object.keys()` order: own enumerable\nstring keys only, with array-index keys numerically ordered before other strings.\n\n```text\nFUNCTION safe_define(result, key, value):\n    Object.defineProperty(result, key, {\n        value: value, enumerable: true, writable: true, configurable: true\n    })\n\nFUNCTION transform_regular_object(object, context, options) -> Object:\n    result = new ordinary Object with prototype exactly Object.prototype\n\n    IF options.preserve_html_text != true:\n        -- Existing generic order: each key, then its value, in insertion order.\n        FOR EACH own enumerable string authored_key IN object:\n            resolved_key = existing_key_interpolation(authored_key, context, options)\n            output = transform(object[authored_key], context, options)\n            safe_define(result, String(resolved_key), output)\n        RETURN result\n\n    entries = []\n    -- Body pass 1: resolve every own key exactly once.\n    FOR EACH own enumerable string authored_key, authored_value, index IN object:\n        resolved_key = existing_key_interpolation(authored_key, context, options)\n        APPEND ResolvedEntry(index, authored_key, String(resolved_key), authored_value)\n            TO entries\n\n    -- Body pass 2: pre-transform every resolved type exactly once.\n    FOR EACH entry IN entries:\n        IF entry.resolved_key == \"type\":\n            entry.transformed_type_value = transform(entry.authored_value, context, options)\n\n    final_type = last transformed_type_value in entries OR UNDEFINED\n    protect = final_type is String AND final_type == \"html\"\n\n    -- Body pass 3: retain raw text or transform, then safely define.\n    FOR EACH entry IN entries:\n        IF entry.resolved_key == \"type\":\n            output = entry.transformed_type_value\n        ELSE IF protect AND entry.resolved_key == \"text\":\n            output = entry.authored_value\n        ELSE:\n            output = transform(entry.authored_value, context, options)\n        safe_define(result, entry.resolved_key, output)\n\n    RETURN result\n```\n\nIn-scope state handlers use the same descriptor-safe copy:\n\n```text\nFUNCTION safe_copy_own(target, source):\n    FOR EACH key IN Object.keys(source):\n        safe_define(target, key, source[key])\n\n$set, $cache.set, and $global.set call safe_copy_own(destination, options)\n\nFUNCTION store_session(state, normalized_domain, options):\n    stored_session = new ordinary Object with prototype Object.prototype\n    safe_copy_own(stored_session, options)\n    safe_define(state.sessions, normalized_domain, stored_session)\n    ASSERT Object.getPrototypeOf(stored_session) == Object.prototype\n    ASSERT Object.getPrototypeOf(state.sessions) == Object.prototype\n\nFUNCTION lookup_session(state, normalized_domain):\n    IF Object.hasOwn(state.sessions, normalized_domain):\n        RETURN state.sessions[normalized_domain]\n    RETURN empty session\n```\n\nBoth session levels receive separate vectors: dangerous option keys on the stored\nsession, and reachable normalized domains `__proto__`, `constructor`, and\n`prototype` on the outer registry. For inherited-domain rejection, set\n`state.sessions = Object.create({\"api.example.com\": inheritedSession})` and issue\na request to `https://api.example.com`; inherited headers/body MUST NOT decorate\nthe request. Null-prototype option inputs remain supported.\n\nAuthor-controlled registries use own-callable lookup:\n\n```text\nFUNCTION own_callable(registry, type) -> Function OPTIONAL:\n    IF type is not String OR Object.hasOwn(registry, type) == false:\n        RETURN NONE\n    candidate = registry[type]\n    IF candidate is not Function:\n        RETURN NONE\n    RETURN candidate\n```\n\nComponent missing/inherited/non-string type follows the existing label default;\nown unknown/prototype-colliding string type returns the visible unknown-component\nelement. Action dispatch reads only own `type` and own `trigger`; inherited type\nis missing. A missing/non-string type with an own string trigger calls\n`lookup_named_action(state.actions, trigger)`. `$lambda` calls the same lookup for\n`options.name`. Own named entries may be action objects or arrays; inherited\nentries (including `toString`) are ignored. Own unknown/prototype-colliding action\ntype returns `undefined` without a continuation. Background rendering requires\nown exact string `html`; all other types create no iframe. No case invokes\ninherited members or throws.\n\n```text\nFUNCTION lookup_named_action(actions, name) -> ActionOrArray OPTIONAL:\n    IF name is not String OR Object.hasOwn(actions, name) == false:\n        RETURN NONE\n    candidate = actions[name]\n    IF candidate is not action object AND candidate is not action array:\n        RETURN NONE\n    RETURN candidate\n```\n\nExact named-action vectors cover own/inherited `toString`, `constructor`, and\n`__proto__` names for both trigger dispatch and `$lambda`; inherited `type` plus\nown trigger falls through to that own trigger.\n\nOrdering is scoped per regular-object frame. Context property getters append\nlabels to an external log. A flat body-mode vector logs all key getters before\ntype getters, then ordinary value getters. A nested vector applies the same rule\nper frame. Off mode preserves existing per-entry key-then-value getter order. A\nvector with own keys `\"2\"`, `\"1\"`, `alpha` proves Object.keys order\n`\"1\"`, `\"2\"`, `alpha`. Dangerous resolved-key vectors combine this ordering\nwith prototype/error checks. No internal transform trace API is required.\n\nExact transform vectors:\n\n| Input/context/mode | Exact relevant output |\n|---|---|\n| `{type:\"html\",text:\"<p>{{secret}}</p>\",style:{height:\"{{height}}\"}}`; `{secret:\"LEAK\",height:40}`; on | `{type:\"html\",text:\"<p>{{secret}}</p>\",style:{height:40}}` |\n| `{type:\"{{kind}}\",\"{{slot}}\":\"<p>{{secret}}</p>\"}`; `{kind:\"html\",slot:\"text\",secret:\"LEAK\"}`; on | `{type:\"html\",text:\"<p>{{secret}}</p>\"}` |\n| `{\"{{typeKey}}\":\"{{kind}}\",text:\"{{secret}}\"}`; `{typeKey:\"type\",kind:\"html\",secret:\"LEAK\"}`; on | `{type:\"html\",text:\"{{secret}}\"}` |\n| `{type:\"html\",\"{{slot}}\":\"first\",text:\"second\"}`; `{slot:\"text\"}`; on | `{type:\"html\",text:\"second\"}` |\n| `{type:\"label\",text:\"{{secret}}\"}`; `{secret:\"OK\"}`; on | `{type:\"label\",text:\"OK\"}` |\n| `{type:\"html\",text:\"{{secret}}\"}`; `{secret:\"GENERIC\"}`; omitted/off | `{type:\"html\",text:\"GENERIC\"}` |\n\nRequired edge vectors:\n\n- Later resolved type `html -> label`: text transforms.\n- Later resolved type `label -> html`: text remains raw.\n- Duplicate resolved text keys under final HTML type: each remains raw; last wins.\n- Protected string output is strictly equal (`===`) to input.\n- Protected object output is the same reference (`===`) as input.\n- Missing, unresolved, and non-string final types do not activate protection.\n- Shapes from `#if`, `#elseif`, `#else`, and `#each` are protected.\n- Shapes nested under each body root/header/footer/section/layout/layer/background\n  and nested array are protected.\n- A body-tree nested action option `{type:\"html\",text:\"{{secret}}\"}` is protected;\n  the same shape in a separate generic action-options transform interpolates.\n- Resolved own `__proto__`, `constructor`, and `prototype` keys are inert\n  enumerable properties in body and off modes;\n  `Object.getPrototypeOf(result) === Object.prototype`.\n- Safe-copy vectors pass those keys through `$set`, `$cache.set`, and\n  `$global.set`; every destination retains `Object.prototype` and owns inert\n  descriptors. Session vectors separately prove the inner stored object and outer\n  domain registry, including inherited-domain lookup rejection.\n- Direct vectors cover inherited, missing, non-string, non-html, `__proto__`,\n  `constructor`, `prototype`, and `toString` type values. Components use the label\n  default or own-string unknown element as specified; only own exact `html` enters\n  component/background HTML paths. Actions invoke no inherited handler and return\n  the specified trigger/undefined output. No case throws.\n- Full transform-to-render vectors cover inherited text/url/css, inherited text\n  plus own URL, inherited URL plus own text, and inherited CSS plus own text.\n- Production generic vector:\n  `await executeAction({type:\"$set\",options:{probe:{type:\"html\",text:\"{{$jason.value}}\"}}}, state, {value:\"ACTION\"})`\n  produces `state.local.probe.text === \"ACTION\"`. Embedded action options are\n  checked immediately after body transform and after their existing action-time\n  generic transform.\n\nExisting unresolved-expression behavior remains unchanged in the named existing\ntransformer tests. Invalid HTML receives no parser or sanitizer.\n\n### 3.3 Select and Compose HTML Sources\n\nOnly own fields participate. Only non-empty string text/URL values are valid\nsources; arbitrary JavaScript/JSON value coercion is not part of the boundary.\n\n```text\nFUNCTION own_value(component, key) -> Any:\n    IF Object.hasOwn(component, key):\n        RETURN component[key]\n    RETURN UNDEFINED\n\nFUNCTION select_source(component) -> HtmlSourceSelection:\n    text = own_value(component, \"text\")\n    url = own_value(component, \"url\")\n\n    IF text is String AND text.length > 0:\n        RETURN HtmlSourceSelection(INLINE, text)\n    IF url is String AND url.length > 0:\n        RETURN HtmlSourceSelection(URL, url)\n    RETURN HtmlSourceSelection(NONE)\n```\n\n| Own text | Own URL | Result |\n|---|---|---|\n| non-empty string, including whitespace | any | INLINE; text wins |\n| empty/non-string/missing | non-empty string | URL |\n| empty/non-string/missing | empty/non-string/missing | NONE |\n| inherited-only text/url | any | ignored |\n| inherited text + own non-empty URL | own URL | URL |\n| inherited URL + own non-empty text | any | INLINE |\n\n```text\nFUNCTION escape_style(css : String) -> String:\n    REPLACE every case-insensitive \"</style\" with runtime code units:\n        U+003C, U+005C, U+002F, \"style\"\n\nFUNCTION compose_srcdoc(component, inline_text : String) -> String:\n    css = own_value(component, \"css\")\n    IF css is not String OR css.length == 0:\n        RETURN inline_text\n    RETURN \"<style>\" + escape_style(css) + \"</style>\" + inline_text\n```\n\n| HTML | Own CSS | Exact runtime `srcdoc` |\n|---|---|---|\n| `<p>x</p>` | absent/empty/null/undefined/non-string/inherited | `<p>x</p>` |\n| `<p>x</p>` | one U+0020 | `<style> </style><p>x</p>` |\n| `<p>x</p>` | tab+newline | identical tab+newline inside `<style>...</style>` |\n| `<p>x</p>` | `</STYLE>` | `<style><\\/style></style><p>x</p>` |\n| `<p>x</p>` | `a</style>b</StYlE>c` | `<style>a<\\/style>b<\\/style>c</style><p>x</p>` |\n\nThe displayed backslash is runtime U+005C; JavaScript source uses `\\\\` to encode\nit. Invalid HTML is assigned unchanged. URL security/network policy is outside\nthis jsdom slice; only source selection/attribute emission is asserted.\n\n### 3.4 Create and Maintain HTML Iframes\n\nThe component registry MUST dispatch only an own registered renderer whose value\nis a function; prototype-colliding type strings follow unknown-component behavior\nwithout throwing. The background path enters HTML only for an own exact string\n`type: \"html\"`.\n\nThe following helper is conceptual pseudocode; component and background paths\nmay implement it separately but MUST have identical observable policy/source\nbehavior. At `renderComponent()` return, the component wrapper is detached from\nthe live document, has class `jasonette-html` and `data-jasonette-type=\"html\"`,\nand owns its iframe child; the iframe keeps width `100%` and border `none`. A\nbackground iframe is a direct child of the renderer root, has class\n`jasonette-background-web` and `aria-hidden=\"true\"`, and remains before\nforeground sections.\n\n```text\nFUNCTION create_html_iframe(component) -> Iframe OPTIONAL:\n    IF Object.hasOwn(component, \"type\") == false OR component.type != \"html\":\n        RETURN NONE\n    selection = select_source(component)\n    IF selection.kind == NONE:\n        RETURN NONE\n\n    iframe = document.createElement(\"iframe\")\n    iframe.setAttribute(\"sandbox\", \"allow-scripts\")\n\n    IF selection.kind == INLINE:\n        iframe.srcdoc = compose_srcdoc(component, selection.value)\n        ASSERT iframe.hasAttribute(\"srcdoc\") == true\n        ASSERT iframe.hasAttribute(\"src\") == false\n    ELSE:\n        iframe.src = selection.value\n        ASSERT iframe.hasAttribute(\"src\") == true\n        ASSERT iframe.hasAttribute(\"srcdoc\") == false\n\n    RETURN iframe\n```\n\nThe security observer records only:\n\n- `CREATE(kind)` for iframe creation (`component` or `background`);\n- `SANDBOX(value)` for sandbox set/remove/replace;\n- `SOURCE(name)` for `srcdoc`/`src` assignment;\n- `APPEND` for every insertion of the iframe into any parent;\n- `RETURN` when `renderComponent()` wrapper creation or background render returns.\n\nPure computation, property reads, wrapper creation, class/ARIA operations, and\nstyle reads/writes are excluded. Exact creation traces:\n\n```text\nCREATE(component) -> SANDBOX(\"allow-scripts\") -> SOURCE(\"srcdoc\"|\"src\")\n-> APPEND -> RETURN\n\nCREATE(background) -> SANDBOX(\"allow-scripts\") -> SOURCE(\"srcdoc\"|\"src\")\n-> APPEND -> RETURN\n```\n\nEvents are grouped per created iframe, not per function call. Each created\niframe records exactly one sandbox event before return: the initial required set.\nNo additional sandbox event occurs in that observation window. Endpoint checks\nafter initial render and `$render` assert exact sandbox state; the disconnected\nold iframe object also retains its attribute. This is not a continuous\npost-return mutation claim. No-source calls emit no per-iframe trace.\n\nInline mode has no `src` attribute. URL mode has no `srcdoc` attribute. Every\niframe has exact sandbox value `allow-scripts`, with no other token.\n\n### 3.5 Integrate Initial Rendering and Actual `$render`\n\nUse initial data `{kind:\"html\",secret:\"LEAK\",height:40,label:\"first\"}` and this\nbody template:\n\n```json\n{\n  \"sections\": [{\n    \"items\": [\n      {\n        \"type\": \"{{$jason.kind}}\",\n        \"text\": \"<script>window.__html_boundary = \\\"{{$jason.secret}}\\\"</script>\",\n        \"style\": {\"height\": \"{{$jason.height}}\"}\n      },\n      {\"type\": \"label\", \"text\": \"{{$jason.label}}\"}\n    ]\n  }]\n}\n```\n\nAfter `renderer.renderDocument(document)`:\n\n- `.jasonette-html iframe` is present;\n- iframe `srcdoc` contains exact\n  `<script>window.__html_boundary = \"{{$jason.secret}}\"</script>`;\n- `.jasonette-html.style.height === \"40px\"`;\n- `.jasonette-label.textContent === \"first\"`;\n- iframe sandbox is exact `allow-scripts` and it has no `src` attribute.\n\nThen invoke the actual action path:\n\n```javascript\nawait executeAction({\n  type: \"$render\",\n  options: {\n    data: {kind: \"html\", secret: \"SECOND\", height: 80, label: \"second\"}\n  }\n}, renderer.getState());\n```\n\nAfter action completion:\n\n- the current iframe is a different node from the initial iframe;\n- both the disconnected initial iframe object and current iframe have exact\n  sandbox endpoint value `allow-scripts`;\n- `.jasonette-html.style.height === \"80px\"`;\n- label text is `second`;\n- the new iframe still contains literal `{{$jason.secret}}`, not `SECOND`.\n\nRender `Jasonpedia/view/component/html/index.json` through\n`JasonetteRenderer`. Its iframe `srcdoc` contains `img{width: 100%;}` and\n`Nexus devices`; source-file grep is not evidence.\n\nBackground vectors place each authored object in the named body slot inside a\ncomplete `JasonDocument` rendered by `JasonetteRenderer`:\n\n| Entry path | Authored object and context | Expected source |\n|---|---|---|\n| static `body.background` | `{type:\"html\",text:\"<p>static</p>\",css:\"p{color:red}\"}` | exact inline `<style>p{color:red}</style><p>static</p>` |\n| static `body.style.background` | `{type:\"html\",url:\"https://example.com/bg.html\"}` | exact URL and absent `srcdoc` attribute |\n| templated `head.templates.body.background` | `{type:\"html\",text:\"<p>{{$jason.secret}}</p>\",css:\"p{color:{{$jason.color}}}\"}` with `{secret:\"LEAK\",color:\"red\"}` | literal secret expression plus transformed red CSS |\n| templated `head.templates.body.style.background` | `{type:\"html\",url:\"{{$jason.url}}\"}` with `{url:\"https://example.com/dynamic.html\"}` | exact dynamic URL and absent `srcdoc` attribute |\n| either static/templated path | `{type:\"html\"}` | no iframe/per-iframe trace |\n\nLocation selection checks own path segments, then nullish precedence:\n\n| Own `body.background` | Own `body.style` object with own `background` | Selected |\n|---|---|---|\n| non-nullish, including false/malformed/no-source | any | canonical; no fallback |\n| null/undefined/missing/inherited | present | legacy |\n| null/undefined/missing/inherited | absent/inherited | none |\n\nDirect vectors cover inherited canonical with own legacy fallback, inherited\n`style`, inherited legacy background inside own style, and each inherited path\nwithout fallback. The full component type/source/CSS matrix\nis repeated independently through canonical and legacy background paths,\nincluding dual source, non-string/empty source, repeated CSS escape, inherited\nfields, prototype-colliding types, and no source. Created rows assert class,\nARIA, exact sandbox/source/trace; no-source rows assert iframe/trace absence.\n\n### 3.6 Enforce Evidence and Verification Boundaries\n\nRequired jsdom evidence:\n\n- transform outputs, strict equality/reference identity, own descriptors, and\n  prototype identity;\n- exact source strings, source-attribute absence/presence, sandbox tokens, finite\n  operation traces, wrapper class/data type, iframe width/border, background\n  class/ARIA, and background-before-foreground placement;\n- initial/$render DOM liveness and rendered Jasonpedia `srcdoc` contents.\n\nProhibited claim: these tests do not prove child script execution, opaque-origin\nenforcement, host DOM/storage denial, browser navigation, or network isolation.\nThose remain browser-platform assumptions.\n\nExact required commands:\n\n```text\nnpm run test --workspace=@jasonette/template-engine -- transformer.test.ts\nnpm run test --workspace=@jasonette/web -- components.test.ts integration.test.ts actions-parity.test.ts renderer.test.ts\nnpm run typecheck --workspace=@jasonette/template-engine\nnpm run typecheck --workspace=@jasonette/web\nnpm run build --workspace=@jasonette/template-engine\nnpm run build --workspace=@jasonette/web\nnpm run test --workspace=@jasonette/template-engine\nnpm run test --workspace=@jasonette/web\n```\n\nFinal full-Web acceptance truth table:\n\n| Local full Web | Isolated `cli.test.ts` | Exact implementation-SHA Web CI | Acceptance |\n|---|---|---|---|\n| pass | not needed | pass | pass |\n| only CLI timeout; all other tests pass | pass | pass | pass with timeout documented |\n| any non-CLI failure | any | any | fail |\n| CLI timeout | fail | any | fail |\n| any | any | missing/fail | fail |\n\n### 3.7 Integration Smoke Test\n\n```text\nFUNCTION integration_smoke_test():\n    renderer = create_renderer()\n    document = document_with_template_from_section_3_5(\n        data = {kind:\"html\", secret:\"LEAK\", height:40, label:\"first\"}\n    )\n\n    renderer.renderDocument(document)\n    first_iframe = query(\".jasonette-html iframe\")\n    ASSERT first_iframe exists\n    ASSERT first_iframe.srcdoc contains literal \"{{$jason.secret}}\"\n    ASSERT query(\".jasonette-html\").style.height == \"40px\"\n    ASSERT query(\".jasonette-label\").textContent == \"first\"\n    ASSERT first_iframe.getAttribute(\"sandbox\") == \"allow-scripts\"\n    ASSERT first_iframe.hasAttribute(\"src\") == false\n\n    await executeAction(\n        {type:\"$render\", options:{data:{\n            kind:\"html\", secret:\"SECOND\", height:80, label:\"second\"\n        }}},\n        renderer.getState()\n    )\n\n    second_iframe = query(\".jasonette-html iframe\")\n    ASSERT second_iframe is not first_iframe\n    ASSERT query(\".jasonette-html\").style.height == \"80px\"\n    ASSERT query(\".jasonette-label\").textContent == \"second\"\n    ASSERT second_iframe.srcdoc contains literal \"{{$jason.secret}}\"\n    ASSERT second_iframe.srcdoc does not contain \"SECOND\"\n\n    generic = transform(\n        {type:\"html\", text:\"{{secret}}\"},\n        {secret:\"GENERIC\"}\n        -- Omit RenderOptions to prove default false.\n    )\n    ASSERT generic.text == \"GENERIC\"\n\n    await executeAction(\n        {type:\"$set\", options:{\n            probe:{type:\"html\", text:\"{{$jason.value}}\"}\n        }},\n        renderer.getState(),\n        {value:\"ACTION\"}\n    )\n    ASSERT renderer.getState().local.probe.text == \"ACTION\"\n\n    ASSERT every observed component/background iframe trace matches section 3.4\n```\n\n---\n\n## 4. Out of Scope\n\n- **Browser sandbox enforcement.** Future Playwright/Vitest-browser tests may\n  verify child execution and denied host DOM/storage access.\n- **HTML sanitization and CSP.** Authored raw HTML remains unchanged.\n- **URL/origin/network security.** Scheme, DNS, origin, redirect, cookies,\n  referrer leakage, and network isolation require separate work.\n- **Iframe policies beyond sandbox.** Permission Policy and Referrer Policy remain\n  unchanged. Forms, popups, downloads, top navigation, and additional sandbox\n  tokens require explicit review.\n- **Native HTML and agent bridges.** iOS/Android renderers and agent APIs are not\n  modified.\n\n---\n\n## 5. Design Decision Rationale\n\n**Why shape-based body protection?** It avoids fragile structural-position lists\nand protects semantic HTML anywhere in a transformed body. Embedded action data\ninside that body is intentionally protected; separate action transforms remain\ngeneric.\n\n**Why define properties rather than assign?** Plain `result[key] = value` invokes\nlegacy `__proto__` behavior. Explicit own data properties preserve ordinary\nobject compatibility while making dangerous key names inert.\n\n**Why sandbox backgrounds too?** The same author controls component and\nbackground HTML. Leaving either path unsandboxed bypasses the host boundary.\n\n**Why allow scripts but not same-origin?** Bundled fixtures need scripts; an\nopaque origin reduces host privilege. Additional tokens are not required.\n\n**Why a finite event observer?** It proves renderer-controlled creation order;\nseparate endpoint assertions avoid claiming continuous post-return monitoring.\n\n**Why no real-browser claim?** jsdom is an attribute/data oracle, not a browser\nsecurity-enforcement oracle.\n\n---\n\n## 6. Definition of Done\n\n### 6.1 Data Model (mirrors 2.1)\n\n- [ ] `RenderOptions.preserveHtmlText` exists and omission is effectively false.\n- [ ] Source selection and iframe-event values match the normative enums in\n      black-box tests; no new public runtime type is required unless useful.\n- [ ] Constants use exact values `html`, `text`, and `allow-scripts`.\n\n### 6.2 Architecture (mirrors 2.2)\n\n- [ ] Initial render and `$render` follow the body-mode pipeline through safe\n      transform and sandboxed component/background iframe creation.\n- [ ] Body roots, headers, footers, sections, layouts, layers, backgrounds,\n      arrays, directives, and nested action/options/payload objects inherit mode.\n- [ ] `executeAction()` option/continuation transforms and direct standalone\n      transforms keep omitted/default-false mode and generic ordering.\n- [ ] Safe own-property definition applies in both modes, `$set`, `$cache.set`,\n      `$global.set`, both session storage levels, and own-only session lookup.\n- [ ] Component/action handler registries dispatch only own registered functions;\n      trigger and `$lambda` named-action lookups accept only own action entries.\n- [ ] Exact own/inherited prototype-name vectors satisfy label/unknown/trigger/\n      undefined outcomes without invoking inherited members.\n\n### 6.3 Vocabulary (mirrors 2.3)\n\n- [ ] Classification uses final resolved own type and protected resolved own text.\n- [ ] Output fields are safe own data properties; iframe source fields are read\n      only when own.\n- [ ] Sandbox value and renderer-owned observation window match the normative\n      definitions.\n\n### 6.4 Body Mode (mirrors 3.1)\n\n- [ ] Initial body-template rendering explicitly enables body mode.\n- [ ] Actual `$render` re-rendering explicitly enables body mode.\n- [ ] Arrays, nested objects, and all conditional/each directive recursion carry\n      the same option.\n- [ ] The exact section 3.2 `executeAction($set, state, {value:\"ACTION\"})`\n      production vector produces `state.local.probe.text === \"ACTION\"`; direct\n      standalone and unrelated-option transforms also prove generic behavior.\n- [ ] An embedded body action option is raw immediately after body transform and\n      follows existing generic interpolation when that action executes.\n\n### 6.5 Safe Object Transformation (mirrors 3.2)\n\n- [ ] Every exact transform vector in section 3.2 passes.\n- [ ] Protected strings satisfy strict equality; protected objects retain strict\n      reference identity.\n- [ ] Observable getter logs prove per-frame all-keys-first body ordering and\n      existing per-entry off-mode ordering for flat/nested vectors; numeric keys\n      prove ECMAScript order `\"1\"`, `\"2\"`, `alpha`; each expression evaluates once.\n- [ ] Later type collisions activate/deactivate protection according to final\n      type; duplicate text collisions remain raw and use last-write-wins.\n- [ ] Missing, unresolved, and non-string type values do not activate protection.\n- [ ] `#if`, `#elseif`, `#else`, `#each`, arrays, and every listed body location\n      receive protection.\n- [ ] Embedded body action HTML shapes remain raw while separate generic action\n      transforms interpolate.\n- [ ] `__proto__`, `constructor`, and `prototype` become inert enumerable,\n      writable, configurable own data properties without prototype mutation in\n      both modes, while preserving their applicable getter/error order.\n- [ ] End-to-end `$set`, `$cache.set`, and `$global.set` vectors retain destination\n      `Object.prototype` and inert own descriptors.\n- [ ] Two-level session vectors prove stored-session dangerous option descriptors,\n      reachable outer domains `__proto__`/`constructor`/`prototype`, both\n      prototypes, null-prototype input, and the reachable inherited\n      `api.example.com` request-decoration rejection.\n- [ ] Enumeration uses own enumerable string keys only.\n- [ ] Direct inherited/missing/non-string/non-html/prototype-colliding component,\n      background, and action type vectors follow exact label/unknown/trigger/\n      undefined outcomes, invoke no inherited handler, and do not throw.\n- [ ] Full transform-to-render inherited text/url/css and mixed inherited/own\n      vectors obey own-field classification and source selection.\n- [ ] Existing unresolved-expression behavior remains unchanged.\n\n### 6.6 Source Selection and Composition (mirrors 3.3)\n\n- [ ] Every string-only source-selection row passes, including dual source,\n      whitespace, empty/non-string, inherited-only/mixed-own, and no source.\n- [ ] Non-string text/URL values are ignored rather than coerced.\n- [ ] Every CSS vector passes with exact runtime equality, including arbitrary\n      non-empty whitespace retained code-unit-for-code-unit.\n- [ ] Mixed-case/repeated closing-style replacements contain runtime U+005C.\n- [ ] Invalid HTML is assigned without parsing/sanitizing/rewriting.\n- [ ] Source selection and emitted attributes match the exact string-domain\n      vectors; arbitrary coercion is not claimed.\n\n### 6.7 Iframe Creation and Lifetime (mirrors 3.4)\n\n- [ ] Component inline and URL traces match the exact observer alphabet/order.\n- [ ] Background inline and URL traces match the exact observer alphabet/order.\n- [ ] Sandbox is each iframe's first security-observed operation, precedes every\n      parent insertion/source event, and is exact `allow-scripts`.\n- [ ] Each created iframe has one sandbox event before return; endpoint checks\n      after initial render and `$render` find exact sandbox on old/new objects\n      without claiming continuous monitoring.\n- [ ] Inline has `srcdoc` and no `src`; URL has `src` and no `srcdoc`.\n- [ ] No-source and inherited-only-source cases create no iframe/per-iframe trace;\n      mixed inherited/own cases follow their own field.\n- [ ] Component wrapper is detached at `renderComponent()` return, owns the\n      iframe child, and has exact class/data type/iframe width/border.\n- [ ] Background iframe is a direct renderer-root child with exact class/ARIA and\n      remains before foreground content.\n\n### 6.8 Renderer Integration (mirrors 3.5)\n\n- [ ] Initial DOM matches every section 3.5 selector/value expectation.\n- [ ] Actual `executeAction($render, renderer.getState())` creates a different\n      iframe node, changes height `40px -> 80px`, and label `first -> second`.\n- [ ] The new iframe retains literal raw script braces and excludes `SECOND`;\n      both old and new iframe objects have exact sandbox endpoint state.\n- [ ] Rendered Jasonpedia iframe `srcdoc` contains `img{width: 100%;}` and\n      `Nexus devices`.\n- [ ] Own-path canonical/legacy background nullish precedence\u2014including inherited\n      canonical/style/legacy segments\u2014and independent full type/source/CSS matrices\n      satisfy exact sandbox/source/ARIA/trace or no-source outcomes.\n- [ ] Rendered component/background HTML retains authored CSS/script contents and\n      ordinary sibling interpolation.\n\n### 6.9 Evidence and Verification (mirrors 3.6)\n\n- [ ] jsdom evidence covers transform output, equality/identity, descriptors,\n      prototypes, strings, exclusive attributes, sandbox tokens, and traces.\n- [ ] No report claims jsdom proved script execution, origin enforcement, host\n      access denial, browser navigation, or network isolation.\n- [ ] The first seven exact commands in section 3.6 pass. The eighth (full Web)\n      passes directly or satisfies the exact documented CLI-timeout substitute.\n- [ ] Final full-Web evidence satisfies one passing row of the acceptance truth\n      table; a CLI timeout requires isolated `cli.test.ts` pass and exact\n      implementation-SHA Web CI pass.\n\n### 6.10 Integration Smoke (mirrors 3.7)\n\n- [ ] The integration smoke test runs the actual initial and `$render` paths,\n      proves DOM liveness through node/style/label changes, preserves raw text,\n      proves omitted-option default behavior, and validates all observed iframe\n      traces.\n"
    },
    {
      "label": "source_spec",
      "kind": "spec",
      "sha256": "8fea114614d0f76e5dc441cd61ddcfee629bacf7a1b332122d68f4f2906ce997",
      "content": "---\ndate: 2026-07-10\ntopic: Web HTML template and iframe safety boundary\nstatus: active\nresearch: docs/research/2026-07-10-web-html-component-safety-research.md\n---\n\n# Specification: Web HTML Template and Iframe Safety Boundary\n\n## Problem Statement\n\nThe Web renderer evaluates every string in a body template, including privileged\n`html.text`, and creates both HTML-component and HTML-background iframes without\na sandbox. Dynamic/resolved keys also flow into ordinary JavaScript object\nassignment, where `__proto__` can mutate an output object's prototype. These\npaths violate the v2 raw-HTML boundary and permit equivalent HTML sources to\nreceive inconsistent privilege. The renderer must preserve HTML raw values only\nin an opt-in body-template mode, construct safe own-property outputs, and apply\nan opaque script-capable sandbox before assigning any HTML iframe source.\n\n## Actors and Boundaries\n\n- **Document author:** supplies body templates, actions embedded in that tree,\n  HTML components, and HTML backgrounds.\n- **Template engine:** resolves keys, directives, and values.\n- **Web renderer:** enables raw-text protection for body-template transforms,\n  including initial rendering and `$render`.\n- **HTML renderers:** construct component and background iframes.\n- **Browser:** enforces the emitted `sandbox=\"allow-scripts\"` policy.\n- **Host page:** owns application DOM, state, and storage outside the iframes.\n\nThe raw-text classifier is deliberately **shape-based inside the complete body\ntree**. Any regular object under the body root whose final resolved own `type`\nvalue is exact string `html` protects resolved own `text` entries, including an\nobject nested inside action/options/payload data authored in that body tree.\nSeparate action/options/payload/data transforms invoked outside body rendering\nremain generic because the option defaults off.\n\nThe body tree includes body roots, headers, footers, sections, layouts, layers,\nbackgrounds, nested arrays, and values emitted by `#if`, `#elseif`, `#else`, and\n`#each`.\n\n## Requirements\n\n- **R1. Explicit body scope.** Add `RenderOptions.preserveHtmlText?: boolean`\n  with effective default `false`. Initial and `$render` body-template transforms\n  MUST pass `true`. `executeAction()` option/continuation transforms and direct\n  standalone `transform()` calls omit the option and remain generic.\n- **R2. Mode matrix.** In body mode, use all-keys-first classification: resolve\n  all own enumerable string keys once in ECMAScript `Object.keys()` order, then\n  transform resolved `type` values, then transform/retain output values. With the\n  mode omitted/false, retain existing per-entry key-then-value evaluation order\n  and do not pre-transform types. Safe own-property definition applies in both.\n  A numeric-key vector MUST prove `Object.keys({\"2\":...,\"1\":...,alpha:...})`\n  order `\"1\"`, `\"2\"`, `\"alpha\"` rather than source-text order.\n- **R3. Literal raw values.** Under final type `html` in body mode, every entry\n  whose resolved key is `text` MUST retain its original in-memory value without\n  evaluation, coercion, normalization, or recursive traversal. String output\n  MUST satisfy strict equality (`===`); object output retains reference identity.\n- **R4. Deterministic safe output and state sinks.** Enumerate only own\n  enumerable string keys in ECMAScript `Object.keys()` order. Later definitions\n  win. In both modes, every result key\u2014including `__proto__`, `constructor`, and\n  `prototype`\u2014MUST be an enumerable, writable, configurable own data property\n  without prototype mutation. `$set`, `$cache.set`, and `$global.set` use the\n  same safe-copy semantics. `$session.set` safely copies transformed options into\n  a fresh stored-session object and safely defines that object under the normalized\n  domain in `state.sessions`; both objects retain `Object.prototype`. Session\n  lookup ignores inherited domains. HTML dispatch/rendering ignores inherited\n  `type`, `text`, `url`, and `css`. Null-prototype source objects remain valid.\n- **R4a. Own-callable registries.** Component and action dispatch select only an\n  own registered function. Component missing/inherited/non-string type follows\n  the existing label default; an own unknown/prototype-colliding string returns\n  the visible unknown-component element. Action dispatch reads only own `type`\n  and own `trigger`; inherited type is missing. A missing/non-string type with an\n  own string trigger uses own-entry named-action lookup. `$lambda` uses the same\n  lookup for `options.name`. Own named entries may be action objects or arrays;\n  inherited entries are ignored. Own unknown/prototype-colliding action type\n  returns `undefined` without a continuation. Background rendering requires own\n  exact string `html`. No case invokes inherited members or throws.\n- **R5. Shape-based body exception.** Resolved `type` values and all entries\n  other than protected resolved `text` continue through normal transformation.\n  Protection applies recursively everywhere in a body tree, including embedded\n  action/options/payload objects. If such an embedded action later executes,\n  `executeAction()` performs its existing second generic options transform; no\n  new promise is made that raw values stay raw after that separate phase.\n- **R6. Source domain and selection.** Only own non-empty string `text` or `url`\n  values are valid sources. Non-string, empty, missing, and inherited values are\n  ignored. Valid text wins over valid URL. This follows the protocol's string\n  types and removes arbitrary JavaScript coercion from the boundary.\n- **R7. Exact inline composition.** CSS is present only when the own transformed\n  value is a non-empty string. Without CSS, `srcdoc === text`. With CSS, it\n  equals `<style>${escaped_css}</style>${text}`. Every case-insensitive\n  `</style` becomes U+003C, U+005C, U+002F, then lowercase `style`; everything\n  else is unchanged.\n- **R8. Sandbox both HTML iframe paths.** Every HTML component/background iframe\n  MUST receive exact `sandbox=\"allow-scripts\"`, without `allow-same-origin` or\n  any additional token.\n- **R9. Security order and endpoint state.** For each created iframe, sandbox\n  assignment is the first security-observed operation and precedes every iframe\n  parent insertion, source assignment, and renderer return. From creation through\n  return, exactly one sandbox event occurs: the required initial set. Tests check\n  exact sandbox endpoint state after initial render and after `$render`; no claim\n  is made about continuous post-return mutation monitoring.\n- **R10. Source-attribute exclusivity.** Inline mode has `srcdoc` and no `src`.\n  URL mode has `src` and no `srcdoc`; an empty property is not equivalent to an\n  absent attribute.\n- **R11. Evidence boundary.** jsdom tests verify transformation, descriptors,\n  prototypes, exact strings/attributes, and finite operation traces. They MUST\n  NOT claim browser script execution or opaque-origin enforcement.\n- **R12. Named regression behavior.** `components.test.ts` HTML class/size/border\n  behavior and `integration.test.ts` rendered Jasonpedia CSS/content remain\n  green. Invalid HTML is assigned unchanged. URL validation/network policy and\n  real-browser coercion/security are outside this slice rather than claimed\n  unchanged by jsdom.\n\n## Behaviors\n\n### Behavior: Transform a Body-Tree Object\n\n- **Trigger:** `transform()` encounters a regular object.\n- **Generic/off process:** Enumerate own enumerable string keys in insertion\n  order. For each entry, resolve its key and immediately transform its value,\n  preserving existing evaluation order. Define the result through safe own data\n  property semantics from R4. Do not classify or protect HTML text.\n- **Body/on process:**\n  1. Resolve all authored keys exactly once in insertion order.\n  2. Transform every value whose resolved key is `type`, in insertion order.\n  3. Use the final such own value to classify the object.\n  4. Iterate entries in insertion order. Reuse pre-transformed type values;\n     retain resolved text values raw only under exact final type `html`; transform\n     all other values normally.\n  5. Define each resolved key as a safe own data property; later definitions in\n     ECMAScript `Object.keys()` order replace earlier own values.\n- **Output:** A normal object whose prototype remains exactly `Object.prototype`,\n  whose dangerous-name keys are inert own properties, and whose raw text is\n  protected only under the shape/body-mode contract.\n- **Errors:** Existing expression errors/unresolved values retain current\n  behavior. Non-string final type does not activate protection.\n\nOrdering is per regular-object frame. Context property getters append labels to\nan external log: a flat body-mode vector logs all dynamic-key getters before any\ndynamic-type getter, then ordinary value getters. A nested vector applies the\nsame rule per frame; child events occur when that child value is transformed. An\nobject with own keys `\"2\"`, `\"1\"`, and `alpha` proves ECMAScript order\n`\"1\"`, `\"2\"`, `alpha`. No internal trace hook is required.\n\nWith body mode off, the same getters MUST preserve existing per-entry\nkey-then-value order. Dangerous resolved keys still use safe own definitions in\nboth modes and do not affect expression/error order.\n\nAuthoritative vectors:\n\n| Input/context/mode | Exact relevant output |\n|---|---|\n| `{type:\"html\",text:\"<p>{{secret}}</p>\",style:{height:\"{{height}}\"}}`; `{secret:\"LEAK\",height:40}`; on | `{type:\"html\",text:\"<p>{{secret}}</p>\",style:{height:40}}` |\n| `{type:\"{{kind}}\",\"{{slot}}\":\"<p>{{secret}}</p>\"}`; `{kind:\"html\",slot:\"text\",secret:\"LEAK\"}`; on | `{type:\"html\",text:\"<p>{{secret}}</p>\"}` |\n| `{\"{{typeKey}}\":\"{{kind}}\",text:\"{{secret}}\"}`; `{typeKey:\"type\",kind:\"html\",secret:\"LEAK\"}`; on | `{type:\"html\",text:\"{{secret}}\"}` |\n| `{type:\"html\",\"{{slot}}\":\"first\",text:\"second\"}`; `{slot:\"text\"}`; on | `{type:\"html\",text:\"second\"}` |\n| `{type:\"label\",text:\"{{secret}}\"}`; `{secret:\"OK\"}`; on | `{type:\"label\",text:\"OK\"}` |\n| `{type:\"html\",text:\"{{secret}}\"}`; `{secret:\"GENERIC\"}`; off/omitted | `{type:\"html\",text:\"GENERIC\"}` |\n\nAdditional required vectors:\n\n- A later resolved `type` changing `html` to `label` transforms text; reverse\n  order retains it raw.\n- A protected object-valued text entry retains the same reference.\n- HTML shapes emitted by every conditional directive and `#each`, and nested in\n  each listed body location, receive protection.\n- An embedded body-tree action option `{type:\"html\",text:\"{{secret}}\"}` retains\n  raw text immediately after body transformation. A production vector invokes\n  `executeAction({type:\"$set\",options:{probe:{type:\"html\",text:\"{{$jason.value}}\"}}}, state, {value:\"ACTION\"})`;\n  the resulting `state.local.probe.text` is `ACTION`, proving action-time generic\n  transformation. A second `$set` vector carries own `__proto__`, `constructor`,\n  and `prototype` options and proves `state.local` keeps `Object.prototype` while\n  receiving inert own properties. `$cache.set` and `$global.set` receive\n  equivalent coverage. `$session.set` separately proves (1) the stored session\n  safely owns dangerous option keys while retaining `Object.prototype`, and (2)\n  `state.sessions` safely owns reachable normalized domains `__proto__`,\n  `constructor`, and `prototype` while retaining `Object.prototype`. A lookup\n  rejection vector sets\n  `state.sessions = Object.create({\"api.example.com\": inheritedSession})`, then\n  requests `https://api.example.com`; inherited headers/body do not decorate the\n  request. Null-prototype input options remain supported.\n- Resolved `__proto__`, `constructor`, and `prototype` are inert enumerable own\n  properties in body and off modes; the result prototype remains\n  `Object.prototype`.\n- Enumeration excludes inherited properties. Direct renderer vectors (without a\n  transform first) prove only own exact string `type: \"html\"` enters component or\n  background HTML paths. Inherited/missing/non-string/non-html and own\n  `__proto__`/`constructor`/`prototype` types create no HTML iframe and do not\n  throw. `executeAction()` receives the same type matrix and treats every\n  non-own/non-string/non-callable/prototype-colliding type as unknown without\n  invoking inherited handlers. Full transform-to-render vectors cover inherited\n  text/url/css, inherited text plus own URL, inherited URL plus own text, and\n  inherited CSS plus own text.\n- A direct `executeAction()` vector proves its production options transform\n  remains generic. An embedded action shape is raw immediately after body\n  transformation; its later execution follows existing generic option templating.\n\n### Behavior: Select and Compose an HTML Source\n\n| Own text | Own URL | Result |\n|---|---|---|\n| non-empty string (including whitespace) | any | INLINE; text wins |\n| empty/non-string/missing | non-empty string | URL |\n| empty/non-string/missing | empty/non-string/missing | NONE |\n| inherited-only text/url | any | ignore inherited fields |\n\nCSS vectors (shown values are exact runtime strings):\n\n| HTML | Own CSS | Exact `srcdoc` |\n|---|---|---|\n| `<p>x</p>` | absent/empty/null/undefined/non-string/inherited | `<p>x</p>` |\n| `<p>x</p>` | one U+0020 space | `<style> </style><p>x</p>` |\n| `<p>x</p>` | tab+newline | same tab+newline retained inside `<style>...</style>` |\n| `<p>x</p>` | `</STYLE>` | `<style><\\/style></style><p>x</p>` |\n| `<p>x</p>` | `a</style>b</StYlE>c` | `<style>a<\\/style>b<\\/style>c</style><p>x</p>` |\n\nA JavaScript assertion encodes one runtime U+005C with two source backslashes:\n\n```javascript\nexpect(htmlSrcdoc(\"<p>x</p>\", \"</STYLE>\"))\n  .toBe(\"<style><\\\\/style></style><p>x</p>\");\n```\n\nInvalid HTML is not parsed, sanitized, or rewritten before assignment.\n\n### Behavior: Create and Maintain HTML Iframes\n\nAt `renderComponent()` return, the `.jasonette-html` wrapper remains detached\nfrom the live document and owns its iframe child. The background path appends\n`.jasonette-background-web` as a direct child of the renderer root. Both use\nthe same security policy and source exclusivity.\n\nSecurity trace observer records only:\n\n- `CREATE(kind)` when `document.createElement(\"iframe\")` returns, where `kind` is\n  assigned by the test call site as `component` or `background`;\n- `SANDBOX(value)` for sandbox set/remove/replace operations;\n- `SOURCE(name)` for `srcdoc` or `src` assignment;\n- `APPEND` for every insertion of that iframe into any parent;\n- `RETURN` when the component wrapper or background render call returns.\n\nPure computation, property reads, class/ARIA operations, and individual style\nreads/writes are excluded. The required creation trace is exactly:\n\n```text\nCREATE(kind) -> SANDBOX(\"allow-scripts\") -> SOURCE(\"srcdoc\"|\"src\")\n-> APPEND -> RETURN\n```\n\nFor component mode, `RETURN` means `renderComponent()` returned its wrapper. For\nbackground mode, it means `renderBodyBackground()` returned. Events are grouped\nper created iframe. Each created iframe has one sandbox event: the required\ninitial set. No additional sandbox event occurs before return. `RETURN` belongs\nonly to a created-iframe trace; no-source calls create no per-iframe trace.\nEndpoint assertions check exact sandbox state after initial render and after\n`$render`; they do not claim continuous post-return observation.\n\n### Behavior: Renderer Integration and `$render` Liveness\n\nUse this body-template shape with initial data\n`{kind:\"html\",secret:\"LEAK\",height:40,label:\"first\"}`:\n\n```json\n{\n  \"sections\": [{\n    \"items\": [\n      {\n        \"type\": \"{{$jason.kind}}\",\n        \"text\": \"<script>window.__html_boundary = \\\"{{$jason.secret}}\\\"</script>\",\n        \"style\": {\"height\": \"{{$jason.height}}\"}\n      },\n      {\"type\": \"label\", \"text\": \"{{$jason.label}}\"}\n    ]\n  }]\n}\n```\n\nInitial exact DOM expectations:\n\n- `.jasonette-html iframe` contains literal `{{$jason.secret}}` in `srcdoc`;\n- `.jasonette-html` has `style.height === \"40px\"`;\n- `.jasonette-label` has `textContent === \"first\"`;\n- iframe sandbox is exact `allow-scripts`, inline iframe has no `src` attribute.\n\nInvoke the actual action path:\n\n```javascript\nawait executeAction({\n  type: \"$render\",\n  options: {\n    data: {kind: \"html\", secret: \"SECOND\", height: 80, label: \"second\"}\n  }\n}, renderer.getState());\n```\n\nPost-action expectations:\n\n- the queried iframe is a different DOM node from the initial iframe;\n- `.jasonette-html` has `style.height === \"80px\"`;\n- label text is `second`;\n- the new iframe still contains literal `{{$jason.secret}}`, not `SECOND`.\n\n`Jasonpedia/view/component/html/index.json` MUST be rendered through\n`JasonetteRenderer`; its iframe `srcdoc` must contain both\n`img{width: 100%;}` and `Nexus devices`. Synthetic script assertions inspect the\nrendered iframe `srcdoc`, not fixture source text.\n\nAuthoritative background vectors (each snippet is placed in the named body slot\ninside a complete `JasonDocument`):\n\n| Entry path | Authored object and context | Expected source |\n|---|---|---|\n| static `body.background` | `{type:\"html\",text:\"<p>static</p>\",css:\"p{color:red}\"}` | inline exact `<style>p{color:red}</style><p>static</p>` |\n| static `body.style.background` | `{type:\"html\",url:\"https://example.com/bg.html\"}` | URL exact authored URL, no `srcdoc` attribute |\n| templated `head.templates.body.background` | `{type:\"html\",text:\"<p>{{$jason.secret}}</p>\",css:\"p{color:{{$jason.color}}}\"}` with `{secret:\"LEAK\",color:\"red\"}` | inline raw literal secret expression and transformed red CSS |\n| templated `head.templates.body.style.background` | `{type:\"html\",url:\"{{$jason.url}}\"}` with `{url:\"https://example.com/dynamic.html\"}` | URL dynamic URL, no `srcdoc` attribute |\n| either path | `{type:\"html\"}` | no iframe/per-iframe trace |\n\nBackground location selection examines own path segments only and then applies\nnullish precedence:\n\n| Own canonical `body.background` | Own `body.style` object with own `background` | Selected value |\n|---|---|---|\n| non-nullish (including false, empty, malformed, or HTML with no source) | any | canonical; never fall back |\n| null or undefined/missing/inherited | present | legacy |\n| null/undefined/missing/inherited | absent/inherited | none |\n\nDirect vectors cover inherited canonical background with own legacy fallback,\ninherited `style` with no canonical value, inherited legacy background inside an\nown style object, and each inherited path combined with no fallback.\n\nFor a selected own exact `type: \"html\"` object, the complete component source\nand CSS matrices apply independently to the background path. Canonical and\nlegacy paths each cover inline, URL, dual-source, non-string/empty sources,\nrepeated CSS escaping, inherited fields, prototype-colliding type values, and no\nsource. Rows that create an iframe assert exact sandbox, exclusive attributes,\nclass `.jasonette-background-web`, `aria-hidden=\"true\"`, and source. No-source\nrows assert iframe absence and an empty per-iframe trace.\n\n## Key Decisions\n\n- **Shape-based body-tree protection:** safer and simpler than maintaining a\n  fragile list of component-bearing structural positions. Embedded action data\n  in a body tree is intentionally protected; separate action transforms remain\n  generic.\n- **Safe own-property definition:** preserves ordinary-object compatibility while\n  making dangerous resolved key names inert.\n- **Both HTML iframe paths are sandboxed:** a document author controls both, so\n  leaving backgrounds unsandboxed would bypass the boundary.\n- **Scripts but no same-origin:** preserves legacy interactive fixtures while\n  isolating host DOM/storage privileges.\n- **Finite renderer-owned observation:** proves creation-through-return ordering\n  and retained final state without claiming control over external DOM mutation.\n- **Policy evidence, not browser enforcement:** jsdom proves emitted behavior;\n  browser enforcement remains a platform assumption.\n\n## Scope Boundaries\n\n- **In scope:** template-engine option and safe output semantics; all body-tree\n  call sites; component/background iframe sandbox/order/source exclusivity;\n  focused and integration tests.\n- **Out of scope:** HTML sanitization; CSP; Permission Policy; Referrer Policy;\n  URL scheme/origin/redirect rules; cookies; referrer leakage; browser execution\n  tests; native HTML renderers; agent bridges; additional sandbox capabilities.\n- **Future:** real-browser tests may verify child script execution and denied host\n  DOM/storage access. Any additional iframe capability requires explicit review.\n\n## Success Criteria\n\n- Every normative transform/source/CSS/dangerous-key/body-location vector passes.\n- Initial and actual `$render` action paths satisfy the liveness/DOM oracle.\n- Component inline, component URL, background inline, and background URL traces\n  match the exact event alphabet/order and have exclusive source attributes.\n- Component wrappers retain class `jasonette-html`, data type `html`, iframe\n  style width `100%`/border `none`, and detached-at-return topology; backgrounds\n  retain direct-root parentage, class, ARIA, and placement before foreground.\n- Initial and replacement iframe endpoint sandbox states are exact after the\n  actual `$render`; the old iframe object also retains its exact attribute after\n  renderer disconnection.\n- No-source and inherited-only-source cases emit no iframe; mixed inherited/own\n  cases follow the own field.\n- jsdom claims remain limited to emitted policy and data.\n- Exact required commands:\n\n```text\nnpm run test --workspace=@jasonette/template-engine -- transformer.test.ts\nnpm run test --workspace=@jasonette/web -- components.test.ts integration.test.ts actions-parity.test.ts renderer.test.ts\nnpm run typecheck --workspace=@jasonette/template-engine\nnpm run typecheck --workspace=@jasonette/web\nnpm run build --workspace=@jasonette/template-engine\nnpm run build --workspace=@jasonette/web\nnpm run test --workspace=@jasonette/template-engine\nnpm run test --workspace=@jasonette/web\n```\n\nAcceptance truth table for the final full Web suite:\n\n| Local full Web suite | Isolated `cli.test.ts` | Exact implementation-SHA Web CI | Slice acceptance |\n|---|---|---|---|\n| pass | not needed | pass | pass |\n| only CLI timeout; all other Web tests pass | pass | pass | pass with local timeout documented |\n| any non-CLI failure | any | any | fail |\n| CLI timeout | fail | any | fail |\n| any | any | missing/fail | fail |\n\n## Open Questions\n\n### Resolved\n\n- Protection is shape-based across the whole body tree.\n- Dangerous resolved keys become inert own data properties.\n- Both HTML component and background iframes are sandboxed.\n- URL mode requires absence, not emptiness, of `srcdoc`.\n- The strict event invariant ends at render-function return; exact endpoint state\n  is checked after initial render and `$render` without continuous monitoring.\n\n### Deferred\n\n- Browser enforcement, URL/origin policy, CSP, permissions/referrers, and extra\n  sandbox capabilities require separate specifications.\n"
    },
    {
      "label": "red_suite_support_round3",
      "kind": "red_test_code",
      "sha256": "176337bcc7ea8531f1fe8a032fc49b6d7bd6e87debbd4f2b180c5d668b6ba60e",
      "content": "===== features/web-html-safety.feature =====\nFeature: Web HTML template and iframe safety boundary\n  The jsdom suite treats transformed data and emitted DOM attributes as evidence.\n  It does not treat jsdom as evidence of browser sandbox enforcement, script\n  execution, origin isolation, host-access denial, navigation, or networking.\n\n  Scenario Outline: The normative transform matrix has exact output\n    Given template <template> and context <context>\n    And preserveHtmlText is <mode>\n    When transform processes the template\n    Then the exact output is <output>\n    And descriptors are recursively checked on every transformed output key\n    Examples:\n      | template                                                               | context                                      | mode    | output                                                    |\n      | {\"type\":\"html\",\"text\":\"<p>{{secret}}</p>\",\"style\":{\"height\":\"{{height}}\"}} | {\"secret\":\"LEAK\",\"height\":40}            | true    | {\"type\":\"html\",\"text\":\"<p>{{secret}}</p>\",\"style\":{\"height\":40}} |\n      | {\"type\":\"{{kind}}\",\"{{slot}}\":\"<p>{{secret}}</p>\"}                | {\"kind\":\"html\",\"slot\":\"text\",\"secret\":\"LEAK\"} | true    | {\"type\":\"html\",\"text\":\"<p>{{secret}}</p>\"}         |\n      | {\"{{typeKey}}\":\"{{kind}}\",\"text\":\"{{secret}}\"}                   | {\"typeKey\":\"type\",\"kind\":\"html\",\"secret\":\"LEAK\"} | true | {\"type\":\"html\",\"text\":\"{{secret}}\"}                |\n      | {\"type\":\"html\",\"{{slot}}\":\"first\",\"text\":\"second\"}            | {\"slot\":\"text\"}                            | true    | {\"type\":\"html\",\"text\":\"second\"}                    |\n      | {\"type\":\"label\",\"text\":\"{{secret}}\"}                             | {\"secret\":\"OK\"}                            | true    | {\"type\":\"label\",\"text\":\"OK\"}                       |\n      | {\"type\":\"html\",\"text\":\"{{secret}}\"}                              | {\"secret\":\"GENERIC\"}                       | omitted | {\"type\":\"html\",\"text\":\"GENERIC\"}                   |\n\n  Scenario Outline: The final resolved type controls raw-text protection\n    Given template <template> and context {\"typeAgain\":\"type\",\"secret\":\"VALUE\"}\n    And preserveHtmlText is true\n    When transform processes the template\n    Then the exact output is <output>\n    Examples:\n      | template                                                        | output                                    |\n      | {\"type\":\"html\",\"{{typeAgain}}\":\"label\",\"text\":\"{{secret}}\"} | {\"type\":\"label\",\"text\":\"VALUE\"}     |\n      | {\"type\":\"label\",\"{{typeAgain}}\":\"html\",\"text\":\"{{secret}}\"} | {\"type\":\"html\",\"text\":\"{{secret}}\"} |\n\n  Scenario: Duplicate protected text entries remain raw before the later value wins\n    Given two authored text entries resolve to text under final type html\n    And both raw expressions have throwing context getters\n    When transform runs in body mode\n    Then neither expression getter is invoked\n    And the exact output text is the later authored raw expression\n\n  Scenario: Protected string and object values preserve identity\n    Given one HTML shape has string text and another has object-valued text\n    When both shapes are transformed in body mode\n    Then the output string is strictly equal to the authored string\n    And the output object is the identical authored reference\n\n  Scenario Outline: A non-HTML final type does not protect text\n    Given template <template> and context <context>\n    When transform runs with preserveHtmlText true\n    Then output text is exactly \"VALUE\"\n    Examples:\n      | template                                      | context                         |\n      | {\"text\":\"{{secret}}\"}                      | {\"secret\":\"VALUE\"}            |\n      | {\"type\":\"{{missing}}\",\"text\":\"{{secret}}\"} | {\"secret\":\"VALUE\"}         |\n      | {\"type\":\"{{kind}}\",\"text\":\"{{secret}}\"} | {\"kind\":7,\"secret\":\"VALUE\"} |\n      | {\"type\":\"{{kind}}\",\"text\":\"{{secret}}\"} | {\"kind\":null,\"secret\":\"VALUE\"} |\n\n  Scenario: Unresolved expressions have exact output in body and generic modes\n    Given a label has text \"prefix {{missing}} suffix\" and nested value \"{{alsoMissing}}\"\n    When it is transformed once in body mode and once in generic mode\n    Then both exact outputs retain \"prefix {{missing}} suffix\"\n    And both exact nested outputs retain \"{{alsoMissing}}\"\n\n  Scenario: Body mode has deterministic all-keys-first ordering\n    Given keyType, keyValue, kind, and ordinary context getters append their names\n    When a flat body object is transformed\n    Then the exact log is keyType, keyValue, kind, ordinary\n    And every expression is read exactly once\n\n  Scenario: Ordering is scoped independently to nested object frames\n    Given a parent and child each contain dynamic keys and type values\n    When the parent is transformed in body mode\n    Then the parent key and type reads precede child reads\n    And the child applies all-keys-first when the child value is visited\n\n  Scenario Outline: Generic mode preserves per-entry ordering\n    Given dynamic key and value getters append their names\n    And preserveHtmlText is <mode>\n    When transform processes two entries\n    Then the exact log is keyType, kind, keyValue, ordinary\n    Examples:\n      | mode    |\n      | omitted |\n      | false   |\n\n  Scenario: Numeric keys use ECMAScript Object.keys order\n    Given keys were authored in order 2, 1, alpha\n    When the object is transformed\n    Then output keys and value-getter logs are exactly 1, 2, alpha\n\n  Scenario: Body recursion protects the root and all nine named nested HTML paths\n    Given HTML shapes occur at root, header, footer, section item, layout, layer, background, nested array, action options, and action payload\n    When renderSync transforms each body shape with preserveHtmlText true\n    Then root text and each of the nine named path texts remain exactly \"<p>{{secret}}</p>\"\n    And each ordinary note sibling is exactly \"OK\"\n    And descriptors are recursively safe throughout each transformed output\n\n  Scenario Outline: Each directive output inherits body mode\n    Given a <directive> fixture whose selected value is HTML text <raw>\n    When renderSync selects the named branch in body mode\n    Then the exact emitted text is <raw>\n    Examples:\n      | directive | raw                 |\n      | #if       | IF {{secret}}       |\n      | #elseif   | ELSEIF {{secret}}   |\n      | #else     | ELSE {{secret}}     |\n      | #each     | ROW {{secret}}      |\n\n  Scenario: Embedded action data is raw during body transformation\n    Given an embedded $set option has text \"{{$jason.value}}\" under type html\n    When the containing body is transformed with preserveHtmlText true\n    Then the exact embedded text remains \"{{$jason.value}}\"\n\n  Scenario: Embedded action data is generic when that action executes\n    Given a body contains an embedded $set action with text \"{{$jason.value}}\"\n    When the body is transformed with preserveHtmlText true\n    And executeAction runs the resulting action with action context value ACTION\n    Then state.local.probe.text is exactly \"ACTION\"\n\n  Scenario Outline: Standalone transforms remain generic\n    Given an HTML shape has text \"{{secret}}\" and secret GENERIC\n    And transform receives <options>\n    When transform processes the shape\n    Then output text is exactly \"GENERIC\"\n    Examples:\n      | options                            |\n      | omitted                            |\n      | {\"preserveHtmlText\":false}         |\n      | {\"unrelated\":true}                 |\n\n  Scenario Outline: Dangerous resolved keys are inert in both modes\n    Given a dynamic key resolves to <key>\n    When the object is transformed once with body mode and once with generic mode\n    Then each result retains Object.prototype\n    And <key> is an own enumerable writable configurable data property\n    Examples:\n      | key         |\n      | __proto__   |\n      | constructor |\n      | prototype   |\n\n  Scenario Outline: Dangerous-key getters retain exact mode order and later definitions win\n    Given dangerous resolves to <key> and all key and value expressions use logging getters\n    When body and generic transforms process the object\n    Then the body log is dangerous, ordinaryKey, dangerousValue, ordinaryValue\n    And the generic log is dangerous, dangerousValue, ordinaryKey, ordinaryValue\n    And a later definition of <key> wins as an inert own data property\n    Examples:\n      | key         |\n      | __proto__   |\n      | constructor |\n      | prototype   |\n\n  Scenario Outline: Dangerous-key failures retain exact getter order\n    Given dangerousKey resolves to <key> and laterKey and dangerousValue are context getters\n    And dangerousValue throws DANGEROUS_VALUE_SENTINEL\n    When body and generic transforms encounter the throwing value\n    Then the body log is dangerousKey, laterKey, dangerousValue\n    And the generic log is dangerousKey, dangerousValue\n    And each transform throws DANGEROUS_VALUE_SENTINEL\n    Examples:\n      | key         |\n      | __proto__   |\n      | constructor |\n      | prototype   |\n\n  Scenario: Only own enumerable string source keys are transformed\n    Given a null-prototype source owns type and text\n    And inherited, non-enumerable, and symbol fields are present\n    When it is transformed\n    Then the exact output owns only type and text\n    And its prototype is Object.prototype\n\n  Scenario Outline: State copy sinks safely define dangerous keys\n    Given null-prototype options own __proto__, constructor, and prototype\n    When <action> executes\n    Then destination <destination> retains Object.prototype\n    And all three keys are inert own data properties\n    Examples:\n      | action      | destination |\n      | $set        | local       |\n      | $cache.set  | cache       |\n      | $global.set | global      |\n\n  Scenario: Session inner storage safely copies dangerous keys\n    Given null-prototype session options own __proto__, constructor, prototype, domain, and header\n    When $session.set stores them under safe.example.com\n    Then the stored session is a fresh ordinary object\n    And all dangerous keys have inert own data descriptors\n\n  Scenario Outline: Session outer storage safely defines dangerous domains\n    Given session options use domain <domain>\n    When $session.set executes\n    Then state.sessions retains Object.prototype\n    And <domain> is a reachable inert own session property\n    Examples:\n      | domain      |\n      | __proto__   |\n      | constructor |\n      | prototype   |\n\n  Scenario: Own session lookup decorates a matching request\n    Given state.sessions owns api.example.com with header X-Own-Session\n    When a request targets https://api.example.com/resource\n    Then normalized fetch Headers return exact X-Own-Session value OWN-SESSION-SENTINEL\n\n  Scenario: Inherited session lookup is rejected\n    Given state.sessions inherits api.example.com with hostile headers and body\n    When a request targets https://api.example.com/resource\n    Then the fetch options contain neither hostile header nor inherited body sentinel\n\n  Scenario Outline: Component dispatch has a concrete own-type outcome\n    Given a component has <type-shape>\n    When renderComponent dispatches it\n    Then its exact outcome is <outcome>\n    And no inherited member is invoked\n    Examples:\n      | type-shape          | outcome                         |\n      | own type html       | HTML iframe                     |\n      | inherited type html | label default without iframe    |\n      | missing type        | label default without iframe    |\n      | own type 7          | label default without iframe    |\n      | own type null       | label default without iframe    |\n      | own type __proto__  | visible unknown component       |\n      | own type constructor| visible unknown component       |\n      | own type prototype  | visible unknown component       |\n      | own type toString   | visible unknown component       |\n\n  Scenario: An inherited callable component renderer is never invoked\n    Given the component handler registry inherits a throwing callable sentinel\n    When public renderComponent receives that sentinel name as an own string type\n    Then the exact outcome is a visible unknown component without an iframe\n    And the inherited callable invocation count is exactly zero\n\n  Scenario: An inherited callable action handler is never invoked\n    Given the action handler registry inherits a throwing callable sentinel\n    When executeAction dispatches that sentinel name as an own action type\n    Then the exact result is undefined and no continuation runs\n    And the inherited action-handler invocation count is exactly zero\n\n  Scenario: An inherited callable named action is never invoked\n    Given state.actions inherits a throwing callable sentinel\n    When executeAction looks up that name through trigger and $lambda\n    Then both exact results are undefined and no continuation runs\n    And the inherited named-action invocation count is exactly zero\n\n  Scenario Outline: Named action lookup has a concrete own-entry outcome\n    Given the <registry> registry has a <ownership> <name> action entry\n    When executeAction performs the lookup\n    Then the exact outcome is <outcome>\n    Examples:\n      | registry | ownership | name        | outcome   |\n      | trigger  | own       | toString    | executes  |\n      | trigger  | inherited | toString    | ignored   |\n      | trigger  | own       | constructor | executes  |\n      | trigger  | inherited | constructor | ignored   |\n      | trigger  | own       | __proto__   | executes  |\n      | trigger  | inherited | __proto__   | ignored   |\n      | $lambda  | own       | toString    | executes  |\n      | $lambda  | inherited | toString    | ignored   |\n      | $lambda  | own       | constructor | executes  |\n      | $lambda  | inherited | constructor | ignored   |\n      | $lambda  | own       | __proto__   | executes  |\n      | $lambda  | inherited | __proto__   | ignored   |\n\n  Scenario: An inherited action type falls through to an own trigger\n    Given an action inherits type $set and owns trigger fallback\n    And state.actions owns fallback as a $set action\n    When executeAction dispatches the action\n    Then fallback runs\n    And inherited $set options do not run\n\n  Scenario Outline: Component source selection uses only non-empty own strings\n    Given an own exact HTML component has <text> text and <url> URL\n    When renderComponent renders it\n    Then the selected source is <result>\n    Examples:\n      | text                       | url                           | result |\n      | \"<p>inline</p>\"            | \"https://example.com/ignored\" | inline |\n      | \" \"                        | \"https://example.com/ignored\" | inline |\n      | \"\"                         | \"https://example.com/url\"     | URL    |\n      | number 42                  | \"https://example.com/url\"     | URL    |\n      | missing                    | missing                       | none   |\n      | \"\"                         | \"\"                            | none   |\n      | object                     | number 17                     | none   |\n      | inherited text and URL     | no own URL                    | none   |\n      | inherited text             | \"https://example.com/own\"     | URL    |\n      | \"<p>own</p>\"               | inherited URL                 | inline |\n\n  Scenario: Non-string HTML source and CSS values are never coerced\n    Given text, URL, and CSS objects throw from toPrimitive, toString, and valueOf\n    When no-source and valid-text components are rendered\n    Then no coercion sentinel is logged\n    And non-string text and URL create no iframe\n    And non-string CSS leaves valid text unchanged\n\n  Scenario Outline: Background paths never coerce text URL or CSS\n    Given a complete document whose <path> has throwing coercion sentinels for own text and URL\n    And a second complete document has valid text and a throwing own CSS sentinel at <path>\n    When JasonetteRenderer renders the no-source document\n    And JasonetteRenderer renders the valid-text document\n    Then all text, URL, and CSS sentinel invocation counts are exactly zero\n    And the no-source render has no transient iframe trace\n    And the valid-text srcdoc is exactly \"<p>own</p>\"\n    Examples:\n      | path                         |\n      | body.background              |\n      | body.style.background        |\n\n  Scenario Outline: Inline CSS composition is exact\n    Given HTML text is the authored paragraph-x string\n    And own CSS is <css>\n    When the component is rendered inline\n    Then srcdoc is exactly <srcdoc>\n    Examples:\n      | css                         | srcdoc                                           |\n      | missing                     | <p>x</p>                                         |\n      | \"\"                          | <p>x</p>                                         |\n      | null                        | <p>x</p>                                         |\n      | undefined                   | <p>x</p>                                         |\n      | number 12                   | <p>x</p>                                         |\n      | inherited p{color:red}      | <p>x</p>                                         |\n      | one U+0020                  | <style> </style><p>x</p>                         |\n      | tab plus newline            | <style>\\t\\n</style><p>x</p>                     |\n      | U+0020,U+0009,U+000A,U+000D,U+000C,U+0020 | <style> \\t\\n\\r\\f </style><p>x</p> |\n      | </STYLE>                    | <style><\\/style></style><p>x</p>                 |\n      | a</style>b</StYlE>c         | <style>a<\\/style>b<\\/style>c</style><p>x</p>    |\n\n  Scenario: Invalid HTML is emitted unchanged\n    Given authored HTML is \"<p><unclosed>&{{not_evaluated}}<script>\"\n    When it is assigned to an inline iframe\n    Then srcdoc strictly equals the authored string\n    And no parser or sanitizer behavior is claimed\n\n  Scenario: The iframe observer catches equivalent mutation APIs\n    Given the iframe security observer is installed\n    When Attr and NamedNodeMap APIs mutate sandbox and source\n    And replaceChildren inserts the iframe\n    And the observed operation returns\n    Then it records CREATE, SANDBOX, SOURCE, APPEND, RETURN in exact order\n\n  Scenario Outline: The iframe observer catches insertion into any pre-existing parent before policy\n    Given a detached <parent> exists before the iframe security observer is installed\n    When a created iframe is appended to that parent\n    And sandbox and source are assigned after the insertion\n    And the observed operation returns\n    Then it records CREATE, APPEND, SANDBOX, SOURCE, RETURN in exact order\n    Examples:\n      | parent           |\n      | DocumentFragment |\n      | Element          |\n\n  Scenario: The iframe observer catches replacements removals and alternate insertion\n    Given the iframe security observer is installed\n    When equivalent DOM APIs replace and remove sandbox\n    And a reflected property assigns source\n    And insertAdjacentElement inserts the iframe\n    And the observed operation returns\n    Then every sandbox mutation, source mutation, and insertion is recorded in order\n\n  Scenario Outline: Component iframe creation follows the exact security trace\n    Given an HTML component has <source> source\n    When public renderComponent returns\n    Then its exact trace is <trace>\n    And exactly one sandbox event occurred\n    Examples:\n      | source | trace                                                                       |\n      | inline | CREATE(component), SANDBOX(allow-scripts), SOURCE(srcdoc), APPEND, RETURN    |\n      | URL    | CREATE(component), SANDBOX(allow-scripts), SOURCE(src), APPEND, RETURN       |\n\n  Scenario: Component iframe endpoint and topology are exact\n    Given an own HTML component is authored with dimensions\n    When public renderComponent returns its wrapper\n    Then the wrapper is detached and owns the iframe\n    And class, data type, width, border, sandbox, and exclusive srcdoc are exact\n\n  Scenario Outline: Background iframe creation follows the exact public render trace\n    Given a complete document has an HTML background with <source> source\n    When JasonetteRenderer renders it through public renderDocument behavior\n    Then the observed background trace is <trace>\n    Examples:\n      | source | trace                                                                       |\n      | inline | CREATE(background), SANDBOX(allow-scripts), SOURCE(srcdoc), APPEND, RETURN   |\n      | URL    | CREATE(background), SANDBOX(allow-scripts), SOURCE(src), APPEND, RETURN      |\n\n  Scenario: Background endpoint and topology are exact\n    Given a background and foreground section are authored\n    When JasonetteRenderer renders the document\n    Then the iframe is a direct renderer-root child\n    And exact class jasonette-background-web and aria-hidden true are present\n    And the iframe precedes foreground content\n\n  Scenario: A component with malformed own source fields creates no iframe trace\n    Given an own HTML component has empty or non-string text and URL fields\n    When public renderComponent renders it with the iframe observer installed\n    Then no iframe is created\n    And no per-iframe trace exists\n\n  Scenario: A component with inherited-only source fields creates no iframe trace\n    Given an own HTML component inherits text and URL without owning either field\n    When public renderComponent renders it with the iframe observer installed\n    Then no iframe is created\n    And no per-iframe trace exists\n\n  Scenario: A background with malformed own source fields creates no transient iframe trace\n    Given a complete document has an own HTML background with empty or non-string text and URL fields\n    When public renderDocument renders it with the iframe observer installed\n    Then no background iframe is created\n    And no per-iframe trace exists\n\n  Scenario: A background with inherited-only source fields creates no transient iframe trace\n    Given a complete document has an own HTML background that inherits text and URL\n    When public renderDocument renders it with the iframe observer installed\n    Then no background iframe is created\n    And no per-iframe trace exists\n\n  Scenario Outline: Transform-to-render source fields remain own-only\n    Given an authored HTML shape has <fields>\n    When transform processes it and public renderComponent renders the result\n    Then the exact source outcome is <outcome>\n    Examples:\n      | fields                         | outcome                           |\n      | inherited text URL and CSS     | no iframe                         |\n      | inherited text plus own URL    | own URL                           |\n      | inherited URL plus own text    | own text                          |\n      | inherited CSS plus own text    | own text without inherited style  |\n\n  Scenario Outline: Each background location has an exact source\n    Given a complete document uses <location>\n    When JasonetteRenderer renders it\n    Then the exact emitted source is <source>\n    And sandbox and source exclusivity are exact\n    Examples:\n      | location                                  | source                                                   |\n      | body.background                           | <style>p{color:red}</style><p>static</p>                 |\n      | body.style.background                     | https://example.com/bg.html                              |\n      | head.templates.body.background            | <style>p{color:red}</style><p>{{$jason.secret}}</p>      |\n      | head.templates.body.style.background      | https://example.com/dynamic.html                         |\n\n  Scenario Outline: Canonical background uses nullish own-path precedence\n    Given canonical background is <canonical>\n    And legacy background is valid inline HTML\n    When JasonetteRenderer renders the document with the iframe observer installed\n    Then the exact selection is <selection>\n    And a no-iframe selection has no transient iframe trace\n    Examples:\n      | canonical                  | selection          |\n      | false                      | no iframe          |\n      | empty object               | no iframe          |\n      | {\"type\":\"html\"}          | no iframe          |\n      | null                       | legacy inline      |\n      | undefined                  | legacy inline      |\n      | missing                    | legacy inline      |\n\n  Scenario: An inherited canonical background uses an own legacy fallback\n    Given body inherits a valid canonical HTML background\n    And body owns a valid legacy HTML background\n    When JasonetteRenderer renders the document with the iframe observer installed\n    Then the exact emitted source is the own legacy inline HTML\n    And the inherited canonical source is absent\n\n  Scenario: An inherited style path segment is ignored\n    Given body inherits a style containing a valid HTML background\n    And body has no own canonical background\n    When JasonetteRenderer renders the document with the iframe observer installed\n    Then no background iframe is created\n    And the observer records no transient iframe trace\n\n  Scenario: An inherited legacy background path segment is ignored\n    Given body owns style and that style inherits a valid HTML background\n    And body has no own canonical background\n    When JasonetteRenderer renders the document with the iframe observer installed\n    Then no background iframe is created\n    And the observer records no transient iframe trace\n\n  Scenario: An inherited canonical path without fallback is ignored\n    Given body inherits a valid canonical HTML background\n    And body has no own canonical or legacy background\n    When JasonetteRenderer renders the document with the iframe observer installed\n    Then no background iframe is created\n    And the observer records no transient iframe trace\n\n  Scenario Outline: Canonical and legacy backgrounds run the complete concrete matrix\n    Given a complete document places a background at <path>\n    And its type is <type>, text is <text>, URL is <url>, and CSS is <css>\n    And its inherited field shape is <inherited>\n    When JasonetteRenderer renders the document through public renderDocument behavior with the iframe observer installed\n    Then the exact background outcome is <outcome>\n    And the exact emitted source is <source>\n    And exact sandbox, source exclusivity, class, ARIA, and observer evidence matches <outcome> with <source-event>\n    And every no-iframe outcome has no transient iframe trace\n\n    Examples: Canonical body.background matrix\n      | path             | type            | text                      | url                             | css                       | inherited        | outcome   | source                                           | source-event   |\n      | body.background  | own html        | \"<p>inline</p>\"          | missing                         | missing                   | none             | inline    | <p>inline</p>                                    | SOURCE(srcdoc) |\n      | body.background  | own html        | missing                   | \"https://example.com/url\"       | missing                   | none             | URL       | https://example.com/url                          | SOURCE(src)    |\n      | body.background  | own html        | \"<p>dual</p>\"            | \"https://example.com/ignored\"   | missing                   | none             | inline    | <p>dual</p>                                      | SOURCE(srcdoc) |\n      | body.background  | own html        | one U+0020                 | \"https://example.com/ignored\"   | missing                   | none             | inline    | one U+0020                                       | SOURCE(srcdoc) |\n      | body.background  | own html        | empty string               | \"https://example.com/url\"       | missing                   | none             | URL       | https://example.com/url                          | SOURCE(src)    |\n      | body.background  | own html        | number 9                   | \"https://example.com/url\"       | missing                   | none             | URL       | https://example.com/url                          | SOURCE(src)    |\n      | body.background  | own html        | empty string               | empty string                    | missing                   | none             | no iframe | absent                                           | none           |\n      | body.background  | own html        | object                     | number 17                       | missing                   | none             | no iframe | absent                                           | none           |\n      | body.background  | own html        | \"<p>x</p>\"               | missing                         | empty string              | none             | inline    | <p>x</p>                                         | SOURCE(srcdoc) |\n      | body.background  | own html        | \"<p>x</p>\"               | missing                         | null                      | none             | inline    | <p>x</p>                                         | SOURCE(srcdoc) |\n      | body.background  | own html        | \"<p>x</p>\"               | missing                         | undefined                 | none             | inline    | <p>x</p>                                         | SOURCE(srcdoc) |\n      | body.background  | own html        | \"<p>x</p>\"               | missing                         | number 12                 | none             | inline    | <p>x</p>                                         | SOURCE(srcdoc) |\n      | body.background  | own html        | \"<p>x</p>\"               | missing                         | one U+0020                | none             | inline    | <style> </style><p>x</p>                         | SOURCE(srcdoc) |\n      | body.background  | own html        | \"<p>x</p>\"               | missing                         | U+0009 then U+000A        | none             | inline    | <style>\\t\\n</style><p>x</p>                    | SOURCE(srcdoc) |\n      | body.background  | own html        | \"<p>x</p>\"               | missing                         | U+0020,U+0009,U+000A,U+000D,U+000C,U+0020 | none | inline | <style> \\t\\n\\r\\f </style><p>x</p> | SOURCE(srcdoc) |\n      | body.background  | own html        | \"<p>x</p>\"               | missing                         | </STYLE>                  | none             | inline    | <style><\\/style></style><p>x</p>                | SOURCE(srcdoc) |\n      | body.background  | own html        | \"<p>x</p>\"               | missing                         | a</style>b</StYlE>c       | none             | inline    | <style>a<\\/style>b<\\/style>c</style><p>x</p>  | SOURCE(srcdoc) |\n      | body.background  | own html        | \"<p><unclosed>&raw\"       | missing                         | missing                   | none             | inline    | <p><unclosed>&raw                                | SOURCE(srcdoc) |\n      | body.background  | own html        | missing                   | missing                         | missing                   | text URL and CSS | no iframe | absent                                           | none           |\n      | body.background  | own html        | missing                   | \"https://example.com/own\"       | missing                   | text             | URL       | https://example.com/own                          | SOURCE(src)    |\n      | body.background  | own html        | \"<p>own</p>\"             | missing                         | missing                   | URL              | inline    | <p>own</p>                                       | SOURCE(srcdoc) |\n      | body.background  | own html        | \"<p>own</p>\"             | missing                         | missing                   | CSS              | inline    | <p>own</p>                                       | SOURCE(srcdoc) |\n      | body.background  | own html        | empty string               | number 12                       | missing                   | none             | no iframe | absent                                           | none           |\n      | body.background  | own html        | missing                   | missing                         | missing                   | none             | no iframe | absent                                           | none           |\n      | body.background  | missing         | \"<p>x</p>\"               | missing                         | missing                   | none             | no iframe | absent                                           | none           |\n      | body.background  | own label       | \"<p>x</p>\"               | missing                         | missing                   | none             | no iframe | absent                                           | none           |\n      | body.background  | own __proto__   | \"<p>x</p>\"               | missing                         | missing                   | none             | no iframe | absent                                           | none           |\n      | body.background  | own constructor | \"<p>x</p>\"               | missing                         | missing                   | none             | no iframe | absent                                           | none           |\n      | body.background  | own prototype   | \"<p>x</p>\"               | missing                         | missing                   | none             | no iframe | absent                                           | none           |\n      | body.background  | own toString    | \"<p>x</p>\"               | missing                         | missing                   | none             | no iframe | absent                                           | none           |\n      | body.background  | own number 4    | \"<p>x</p>\"               | missing                         | missing                   | none             | no iframe | absent                                           | none           |\n      | body.background  | inherited html  | \"<p>x</p>\"               | missing                         | missing                   | type             | no iframe | absent                                           | none           |\n\n    Examples: Legacy body.style.background matrix\n      | path                    | type            | text                      | url                             | css                       | inherited        | outcome   | source                                           | source-event   |\n      | body.style.background   | own html        | \"<p>inline</p>\"          | missing                         | missing                   | none             | inline    | <p>inline</p>                                    | SOURCE(srcdoc) |\n      | body.style.background   | own html        | missing                   | \"https://example.com/url\"       | missing                   | none             | URL       | https://example.com/url                          | SOURCE(src)    |\n      | body.style.background   | own html        | \"<p>dual</p>\"            | \"https://example.com/ignored\"   | missing                   | none             | inline    | <p>dual</p>                                      | SOURCE(srcdoc) |\n      | body.style.background   | own html        | one U+0020                 | \"https://example.com/ignored\"   | missing                   | none             | inline    | one U+0020                                       | SOURCE(srcdoc) |\n      | body.style.background   | own html        | empty string               | \"https://example.com/url\"       | missing                   | none             | URL       | https://example.com/url                          | SOURCE(src)    |\n      | body.style.background   | own html        | number 9                   | \"https://example.com/url\"       | missing                   | none             | URL       | https://example.com/url                          | SOURCE(src)    |\n      | body.style.background   | own html        | empty string               | empty string                    | missing                   | none             | no iframe | absent                                           | none           |\n      | body.style.background   | own html        | object                     | number 17                       | missing                   | none             | no iframe | absent                                           | none           |\n      | body.style.background   | own html        | \"<p>x</p>\"               | missing                         | empty string              | none             | inline    | <p>x</p>                                         | SOURCE(srcdoc) |\n      | body.style.background   | own html        | \"<p>x</p>\"               | missing                         | null                      | none             | inline    | <p>x</p>                                         | SOURCE(srcdoc) |\n      | body.style.background   | own html        | \"<p>x</p>\"               | missing                         | undefined                 | none             | inline    | <p>x</p>                                         | SOURCE(srcdoc) |\n      | body.style.background   | own html        | \"<p>x</p>\"               | missing                         | number 12                 | none             | inline    | <p>x</p>                                         | SOURCE(srcdoc) |\n      | body.style.background   | own html        | \"<p>x</p>\"               | missing                         | one U+0020                | none             | inline    | <style> </style><p>x</p>                         | SOURCE(srcdoc) |\n      | body.style.background   | own html        | \"<p>x</p>\"               | missing                         | U+0009 then U+000A        | none             | inline    | <style>\\t\\n</style><p>x</p>                    | SOURCE(srcdoc) |\n      | body.style.background   | own html        | \"<p>x</p>\"               | missing                         | U+0020,U+0009,U+000A,U+000D,U+000C,U+0020 | none | inline | <style> \\t\\n\\r\\f </style><p>x</p> | SOURCE(srcdoc) |\n      | body.style.background   | own html        | \"<p>x</p>\"               | missing                         | </STYLE>                  | none             | inline    | <style><\\/style></style><p>x</p>                | SOURCE(srcdoc) |\n      | body.style.background   | own html        | \"<p>x</p>\"               | missing                         | a</style>b</StYlE>c       | none             | inline    | <style>a<\\/style>b<\\/style>c</style><p>x</p>  | SOURCE(srcdoc) |\n      | body.style.background   | own html        | \"<p><unclosed>&raw\"       | missing                         | missing                   | none             | inline    | <p><unclosed>&raw                                | SOURCE(srcdoc) |\n      | body.style.background   | own html        | missing                   | missing                         | missing                   | text URL and CSS | no iframe | absent                                           | none           |\n      | body.style.background   | own html        | missing                   | \"https://example.com/own\"       | missing                   | text             | URL       | https://example.com/own                          | SOURCE(src)    |\n      | body.style.background   | own html        | \"<p>own</p>\"             | missing                         | missing                   | URL              | inline    | <p>own</p>                                       | SOURCE(srcdoc) |\n      | body.style.background   | own html        | \"<p>own</p>\"             | missing                         | missing                   | CSS              | inline    | <p>own</p>                                       | SOURCE(srcdoc) |\n      | body.style.background   | own html        | empty string               | number 12                       | missing                   | none             | no iframe | absent                                           | none           |\n      | body.style.background   | own html        | missing                   | missing                         | missing                   | none             | no iframe | absent                                           | none           |\n      | body.style.background   | missing         | \"<p>x</p>\"               | missing                         | missing                   | none             | no iframe | absent                                           | none           |\n      | body.style.background   | own label       | \"<p>x</p>\"               | missing                         | missing                   | none             | no iframe | absent                                           | none           |\n      | body.style.background   | own __proto__   | \"<p>x</p>\"               | missing                         | missing                   | none             | no iframe | absent                                           | none           |\n      | body.style.background   | own constructor | \"<p>x</p>\"               | missing                         | missing                   | none             | no iframe | absent                                           | none           |\n      | body.style.background   | own prototype   | \"<p>x</p>\"               | missing                         | missing                   | none             | no iframe | absent                                           | none           |\n      | body.style.background   | own toString    | \"<p>x</p>\"               | missing                         | missing                   | none             | no iframe | absent                                           | none           |\n      | body.style.background   | own number 4    | \"<p>x</p>\"               | missing                         | missing                   | none             | no iframe | absent                                           | none           |\n      | body.style.background   | inherited html  | \"<p>x</p>\"               | missing                         | missing                   | type             | no iframe | absent                                           | none           |\n\n  Scenario: Initial body rendering remains live and safe\n    Given section 3.5's exact body template and initial data\n    When JasonetteRenderer performs initial rendering\n    Then srcdoc contains the exact raw script with \"{{$jason.secret}}\"\n    And height is 40px, label is first, sandbox is allow-scripts, and src is absent\n\n  Scenario: The actual $render action replaces the component safely\n    Given section 3.5's exact initial document and replacement data are available\n    When JasonetteRenderer performs the initial render\n    And executeAction performs $render with SECOND, height 80, and label second\n    Then a different iframe contains raw \"{{$jason.secret}}\" and excludes SECOND\n    And height is 80px and label is second\n    And old and new iframe sandbox endpoints are exactly allow-scripts\n\n  Scenario: The exact production executeAction vector remains generic\n    Given section 3.7's $set action has text \"{{$jason.value}}\"\n    When executeAction runs with action context value ACTION\n    Then state.local.probe.text is exactly \"ACTION\"\n\n  Scenario: Actual render replaces a background safely\n    Given initial and replacement data select different HTML background URLs\n    When JasonetteRenderer performs the initial render\n    And the actual $render action runs\n    Then the background iframe node changes\n    And old and new endpoint sandbox attributes remain exactly allow-scripts\n\n  Scenario: Rendered HTML retains authored content and sibling interpolation\n    Given component and background HTML contain authored CSS and script text\n    And ordinary label and style siblings contain expressions\n    When the document renders\n    Then HTML script text stays authored\n    And CSS, label, and style siblings interpolate exactly\n\n  Scenario: Jasonpedia fixture assertions inspect renderer output\n    Given support/fixtures/jasonpedia-html-index.json is imported as allowed test data\n    When JasonetteRenderer renders that document\n    Then the rendered iframe srcdoc contains \"img{width: 100%;}\"\n    And the rendered iframe srcdoc contains \"Nexus devices\"\n\n  Scenario: Evidence remains within the jsdom boundary\n    Given the black-box suite observes transforms and emitted DOM\n    When it evaluates safety expectations\n    Then it asserts equality, identity, descriptors, prototypes, strings, attributes, tokens, topology, and finite traces\n    But it does not claim browser execution, opaque-origin enforcement, host denial, navigation, or network isolation\n\n  Scenario: Repository commands and exact-SHA CI are external pending evidence\n    Given Definition of Done items 6.9.3 and 6.9.4 are process obligations\n    When coverage metadata classifies those items\n    Then both are typed external with pending evidence\n    And neither item names an executable Vitest declaration\n\n\n===== support/fixtures/jasonpedia-html-index.json =====\n{\n  \"$jason\": {\n    \"head\": {\n      \"title\": \"HTML\"\n    },\n    \"body\": {\n      \"header\": {\n        \"menu\": {\n          \"text\": \"View JSON\",\n          \"href\": {\n            \"url\": \"https://github.com/Bande-a-Bonnot/JASONETTE-Reborn/blob/main/Jasonpedia/view/component/html/index.json\",\n            \"view\": \"web\"\n          }\n        }\n      },\n      \"sections\": [{\n        \"items\": [{\n          \"type\": \"html\",\n          \"css\": \"img{width: 100%;} p{font-family: Helvetica; font-size: 14px;}\",\n          \"text\": \"<img alt=\\\"\\\" src=\\\"https://cdn0.vox-cdn.com/thumbor/PPvtkzgfvglcclm7DNwByCxjY1M=/0x0:2500x1667/1310x873/cdn0.vox-cdn.com/uploads/chorus_image/image/51427661/nexus-6p-9736.0.0.jpg\\\" /> <p>Well it turns out Nexus devices <i>are</i> getting Android 7.1 at the same time as Google's new Pixel phones \u2014 or at least a developer preview build of it. This evening Google rolled out a preview release of 7.1 Nougat for the Nexus 6P, 5X, and Pixel C tablet. If your device is installed in the Android Beta program, you can install the update right now to get new features like Night Light, long-press app shortcuts, support for new circular icons, and optimizations to touch and display responsiveness among other changes.</p> <p>\\\"We\u2019re delivering the initial Developer Preview at beta quality for the Nexus lineup of devices,\\\" Google's Dave Burke wrote in a <a href=\\\"http://android-developers.blogspot.com/2016/10/android71-dev-preview-available.html\\\" target=\\\"_blank\\\">blog post announcing the release</a>. \\\"The goal is to tease out any device-specific issues.\\\" There's a...</p> <p> <a href=\\\"http://www.theverge.com/circuitbreaker/2016/10/19/13340688/google-android-7-1-developer-preview-now-available\\\">Continue reading&hellip;</a> </p>\"\n        }]\n      }]\n    }\n  }\n}\n\n\n===== support/public-api.md =====\n# Public API Contract Available to Red\n\nTests run in the Jasonette Reborn npm workspace and may import only these public package APIs:\n\n- `@jasonette/template-engine`: `transform`, `renderSync`; types `RenderContext`, `RenderOptions`.\n- `@jasonette/web`: `JasonetteRenderer`, `renderComponent`, `executeAction`; public document/action/state types.\n\nUse Vitest and jsdom. Do not reference implementation file paths. Write executable tests under `tests/` and Gherkin scenarios under `features/`. The runner will copy these black-box tests into an isolated repository checkout with built workspace packages.\n\nAllowed test data:\n- `support/fixtures/jasonpedia-html-index.json` is a copied public Jasonpedia fixture and may be imported/rendered by black-box tests.\n\nExternal evidence obligations (workspace commands and exact-SHA CI) must be represented as external/pending coverage metadata, not fabricated Vitest assertions. The orchestrator supplies that evidence after implementation.\n\n\n===== tests/coverage-map.json =====\n{\n  \"schemaVersion\": 2,\n  \"specification\": \"Web HTML Template and Iframe Safety Boundary NLSpec\",\n  \"executableReferenceKind\": \"exact globally unique Vitest test title\",\n  \"definitionOfDone\": {\n    \"6.1.1\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"RenderOptions.preserveHtmlText exists and omission is effectively false.\",\n      \"tests\": [\n        \"transform defaults: omitted, false, and unrelated options remain generic\",\n        \"transform matrix: omitted body option\"\n      ]\n    },\n    \"6.1.2\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Source selection and iframe-event values match the normative enums.\",\n      \"tests\": [\n        \"component source matrix: text wins over URL\",\n        \"component source matrix: missing values select none\",\n        \"component trace: inline has exact finite creation order\",\n        \"background trace: URL has exact finite creation order\"\n      ]\n    },\n    \"6.1.3\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Constants use exact values html, text, and allow-scripts.\",\n      \"tests\": [\n        \"transform matrix: literal HTML text with transformed style\",\n        \"component topology: detached wrapper owns an exact class/data/style iframe\",\n        \"background locations: static canonical inline CSS\"\n      ]\n    },\n    \"6.2.1\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Initial render and $render follow safe body mode and sandboxed component/background creation.\",\n      \"tests\": [\n        \"integration smoke: exact initial and actual $render paths preserve raw HTML and remain live\",\n        \"renderer liveness: actual $render replaces background and retains old/new sandbox endpoints\"\n      ]\n    },\n    \"6.2.2\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Every body location, arrays, directives, and nested action/options/payload objects inherit mode.\",\n      \"tests\": [\n        \"renderSync recursion: body roots and every named nested location preserve HTML shapes\",\n        \"renderSync directives: #if, #elseif, and #else outputs retain body mode\",\n        \"renderSync directives: #each outputs and nested arrays retain body mode\"\n      ]\n    },\n    \"6.2.3\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Action option/continuation transforms and standalone transforms remain generic and ordered.\",\n      \"tests\": [\n        \"executeAction production vector: action options remain generic\",\n        \"executeAction continuation vector: continuation options retain generic interpolation\",\n        \"transform ordering: omitted and false modes preserve per-entry key-then-value evaluation\"\n      ]\n    },\n    \"6.2.4\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Safe definitions cover both transform modes, state sinks, both session levels, and own session lookup.\",\n      \"tests\": [\n        \"safe output: resolved __proto__ is inert in body and generic modes\",\n        \"safe state sink: $set defines inert own keys\",\n        \"safe state sink: $cache.set defines inert own keys\",\n        \"safe state sink: $global.set defines inert own keys\",\n        \"session inner storage: null-prototype options become a fresh safe ordinary session\",\n        \"session outer storage: normalized domain __proto__ is an inert reachable own key\",\n        \"session lookup control: own api.example.com decorates a network request\",\n        \"session lookup: inherited api.example.com does not decorate a network request\"\n      ]\n    },\n    \"6.2.5\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Component/action registries and trigger/$lambda named actions use own entries only.\",\n      \"tests\": [\n        \"component dispatch: only own exact html enters the HTML renderer\",\n        \"component registry: an inherited callable renderer is ignored with exact unknown-component outcome\",\n        \"action type registry: an inherited callable handler returns undefined and is never invoked\",\n        \"named action registry: an inherited callable is ignored by trigger and $lambda with zero invocations\",\n        \"named trigger registry: inherited prototype-colliding entry toString is ignored\",\n        \"$lambda registry: inherited prototype-colliding entry constructor is ignored\",\n        \"named registry: own trigger and lambda action arrays execute\"\n      ]\n    },\n    \"6.2.6\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Exact own/inherited prototype-name vectors produce label/unknown/trigger/undefined without inherited invocation.\",\n      \"tests\": [\n        \"component dispatch: own unknown type __proto__ is visible and never calls a prototype member\",\n        \"component dispatch: own unknown type constructor is visible and never calls a prototype member\",\n        \"component registry: an inherited callable renderer is ignored with exact unknown-component outcome\",\n        \"action type registry: an inherited callable handler returns undefined and is never invoked\",\n        \"named action registry: an inherited callable is ignored by trigger and $lambda with zero invocations\",\n        \"named trigger registry: own prototype-colliding entry __proto__ executes\",\n        \"action type registry: own unknown type constructor returns undefined without continuation\",\n        \"action type registry: inherited, missing, and non-string types invoke no inherited handler\"\n      ]\n    },\n    \"6.3.1\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Classification uses final resolved own type and protected resolved own text.\",\n      \"tests\": [\n        \"transform classification: later label type deactivates raw-text protection\",\n        \"transform classification: later HTML type activates raw-text protection\",\n        \"transform classification: duplicate protected text entries stay raw and the later value wins\",\n        \"transform matrix: dynamic resolved type key\"\n      ]\n    },\n    \"6.3.2\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Output fields are safe own data properties and iframe fields are own-only.\",\n      \"tests\": [\n        \"safe output: resolved constructor is inert in body and generic modes\",\n        \"component source matrix: inherited-only values select none\",\n        \"transform-to-render: inherited text, URL, and CSS are excluded while mixed own fields win\"\n      ]\n    },\n    \"6.3.3\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Sandbox value and finite renderer-owned window match definitions.\",\n      \"tests\": [\n        \"component trace: URL has exact finite creation order\",\n        \"background trace: inline has exact finite creation order\",\n        \"evidence boundary: jsdom assertions inspect emitted policy and data, not browser enforcement\"\n      ]\n    },\n    \"6.4.1\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Initial body-template rendering enables body mode.\",\n      \"tests\": [\n        \"integration smoke: exact initial and actual $render paths preserve raw HTML and remain live\"\n      ]\n    },\n    \"6.4.2\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Actual $render re-rendering enables body mode.\",\n      \"tests\": [\n        \"integration smoke: exact initial and actual $render paths preserve raw HTML and remain live\"\n      ]\n    },\n    \"6.4.3\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Arrays, nested objects, and every conditional/each recursion carry mode.\",\n      \"tests\": [\n        \"renderSync recursion: body roots and every named nested location preserve HTML shapes\",\n        \"renderSync directives: #if, #elseif, and #else outputs retain body mode\",\n        \"renderSync directives: #each outputs and nested arrays retain body mode\"\n      ]\n    },\n    \"6.4.4\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Exact production $set vector yields ACTION and standalone/unrelated options are generic.\",\n      \"tests\": [\n        \"integration smoke: exact production executeAction vector yields ACTION\",\n        \"transform defaults: omitted, false, and unrelated options remain generic\"\n      ]\n    },\n    \"6.4.5\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Embedded body action is raw then interpolates when executed.\",\n      \"tests\": [\n        \"executeAction embedded vector: body-raw action options interpolate during their second generic transform\"\n      ]\n    },\n    \"6.5.1\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Every exact section 3.2 transform vector passes.\",\n      \"tests\": [\n        \"transform matrix: literal HTML text with transformed style\",\n        \"transform matrix: dynamic type and text keys\",\n        \"transform matrix: dynamic resolved type key\",\n        \"transform matrix: later duplicate resolved text\",\n        \"transform matrix: non-HTML label\",\n        \"transform matrix: omitted body option\"\n      ]\n    },\n    \"6.5.2\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Protected strings and objects retain strict identity.\",\n      \"tests\": [\n        \"transform identity: protected strings and objects retain strict identity\"\n      ]\n    },\n    \"6.5.3\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Getter logs prove flat/nested ordering, numeric order, and once-only evaluation.\",\n      \"tests\": [\n        \"transform ordering: body mode resolves all flat keys, then types, then ordinary values exactly once\",\n        \"transform ordering: nested frames each apply all-keys-first at visitation time\",\n        \"transform ordering: omitted and false modes preserve per-entry key-then-value evaluation\",\n        \"transform ordering: numeric keys use Object.keys order 1, 2, alpha\"\n      ]\n    },\n    \"6.5.4\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Final type and duplicate text collisions obey activation and last-write-wins.\",\n      \"tests\": [\n        \"transform classification: later label type deactivates raw-text protection\",\n        \"transform classification: later HTML type activates raw-text protection\",\n        \"transform classification: duplicate protected text entries stay raw and the later value wins\",\n        \"transform classification sentinel: overwritten raw text never evaluates either expression\"\n      ]\n    },\n    \"6.5.5\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Missing, unresolved, and non-string types do not protect.\",\n      \"tests\": [\n        \"transform classification: missing, unresolved, and non-string types do not protect\",\n        \"unresolved expressions: body and generic modes return the exact unresolved strings\"\n      ]\n    },\n    \"6.5.6\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Directives, arrays, and every body location receive protection.\",\n      \"tests\": [\n        \"renderSync recursion: body roots and every named nested location preserve HTML shapes\",\n        \"renderSync directives: #if, #elseif, and #else outputs retain body mode\",\n        \"renderSync directives: #each outputs and nested arrays retain body mode\"\n      ]\n    },\n    \"6.5.7\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Embedded action shapes stay raw while separate action transforms interpolate.\",\n      \"tests\": [\n        \"executeAction embedded vector: body-raw action options interpolate during their second generic transform\"\n      ]\n    },\n    \"6.5.8\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Dangerous resolved keys have inert descriptors and preserve prototypes/order in both modes.\",\n      \"tests\": [\n        \"safe output: resolved __proto__ is inert in body and generic modes\",\n        \"safe output: resolved constructor is inert in body and generic modes\",\n        \"safe output: resolved prototype is inert in body and generic modes\",\n        \"safe output ordering: dangerous resolved keys retain mode ordering and later safe definitions win\",\n        \"safe output error ordering: dangerous resolved keys preserve body and generic getter order\",\n        \"safe output getter/error variant: resolved constructor preserves exact body and generic order\",\n        \"safe output getter/error variant: resolved prototype preserves exact body and generic order\"\n      ]\n    },\n    \"6.5.9\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"$set, $cache.set, and $global.set retain ordinary prototypes and inert descriptors.\",\n      \"tests\": [\n        \"safe state sink: $set defines inert own keys\",\n        \"safe state sink: $cache.set defines inert own keys\",\n        \"safe state sink: $global.set defines inert own keys\"\n      ]\n    },\n    \"6.5.10\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Session inner/outer dangerous keys, prototypes, null prototype input, and inherited lookup rejection pass.\",\n      \"tests\": [\n        \"session inner storage: null-prototype options become a fresh safe ordinary session\",\n        \"session outer storage: normalized domain __proto__ is an inert reachable own key\",\n        \"session outer storage: normalized domain constructor is an inert reachable own key\",\n        \"session outer storage: normalized domain prototype is an inert reachable own key\",\n        \"session lookup control: own api.example.com decorates a network request\",\n        \"session lookup: inherited api.example.com does not decorate a network request\"\n      ]\n    },\n    \"6.5.11\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Enumeration uses own enumerable string keys only.\",\n      \"tests\": [\n        \"safe output: null-prototype sources work and inherited/non-enumerable/symbol keys are excluded\"\n      ]\n    },\n    \"6.5.12\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Direct component/background/action type matrices have exact safe outcomes.\",\n      \"tests\": [\n        \"component dispatch: only own exact html enters the HTML renderer\",\n        \"component dispatch: own unknown type constructor is visible and never calls a prototype member\",\n        \"background full matrix: canonical path covers every type source inherited-field and CSS vector\",\n        \"background full matrix: legacy path covers every type source inherited-field and CSS vector\",\n        \"component registry: an inherited callable renderer is ignored with exact unknown-component outcome\",\n        \"action type registry: an inherited callable handler returns undefined and is never invoked\",\n        \"action type registry: own unknown type prototype returns undefined without continuation\",\n        \"action type registry: inherited, missing, and non-string types invoke no inherited handler\"\n      ]\n    },\n    \"6.5.13\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Transform-to-render inherited and mixed own fields obey own-field selection.\",\n      \"tests\": [\n        \"transform-to-render: inherited text, URL, and CSS are excluded while mixed own fields win\"\n      ]\n    },\n    \"6.5.14\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Existing unresolved-expression behavior is unchanged with exact output asserted.\",\n      \"tests\": [\n        \"unresolved expressions: body and generic modes return the exact unresolved strings\"\n      ]\n    },\n    \"6.6.1\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Every string-only source row including dual, whitespace, inherited, mixed, and none passes.\",\n      \"tests\": [\n        \"component source matrix: text wins over URL\",\n        \"component source matrix: whitespace text is valid\",\n        \"component source matrix: empty text falls back to URL\",\n        \"component source matrix: non-string text falls back to URL\",\n        \"component source matrix: missing values select none\",\n        \"component source matrix: empty values select none\",\n        \"component source matrix: non-string values select none\",\n        \"component source matrix: inherited-only values select none\",\n        \"component source matrix: inherited text plus own URL selects URL\",\n        \"component source matrix: inherited URL plus own text selects inline\"\n      ]\n    },\n    \"6.6.2\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Non-string text and URL values are ignored rather than coerced.\",\n      \"tests\": [\n        \"component source matrix: non-string text falls back to URL\",\n        \"component source matrix: non-string values select none\",\n        \"component non-string source and CSS sentinels are never coerced\",\n        \"background coercion sentinels: canonical path never coerces text URL or CSS\",\n        \"background coercion sentinels: legacy path never coerces text URL or CSS\"\n      ]\n    },\n    \"6.6.3\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Every exact CSS vector and arbitrary whitespace pass.\",\n      \"tests\": [\n        \"component CSS matrix: absent\",\n        \"component CSS matrix: empty\",\n        \"component CSS matrix: null\",\n        \"component CSS matrix: undefined\",\n        \"component CSS matrix: non-string\",\n        \"component CSS matrix: inherited\",\n        \"component CSS matrix: one space\",\n        \"component CSS matrix: tab and newline\",\n        \"component CSS matrix: mixed-case closer\",\n        \"component CSS matrix: repeated mixed-case closers\",\n        \"component CSS matrix: arbitrary non-empty whitespace is retained code-unit-for-code-unit\"\n      ]\n    },\n    \"6.6.4\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Mixed-case repeated style replacements contain runtime U+005C.\",\n      \"tests\": [\n        \"component CSS matrix: mixed-case closer\",\n        \"component CSS matrix: repeated mixed-case closers\",\n        \"background full matrix: canonical path covers every type source inherited-field and CSS vector\",\n        \"background full matrix: legacy path covers every type source inherited-field and CSS vector\"\n      ]\n    },\n    \"6.6.5\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Invalid HTML is assigned without rewriting.\",\n      \"tests\": [\n        \"component invalid HTML: srcdoc receives the authored string unchanged\"\n      ]\n    },\n    \"6.6.6\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Exact string-domain attributes are asserted without coercion claims.\",\n      \"tests\": [\n        \"component source matrix: text wins over URL\",\n        \"component source matrix: empty text falls back to URL\",\n        \"component non-string source and CSS sentinels are never coerced\",\n        \"background coercion sentinels: canonical path never coerces text URL or CSS\",\n        \"background coercion sentinels: legacy path never coerces text URL or CSS\",\n        \"evidence boundary: jsdom assertions inspect emitted policy and data, not browser enforcement\"\n      ]\n    },\n    \"6.7.1\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Component inline and URL traces exactly match.\",\n      \"tests\": [\n        \"component trace: inline has exact finite creation order\",\n        \"component trace: URL has exact finite creation order\"\n      ]\n    },\n    \"6.7.2\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Background inline and URL traces exactly match through public renderDocument return.\",\n      \"tests\": [\n        \"background trace: inline has exact finite creation order\",\n        \"background trace: URL has exact finite creation order\"\n      ],\n      \"observationBoundary\": \"RETURN is marked when the public JasonetteRenderer.renderDocument call returns; no implementation method is inspected or wrapped.\"\n    },\n    \"6.7.3\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Sandbox is first, before source/insertion, and exactly allow-scripts.\",\n      \"tests\": [\n        \"component trace: inline has exact finite creation order\",\n        \"background trace: inline has exact finite creation order\",\n        \"iframe observer self-test: Attr source and sandbox plus replaceChildren cannot evade tracing\",\n        \"iframe observer self-test: pre-existing DocumentFragment insertion is traced before later sandbox\",\n        \"iframe observer self-test: pre-existing detached Element insertion is traced before later sandbox\"\n      ]\n    },\n    \"6.7.4\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"One sandbox event and initial/$render old/new endpoint checks pass without continuous-monitor claim.\",\n      \"tests\": [\n        \"integration smoke: exact initial and actual $render paths preserve raw HTML and remain live\",\n        \"renderer liveness: actual $render replaces background and retains old/new sandbox endpoints\",\n        \"iframe observer self-test: replacement removal property source and insertAdjacentElement are traced\",\n        \"evidence boundary: jsdom assertions inspect emitted policy and data, not browser enforcement\"\n      ]\n    },\n    \"6.7.5\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Inline and URL source attributes are exclusive.\",\n      \"tests\": [\n        \"component source matrix: text wins over URL\",\n        \"component source matrix: empty text falls back to URL\",\n        \"background locations: static canonical inline CSS\",\n        \"background locations: static legacy URL\"\n      ]\n    },\n    \"6.7.6\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"No-source and inherited-only cases have no iframe trace; mixed own fields win.\",\n      \"tests\": [\n        \"component no-source trace: invalid own and inherited-only sources create no iframe record\",\n        \"background no-source: canonical static and templated locations create no iframe\",\n        \"background no-source: legacy static and templated locations create no iframe\",\n        \"background coercion sentinels: canonical path never coerces text URL or CSS\",\n        \"background coercion sentinels: legacy path never coerces text URL or CSS\",\n        \"transform-to-render: inherited text, URL, and CSS are excluded while mixed own fields win\"\n      ]\n    },\n    \"6.7.7\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Component detached wrapper topology, class/data type, width, and border are exact.\",\n      \"tests\": [\n        \"component topology: detached wrapper owns an exact class/data/style iframe\"\n      ]\n    },\n    \"6.7.8\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Background is a direct root child with exact class/ARIA before foreground.\",\n      \"tests\": [\n        \"background topology: iframe is a direct root child before foreground content\"\n      ]\n    },\n    \"6.8.1\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Initial DOM selector/value expectations all pass.\",\n      \"tests\": [\n        \"integration smoke: exact initial and actual $render paths preserve raw HTML and remain live\"\n      ]\n    },\n    \"6.8.2\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Actual $render replaces node and changes height and label.\",\n      \"tests\": [\n        \"integration smoke: exact initial and actual $render paths preserve raw HTML and remain live\"\n      ]\n    },\n    \"6.8.3\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Replacement iframe stays raw, excludes SECOND, and old/new sandboxes are exact.\",\n      \"tests\": [\n        \"integration smoke: exact initial and actual $render paths preserve raw HTML and remain live\"\n      ]\n    },\n    \"6.8.4\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Rendered Jasonpedia iframe srcdoc contains required CSS and Nexus content.\",\n      \"tests\": [\n        \"Jasonpedia fixture: public renderer output contains required CSS and Nexus content\"\n      ]\n    },\n    \"6.8.5\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Background own-path precedence and independent full canonical/legacy matrices pass.\",\n      \"tests\": [\n        \"background precedence: canonical false blocks fallback\",\n        \"background precedence: canonical empty object blocks fallback\",\n        \"background precedence: canonical HTML no source blocks fallback\",\n        \"background precedence: canonical null uses legacy\",\n        \"background precedence: canonical undefined uses legacy\",\n        \"background precedence: missing canonical value uses an own legacy fallback\",\n        \"background precedence: inherited canonical value uses an own legacy fallback\",\n        \"background precedence: inherited canonical path without fallback has no transient iframe\",\n        \"background precedence: inherited style path segment has no transient iframe\",\n        \"background precedence: inherited legacy background segment has no transient iframe\",\n        \"background full matrix: canonical path covers every type source inherited-field and CSS vector\",\n        \"background full matrix: legacy path covers every type source inherited-field and CSS vector\",\n        \"background locations: static canonical inline CSS\",\n        \"background locations: static legacy URL\",\n        \"background locations: templated canonical raw text and transformed CSS\",\n        \"background locations: templated legacy dynamic URL\"\n      ]\n    },\n    \"6.8.6\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Component/background HTML stays authored while CSS and ordinary siblings interpolate.\",\n      \"tests\": [\n        \"renderer content: component/background authored HTML stays raw while CSS and ordinary siblings interpolate\"\n      ]\n    },\n    \"6.9.1\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"jsdom evidence covers outputs, identity, descriptors, prototypes, strings, attributes, sandbox, and traces.\",\n      \"tests\": [\n        \"transform identity: protected strings and objects retain strict identity\",\n        \"safe output: resolved __proto__ is inert in body and generic modes\",\n        \"component trace: inline has exact finite creation order\",\n        \"background full matrix: canonical path covers every type source inherited-field and CSS vector\"\n      ]\n    },\n    \"6.9.2\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"No report claims browser enforcement properties from jsdom.\",\n      \"tests\": [\n        \"evidence boundary: jsdom assertions inspect emitted policy and data, not browser enforcement\"\n      ]\n    },\n    \"6.9.3\": {\n      \"type\": \"external\",\n      \"status\": \"pending\",\n      \"checkbox\": \"The eight repository verification commands satisfy their required outcomes.\",\n      \"externalEvidenceIds\": [\n        \"repository-verification-commands\"\n      ]\n    },\n    \"6.9.4\": {\n      \"type\": \"external\",\n      \"status\": \"pending\",\n      \"checkbox\": \"Full-Web evidence satisfies a passing truth-table row.\",\n      \"externalEvidenceIds\": [\n        \"exact-sha-web-ci-acceptance\"\n      ]\n    },\n    \"6.10.1\": {\n      \"type\": \"executable\",\n      \"checkbox\": \"Integration smoke covers actual initial/$render liveness, raw text, generic default, and all observed traces.\",\n      \"tests\": [\n        \"integration smoke: exact initial and actual $render paths preserve raw HTML and remain live\"\n      ]\n    }\n  },\n  \"externalEvidence\": [\n    {\n      \"id\": \"repository-verification-commands\",\n      \"type\": \"external\",\n      \"status\": \"pending\",\n      \"dod\": \"6.9.3\",\n      \"owner\": \"orchestrator\",\n      \"evidenceRequired\": \"Recorded outcomes for all eight exact section 3.6 repository commands; no Vitest runtime assertion is claimed.\",\n      \"commands\": [\n        \"npm run test --workspace=@jasonette/template-engine -- transformer.test.ts\",\n        \"npm run test --workspace=@jasonette/web -- components.test.ts integration.test.ts actions-parity.test.ts renderer.test.ts\",\n        \"npm run typecheck --workspace=@jasonette/template-engine\",\n        \"npm run typecheck --workspace=@jasonette/web\",\n        \"npm run build --workspace=@jasonette/template-engine\",\n        \"npm run build --workspace=@jasonette/web\",\n        \"npm run test --workspace=@jasonette/template-engine\",\n        \"npm run test --workspace=@jasonette/web\"\n      ]\n    },\n    {\n      \"id\": \"exact-sha-web-ci-acceptance\",\n      \"type\": \"external\",\n      \"status\": \"pending\",\n      \"dod\": \"6.9.4\",\n      \"owner\": \"orchestrator\",\n      \"evidenceRequired\": \"A passing section 3.6 acceptance-truth-table row, including exact implementation-SHA Web CI and any required isolated cli.test.ts result.\",\n      \"acceptanceRows\": [\n        \"local full Web pass + exact implementation-SHA Web CI pass\",\n        \"only local CLI timeout + all other Web tests pass + isolated cli.test.ts pass + exact implementation-SHA Web CI pass\"\n      ]\n    }\n  ]\n}\n\n\n===== tests/evidence-boundary.test.ts =====\n// @vitest-environment jsdom\n\nimport { afterEach, describe, expect, it } from \"vitest\";\nimport { JasonetteRenderer, renderComponent } from \"@jasonette/web\";\nimport jasonpediaDocument from \"../support/fixtures/jasonpedia-html-index.json\";\n\nafterEach(() => {\n  document.body.replaceChildren();\n});\n\ndescribe(\"black-box evidence boundary\", () => {\n  it(\"evidence boundary: jsdom assertions inspect emitted policy and data, not browser enforcement\", () => {\n    const wrapper = (renderComponent as unknown as (component: unknown) => HTMLElement)({\n      type: \"html\",\n      text: \"<script>window.__evidence_boundary_sentinel = true</script>\",\n    });\n    const iframe = wrapper.querySelector(\"iframe\") as HTMLIFrameElement;\n\n    expect(iframe.getAttribute(\"sandbox\")).toBe(\"allow-scripts\");\n    expect(iframe.getAttribute(\"sandbox\")?.split(/\\s+/)).toEqual([\"allow-scripts\"]);\n    expect(iframe.getAttribute(\"srcdoc\")).toBe(\n      \"<script>window.__evidence_boundary_sentinel = true</script>\",\n    );\n    expect(iframe.hasAttribute(\"src\")).toBe(false);\n\n    // Deliberately no assertion about child execution, origin, host access,\n    // navigation, storage, cookies, or network behavior. jsdom is only the\n    // emitted attribute/data oracle for this NLSpec.\n  });\n\n  it(\"Jasonpedia fixture: public renderer output contains required CSS and Nexus content\", () => {\n    const root = document.createElement(\"main\");\n    document.body.appendChild(root);\n    const renderer = new (JasonetteRenderer as any)(root);\n\n    renderer.renderDocument(jasonpediaDocument);\n\n    const iframe = root.querySelector(\".jasonette-html iframe\") as HTMLIFrameElement;\n    expect(iframe).toBeInstanceOf(HTMLIFrameElement);\n    const srcdoc = iframe.getAttribute(\"srcdoc\");\n    expect(srcdoc).toContain(\"img{width: 100%;}\");\n    expect(srcdoc).toContain(\"Nexus devices\");\n  });\n});\n\n\n===== tests/iframe-observer.ts =====\nexport type TraceRecord = {\n  kind: \"component\" | \"background\";\n  iframe: HTMLIFrameElement;\n  events: string[];\n};\n\nexport type IframeObserver = {\n  records: TraceRecord[];\n  run<T>(kind: TraceRecord[\"kind\"], operation: () => T): T;\n  runAsync<T>(kind: TraceRecord[\"kind\"], operation: () => Promise<T>): Promise<T>;\n  restore(): void;\n};\n\n/**\n * Observes the DOM mutation layer rather than selected convenience methods.\n * Attribute records cover property reflection, Attr/NamedNodeMap operations,\n * DOMTokenList operations, and set/remove/toggleAttribute. Child-list records\n * cover every insertion API used on an observed parent.\n */\nexport function installIframeObserver(): IframeObserver {\n  const records: TraceRecord[] = [];\n  const byIframe = new WeakMap<HTMLIFrameElement, TraceRecord>();\n  const observed = new WeakSet<Node>();\n  let activeKind: TraceRecord[\"kind\"] | undefined;\n\n  const nativeCreateElement = Document.prototype.createElement;\n  const nativeCreateElementNS = Document.prototype.createElementNS;\n  const nativeCreateDocumentFragment = Document.prototype.createDocumentFragment;\n  const nativeCloneNode = Node.prototype.cloneNode;\n  const parentMutationRestorers: Array<() => void> = [];\n\n  const ensureRecord = (iframe: HTMLIFrameElement): TraceRecord | undefined => {\n    const existing = byIframe.get(iframe);\n    if (existing) return existing;\n    if (!activeKind) return undefined;\n    const record: TraceRecord = {\n      kind: activeKind,\n      iframe,\n      events: [`CREATE(${activeKind})`],\n    };\n    records.push(record);\n    byIframe.set(iframe, record);\n    return record;\n  };\n\n  const valueAfterMutation = (batch: MutationRecord[], index: number): string | null => {\n    const mutation = batch[index];\n    for (let next = index + 1; next < batch.length; next += 1) {\n      const candidate = batch[next];\n      if (\n        candidate.type === \"attributes\"\n        && candidate.target === mutation.target\n        && candidate.attributeName === mutation.attributeName\n      ) {\n        return candidate.oldValue;\n      }\n    }\n    return (mutation.target as Element).getAttribute(mutation.attributeName as string);\n  };\n\n  const process = (batch: MutationRecord[]): void => {\n    for (let index = 0; index < batch.length; index += 1) {\n      const mutation = batch[index];\n      if (mutation.type === \"attributes\" && mutation.target instanceof HTMLIFrameElement) {\n        const record = byIframe.get(mutation.target);\n        if (!record) continue;\n        const name = mutation.attributeName?.toLowerCase();\n        if (name === \"sandbox\") {\n          const value = valueAfterMutation(batch, index);\n          record.events.push(value === null ? \"SANDBOX(null)\" : `SANDBOX(${JSON.stringify(value)})`);\n        } else if (name === \"srcdoc\" || name === \"src\") {\n          record.events.push(`SOURCE(${name})`);\n        }\n      }\n\n      if (mutation.type === \"childList\") {\n        for (const added of mutation.addedNodes) {\n          if (added instanceof HTMLIFrameElement) ensureRecord(added)?.events.push(\"APPEND\");\n          // Keep nodes introduced by parser/range APIs in the observation set for\n          // any later iframe mutation. Only a directly-added iframe is APPEND:\n          // moving an ancestor does not change that iframe's parent.\n          observeTree(added);\n        }\n      }\n    }\n  };\n\n  const mutationObserver = new MutationObserver((batch) => process(batch));\n\n  const observeNode = (node: Node): void => {\n    if (observed.has(node)) return;\n    observed.add(node);\n    mutationObserver.observe(node, {\n      attributes: node instanceof Element,\n      attributeOldValue: node instanceof Element,\n      childList: true,\n    });\n  };\n\n  const observeTree = (node: Node): void => {\n    observeNode(node);\n    for (const child of node.childNodes) observeTree(child);\n  };\n\n  observeTree(document);\n\n  // A detached Element, ShadowRoot, DocumentFragment, or Document can predate\n  // this observer, and a fragment can come from a factory other than\n  // document.createDocumentFragment(). Creation hooks alone are therefore not\n  // a complete boundary. Every DOM surface that can change a parent is wrapped\n  // below so the receiver, moved nodes, and their old parents are observed\n  // before the operation. MutationObserver remains the event oracle.\n  const observeMutationBoundary = (receiver: unknown, args: unknown[]): void => {\n    if (receiver instanceof Node) {\n      observeTree(receiver);\n      if (receiver.parentNode) observeTree(receiver.parentNode);\n    } else if (typeof Range !== \"undefined\" && receiver instanceof Range) {\n      observeTree(receiver.startContainer);\n      observeTree(receiver.commonAncestorContainer);\n    }\n    for (const argument of args) {\n      if (argument instanceof Node) {\n        observeTree(argument);\n        if (argument.parentNode) observeTree(argument.parentNode);\n      }\n    }\n  };\n\n  const instrumentParentMutation = (owner: object | undefined, name: string): void => {\n    if (!owner) return;\n    const descriptor = Object.getOwnPropertyDescriptor(owner, name);\n    if (!descriptor || typeof descriptor.value !== \"function\") return;\n    const original = descriptor.value as (...args: unknown[]) => unknown;\n    Object.defineProperty(owner, name, {\n      ...descriptor,\n      value(this: unknown, ...args: unknown[]) {\n        observeMutationBoundary(this, args);\n        return original.apply(this, args);\n      },\n    });\n    parentMutationRestorers.push(() => Object.defineProperty(owner, name, descriptor));\n  };\n\n  const instrumentParentSetter = (owner: object | undefined, name: string): void => {\n    if (!owner) return;\n    const descriptor = Object.getOwnPropertyDescriptor(owner, name);\n    if (!descriptor?.set) return;\n    const originalSet = descriptor.set;\n    Object.defineProperty(owner, name, {\n      ...descriptor,\n      set(this: unknown, value: unknown) {\n        observeMutationBoundary(this, [value]);\n        originalSet.call(this, value);\n      },\n    });\n    parentMutationRestorers.push(() => Object.defineProperty(owner, name, descriptor));\n  };\n\n  // Node, ParentNode, ChildNode, Element insertion, and Range are the complete\n  // public parent-changing method boundary for existing nodes in this DOM realm.\n  for (const [owner, names] of [\n    [Node.prototype, [\"appendChild\", \"insertBefore\", \"replaceChild\", \"removeChild\"]],\n    [Element.prototype, [\n      \"append\", \"prepend\", \"replaceChildren\", \"moveBefore\", \"before\", \"after\", \"replaceWith\", \"remove\",\n      \"insertAdjacentElement\", \"insertAdjacentHTML\", \"setHTMLUnsafe\",\n    ]],\n    [Document.prototype, [\n      \"append\", \"prepend\", \"replaceChildren\", \"moveBefore\", \"write\", \"writeln\", \"adoptNode\",\n    ]],\n    [DocumentFragment.prototype, [\"append\", \"prepend\", \"replaceChildren\", \"moveBefore\"]],\n    [globalThis.ShadowRoot?.prototype, [\"setHTMLUnsafe\"]],\n    [globalThis.CharacterData?.prototype, [\"before\", \"after\", \"replaceWith\", \"remove\"]],\n    [globalThis.DocumentType?.prototype, [\"before\", \"after\", \"replaceWith\", \"remove\"]],\n    [globalThis.Range?.prototype, [\n      \"insertNode\", \"surroundContents\", \"deleteContents\", \"extractContents\",\n    ]],\n  ] as Array<[object | undefined, string[]]>) {\n    for (const name of names) instrumentParentMutation(owner, name);\n  }\n  for (const [owner, names] of [\n    [Node.prototype, [\"textContent\"]],\n    [Element.prototype, [\"innerHTML\", \"outerHTML\"]],\n    [globalThis.ShadowRoot?.prototype, [\"innerHTML\"]],\n  ] as Array<[object | undefined, string[]]>) {\n    for (const name of names) instrumentParentSetter(owner, name);\n  }\n\n  Document.prototype.createElement = function observedCreateElement(\n    name: string,\n    options?: ElementCreationOptions,\n  ): HTMLElement {\n    const element = nativeCreateElement.call(this, name, options) as HTMLElement;\n    observeTree(element);\n    if (element instanceof HTMLIFrameElement) ensureRecord(element);\n    return element;\n  } as typeof Document.prototype.createElement;\n\n  Document.prototype.createElementNS = function observedCreateElementNS(\n    namespaceURI: string | null,\n    qualifiedName: string,\n    options?: string | ElementCreationOptions,\n  ): Element {\n    const element = nativeCreateElementNS.call(this, namespaceURI, qualifiedName, options as ElementCreationOptions);\n    observeTree(element);\n    if (element instanceof HTMLIFrameElement) ensureRecord(element);\n    return element;\n  } as typeof Document.prototype.createElementNS;\n\n  Document.prototype.createDocumentFragment = function observedCreateDocumentFragment(): DocumentFragment {\n    const fragment = nativeCreateDocumentFragment.call(this);\n    observeTree(fragment);\n    return fragment;\n  };\n\n  Node.prototype.cloneNode = function observedCloneNode(deep?: boolean): Node {\n    const clone = nativeCloneNode.call(this, deep);\n    observeTree(clone);\n    if (clone instanceof HTMLIFrameElement) ensureRecord(clone);\n    return clone;\n  };\n\n  const flush = (): void => process(mutationObserver.takeRecords());\n  const finish = (start: number): void => {\n    flush();\n    for (const record of records.slice(start)) record.events.push(\"RETURN\");\n  };\n\n  return {\n    records,\n    run<T>(kind, operation): T {\n      const start = records.length;\n      const previousKind = activeKind;\n      activeKind = kind;\n      try {\n        const result = operation();\n        finish(start);\n        return result;\n      } finally {\n        activeKind = previousKind;\n      }\n    },\n    async runAsync<T>(kind, operation): Promise<T> {\n      const start = records.length;\n      const previousKind = activeKind;\n      activeKind = kind;\n      try {\n        const result = await operation();\n        finish(start);\n        return result;\n      } finally {\n        activeKind = previousKind;\n      }\n    },\n    restore(): void {\n      flush();\n      mutationObserver.disconnect();\n      Document.prototype.createElement = nativeCreateElement;\n      Document.prototype.createElementNS = nativeCreateElementNS;\n      Document.prototype.createDocumentFragment = nativeCreateDocumentFragment;\n      Node.prototype.cloneNode = nativeCloneNode;\n      for (const restore of parentMutationRestorers.reverse()) restore();\n    },\n  };\n}\n\n\n===== tests/template-engine-safety.test.ts =====\nimport { describe, expect, it } from \"vitest\";\nimport {\n  renderSync,\n  transform,\n  type RenderOptions,\n} from \"@jasonette/template-engine\";\n\nconst bodyMode: RenderOptions = { preserveHtmlText: true };\n\nfunction descriptorIsSafe(object: object, key: string, value?: unknown): void {\n  const descriptor = Object.getOwnPropertyDescriptor(object, key);\n  expect(descriptor, `missing own descriptor for ${key}`).toBeDefined();\n  expect(descriptor).toMatchObject({\n    enumerable: true,\n    writable: true,\n    configurable: true,\n  });\n  expect(descriptor).not.toHaveProperty(\"get\");\n  expect(descriptor).not.toHaveProperty(\"set\");\n  if (arguments.length >= 3) expect(descriptor?.value).toBe(value);\n}\n\nfunction defineOwn(target: object, key: string, value: unknown): void {\n  Object.defineProperty(target, key, {\n    value,\n    enumerable: true,\n    writable: true,\n    configurable: true,\n  });\n}\n\nfunction collectHtmlShapes(value: unknown, found: Array<Record<string, unknown>> = []): Array<Record<string, unknown>> {\n  if (Array.isArray(value)) {\n    for (const entry of value) collectHtmlShapes(entry, found);\n  } else if (value !== null && typeof value === \"object\") {\n    const record = value as Record<string, unknown>;\n    if (Object.hasOwn(record, \"type\") && record.type === \"html\") found.push(record);\n    for (const key of Object.keys(record)) collectHtmlShapes(record[key], found);\n  }\n  return found;\n}\n\nfunction expectSafeOutputTree(value: unknown, visited = new WeakSet<object>()): void {\n  if (value === null || typeof value !== \"object\" || visited.has(value)) return;\n  visited.add(value);\n  for (const key of Object.keys(value)) {\n    const record = value as Record<string, unknown>;\n    descriptorIsSafe(value, key, record[key]);\n    expectSafeOutputTree(record[key], visited);\n  }\n}\n\ndescribe(\"black-box body-mode transformation\", () => {\n  const exactVectors: Array<{\n    testId: string;\n    input: Record<string, unknown>;\n    context: Record<string, unknown>;\n    options?: RenderOptions;\n    expected: Record<string, unknown>;\n  }> = [\n    {\n      testId: \"transform matrix: literal HTML text with transformed style\",\n      input: { type: \"html\", text: \"<p>{{secret}}</p>\", style: { height: \"{{height}}\" } },\n      context: { secret: \"LEAK\", height: 40 },\n      options: bodyMode,\n      expected: { type: \"html\", text: \"<p>{{secret}}</p>\", style: { height: 40 } },\n    },\n    {\n      testId: \"transform matrix: dynamic type and text keys\",\n      input: { type: \"{{kind}}\", \"{{slot}}\": \"<p>{{secret}}</p>\" },\n      context: { kind: \"html\", slot: \"text\", secret: \"LEAK\" },\n      options: bodyMode,\n      expected: { type: \"html\", text: \"<p>{{secret}}</p>\" },\n    },\n    {\n      testId: \"transform matrix: dynamic resolved type key\",\n      input: { \"{{typeKey}}\": \"{{kind}}\", text: \"{{secret}}\" },\n      context: { typeKey: \"type\", kind: \"html\", secret: \"LEAK\" },\n      options: bodyMode,\n      expected: { type: \"html\", text: \"{{secret}}\" },\n    },\n    {\n      testId: \"transform matrix: later duplicate resolved text\",\n      input: { type: \"html\", \"{{slot}}\": \"first\", text: \"second\" },\n      context: { slot: \"text\" },\n      options: bodyMode,\n      expected: { type: \"html\", text: \"second\" },\n    },\n    {\n      testId: \"transform matrix: non-HTML label\",\n      input: { type: \"label\", text: \"{{secret}}\" },\n      context: { secret: \"OK\" },\n      options: bodyMode,\n      expected: { type: \"label\", text: \"OK\" },\n    },\n    {\n      testId: \"transform matrix: omitted body option\",\n      input: { type: \"html\", text: \"{{secret}}\" },\n      context: { secret: \"GENERIC\" },\n      expected: { type: \"html\", text: \"GENERIC\" },\n    },\n  ];\n\n  it.each(exactVectors)(\"$testId\", ({ input, context, options, expected }) => {\n    const output = transform(input, context, options);\n    expect(output).toEqual(expected);\n    expect(Object.getPrototypeOf(output)).toBe(Object.prototype);\n    expectSafeOutputTree(output);\n  });\n\n  it(\"transform classification: later label type deactivates raw-text protection\", () => {\n    const output = transform(\n      { type: \"html\", \"{{typeAgain}}\": \"label\", text: \"{{secret}}\" },\n      { typeAgain: \"type\", secret: \"VALUE\" },\n      bodyMode,\n    );\n    expect(output).toEqual({ type: \"label\", text: \"VALUE\" });\n  });\n\n  it(\"transform classification: later HTML type activates raw-text protection\", () => {\n    const output = transform(\n      { type: \"label\", \"{{typeAgain}}\": \"html\", text: \"{{secret}}\" },\n      { typeAgain: \"type\", secret: \"VALUE\" },\n      bodyMode,\n    );\n    expect(output).toEqual({ type: \"html\", text: \"{{secret}}\" });\n  });\n\n  it(\"transform classification: duplicate protected text entries stay raw and the later value wins\", () => {\n    const output = transform(\n      { type: \"html\", \"{{firstText}}\": \"{{first}}\", text: \"{{second}}\" },\n      { firstText: \"text\", first: \"FIRST\", second: \"SECOND\" },\n      bodyMode,\n    );\n    expect(output).toEqual({ type: \"html\", text: \"{{second}}\" });\n  });\n\n  it(\"transform classification sentinel: overwritten raw text never evaluates either expression\", () => {\n    const reads: string[] = [];\n    const context: Record<string, unknown> = { firstText: \"text\" };\n    for (const name of [\"overwritten\", \"winning\"] as const) {\n      Object.defineProperty(context, name, {\n        enumerable: true,\n        get() {\n          reads.push(name);\n          throw new Error(`${name} raw expression must not run`);\n        },\n      });\n    }\n    const output = transform(\n      { type: \"html\", \"{{firstText}}\": \"{{overwritten}}\", text: \"{{winning}}\" },\n      context,\n      bodyMode,\n    );\n    expect(output).toEqual({ type: \"html\", text: \"{{winning}}\" });\n    expect(reads).toEqual([]);\n  });\n\n  it(\"transform identity: protected strings and objects retain strict identity\", () => {\n    const rawString = \"<script>{{secret}}</script>\";\n    const stringResult = transform({ type: \"html\", text: rawString }, { secret: \"LEAK\" }, bodyMode);\n    expect(stringResult.text).toBe(rawString);\n\n    const rawObject = { nested: \"{{secret}}\" };\n    const objectResult = transform({ type: \"html\", text: rawObject }, { secret: \"LEAK\" }, bodyMode);\n    expect(objectResult.text).toBe(rawObject);\n    expect(rawObject.nested).toBe(\"{{secret}}\");\n  });\n\n  it(\"transform classification: missing, unresolved, and non-string types do not protect\", () => {\n    expect(transform({ text: \"{{secret}}\" }, { secret: \"VALUE\" }, bodyMode)).toEqual({ text: \"VALUE\" });\n    expect(transform({ type: \"{{missing}}\", text: \"{{secret}}\" }, { secret: \"VALUE\" }, bodyMode).text).toBe(\"VALUE\");\n    expect(transform({ type: \"{{kind}}\", text: \"{{secret}}\" }, { kind: 7, secret: \"VALUE\" }, bodyMode).text).toBe(\"VALUE\");\n    expect(transform({ type: \"{{kind}}\", text: \"{{secret}}\" }, { kind: null, secret: \"VALUE\" }, bodyMode).text).toBe(\"VALUE\");\n  });\n\n  it(\"transform ordering: body mode resolves all flat keys, then types, then ordinary values exactly once\", () => {\n    const log: string[] = [];\n    const context: Record<string, unknown> = {};\n    for (const [name, value] of [\n      [\"keyType\", \"type\"],\n      [\"keyValue\", \"value\"],\n      [\"kind\", \"label\"],\n      [\"ordinary\", \"done\"],\n    ] as const) {\n      Object.defineProperty(context, name, {\n        enumerable: true,\n        get() {\n          log.push(name);\n          return value;\n        },\n      });\n    }\n\n    const result = transform(\n      { \"{{keyType}}\": \"{{kind}}\", \"{{keyValue}}\": \"{{ordinary}}\" },\n      context,\n      bodyMode,\n    );\n    expect(result).toEqual({ type: \"label\", value: \"done\" });\n    expect(log).toEqual([\"keyType\", \"keyValue\", \"kind\", \"ordinary\"]);\n  });\n\n  it(\"transform ordering: nested frames each apply all-keys-first at visitation time\", () => {\n    const log: string[] = [];\n    const context: Record<string, unknown> = {};\n    const values: Record<string, unknown> = {\n      parentTypeKey: \"type\",\n      parentChildKey: \"child\",\n      parentTailKey: \"tail\",\n      parentKind: \"label\",\n      childTypeKey: \"type\",\n      childValueKey: \"value\",\n      childKind: \"label\",\n      childValue: \"inside\",\n      parentTail: \"outside\",\n    };\n    for (const [name, value] of Object.entries(values)) {\n      Object.defineProperty(context, name, {\n        enumerable: true,\n        get() {\n          log.push(name);\n          return value;\n        },\n      });\n    }\n\n    const result = transform(\n      {\n        \"{{parentTypeKey}}\": \"{{parentKind}}\",\n        \"{{parentChildKey}}\": {\n          \"{{childTypeKey}}\": \"{{childKind}}\",\n          \"{{childValueKey}}\": \"{{childValue}}\",\n        },\n        \"{{parentTailKey}}\": \"{{parentTail}}\",\n      },\n      context,\n      bodyMode,\n    );\n\n    expect(result).toEqual({ type: \"label\", child: { type: \"label\", value: \"inside\" }, tail: \"outside\" });\n    expect(log).toEqual([\n      \"parentTypeKey\",\n      \"parentChildKey\",\n      \"parentTailKey\",\n      \"parentKind\",\n      \"childTypeKey\",\n      \"childValueKey\",\n      \"childKind\",\n      \"childValue\",\n      \"parentTail\",\n    ]);\n  });\n\n  it(\"transform ordering: omitted and false modes preserve per-entry key-then-value evaluation\", () => {\n    for (const options of [undefined, { preserveHtmlText: false } as RenderOptions]) {\n      const log: string[] = [];\n      const context: Record<string, unknown> = {};\n      for (const [name, value] of [\n        [\"keyType\", \"type\"],\n        [\"kind\", \"label\"],\n        [\"keyValue\", \"value\"],\n        [\"ordinary\", \"done\"],\n      ] as const) {\n        Object.defineProperty(context, name, {\n          enumerable: true,\n          get() {\n            log.push(name);\n            return value;\n          },\n        });\n      }\n      expect(transform(\n        { \"{{keyType}}\": \"{{kind}}\", \"{{keyValue}}\": \"{{ordinary}}\" },\n        context,\n        options,\n      )).toEqual({ type: \"label\", value: \"done\" });\n      expect(log).toEqual([\"keyType\", \"kind\", \"keyValue\", \"ordinary\"]);\n    }\n  });\n\n  it(\"transform ordering: numeric keys use Object.keys order 1, 2, alpha\", () => {\n    const log: string[] = [];\n    const context: Record<string, unknown> = {};\n    for (const name of [\"one\", \"two\", \"alpha\"] as const) {\n      Object.defineProperty(context, name, {\n        enumerable: true,\n        get() {\n          log.push(name);\n          return name;\n        },\n      });\n    }\n    const input: Record<string, unknown> = {};\n    input[\"2\"] = \"{{two}}\";\n    input[\"1\"] = \"{{one}}\";\n    input.alpha = \"{{alpha}}\";\n\n    const result = transform(input, context, bodyMode);\n    expect(Object.keys(result)).toEqual([\"1\", \"2\", \"alpha\"]);\n    expect(log).toEqual([\"one\", \"two\", \"alpha\"]);\n  });\n\n  it(\"renderSync recursion: body roots and every named nested location preserve HTML shapes\", () => {\n    const shape = () => ({ type: \"html\", text: \"<p>{{secret}}</p>\", note: \"{{ordinary}}\" });\n    const body = {\n      header: shape(),\n      footer: shape(),\n      sections: [{ items: [shape()], layouts: [shape()] }],\n      layers: [shape()],\n      background: shape(),\n      nestedArray: [[shape()]],\n      action: {\n        type: \"$set\",\n        options: { probe: shape() },\n        payload: { probe: shape() },\n      },\n    };\n    const transformed = renderSync(body, { secret: \"LEAK\", ordinary: \"OK\" }, bodyMode) as any;\n    const namedPaths: Array<[string, Record<string, unknown>]> = [\n      [\"header\", transformed.header],\n      [\"footer\", transformed.footer],\n      [\"section item\", transformed.sections[0].items[0]],\n      [\"layout\", transformed.sections[0].layouts[0]],\n      [\"layer\", transformed.layers[0]],\n      [\"background\", transformed.background],\n      [\"nested array\", transformed.nestedArray[0][0]],\n      [\"action options\", transformed.action.options.probe],\n      [\"action payload\", transformed.action.payload.probe],\n    ];\n    expect(namedPaths).toHaveLength(9);\n    for (const [path, html] of namedPaths) {\n      expect(html.text, `${path} raw text`).toBe(\"<p>{{secret}}</p>\");\n      expect(html.note, `${path} ordinary sibling`).toBe(\"OK\");\n    }\n    expect(collectHtmlShapes(transformed)).toHaveLength(9);\n    expectSafeOutputTree(transformed);\n\n    const root = renderSync(shape(), { secret: \"LEAK\", ordinary: \"OK\" }, bodyMode);\n    expect(root).toEqual({ type: \"html\", text: \"<p>{{secret}}</p>\", note: \"OK\" });\n    expectSafeOutputTree(root);\n  });\n\n  it(\"renderSync directives: #if, #elseif, and #else outputs retain body mode\", () => {\n    const conditional = {\n      \"{{#if first}}\": { type: \"html\", text: \"IF {{secret}}\" },\n      \"{{#elseif second}}\": { type: \"html\", text: \"ELSEIF {{secret}}\" },\n      \"{{#else}}\": { type: \"html\", text: \"ELSE {{secret}}\" },\n    };\n    const selected = [\n      renderSync(conditional, { first: true, second: false, secret: \"LEAK\" }, bodyMode),\n      renderSync(conditional, { first: false, second: true, secret: \"LEAK\" }, bodyMode),\n      renderSync(conditional, { first: false, second: false, secret: \"LEAK\" }, bodyMode),\n    ];\n    expect(collectHtmlShapes(selected[0])[0]?.text).toBe(\"IF {{secret}}\");\n    expect(collectHtmlShapes(selected[1])[0]?.text).toBe(\"ELSEIF {{secret}}\");\n    expect(collectHtmlShapes(selected[2])[0]?.text).toBe(\"ELSE {{secret}}\");\n  });\n\n  it(\"renderSync directives: #each outputs and nested arrays retain body mode\", () => {\n    const output = renderSync(\n      { \"{{#each rows}}\": { type: \"html\", text: \"ROW {{secret}}\", ordinal: \"{{value}}\" } },\n      { rows: [{ value: 1 }, { value: 2 }], secret: \"LEAK\" },\n      bodyMode,\n    );\n    const html = collectHtmlShapes(output);\n    expect(html).toHaveLength(2);\n    expect(html.map((entry) => entry.text)).toEqual([\"ROW {{secret}}\", \"ROW {{secret}}\");\n    expect(html.map((entry) => entry.ordinal)).toEqual([1, 2]);\n  });\n\n  it(\"transform defaults: omitted, false, and unrelated options remain generic\", () => {\n    const input = { type: \"html\", text: \"{{secret}}\" };\n    expect(transform(input, { secret: \"GENERIC\" }).text).toBe(\"GENERIC\");\n    expect(transform(input, { secret: \"GENERIC\" }, { preserveHtmlText: false }).text).toBe(\"GENERIC\");\n    expect(transform(input, { secret: \"GENERIC\" }, { unrelated: true } as unknown as RenderOptions).text).toBe(\"GENERIC\");\n  });\n\n  it.each([\n    { dangerousKey: \"__proto__\", testId: \"safe output: resolved __proto__ is inert in body and generic modes\" },\n    { dangerousKey: \"constructor\", testId: \"safe output: resolved constructor is inert in body and generic modes\" },\n    { dangerousKey: \"prototype\", testId: \"safe output: resolved prototype is inert in body and generic modes\" },\n  ])(\n    \"$testId\",\n    ({ dangerousKey }) => {\n      for (const options of [bodyMode, undefined]) {\n        const result = transform(\n          { \"{{slot}}\": \"{{value}}\", stable: true },\n          { slot: dangerousKey, value: `${dangerousKey}-value` },\n          options,\n        );\n        expect(Object.getPrototypeOf(result)).toBe(Object.prototype);\n        expect(Object.hasOwn(result, dangerousKey)).toBe(true);\n        expect((result as Record<string, unknown>)[dangerousKey]).toBe(`${dangerousKey}-value`);\n        descriptorIsSafe(result, dangerousKey, `${dangerousKey}-value`);\n        expect(({} as Record<string, unknown>)[`${dangerousKey}-value`]).toBeUndefined();\n      }\n    },\n  );\n\n  it(\"safe output ordering: dangerous resolved keys retain mode ordering and later safe definitions win\", () => {\n    for (const dangerousKey of [\"__proto__\", \"constructor\", \"prototype\"] as const) {\n      for (const options of [bodyMode, undefined]) {\n        const log: string[] = [];\n        const context: Record<string, unknown> = {};\n        for (const [name, value] of [\n          [\"dangerous\", dangerousKey],\n          [\"dangerousValue\", \"first\"],\n          [\"ordinaryKey\", \"ordinary\"],\n          [\"ordinaryValue\", \"second\"],\n        ] as const) {\n          Object.defineProperty(context, name, {\n            enumerable: true,\n            get() {\n              log.push(name);\n              return value;\n            },\n          });\n        }\n        const input = {\n          \"{{dangerous}}\": \"{{dangerousValue}}\",\n          \"{{ordinaryKey}}\": \"{{ordinaryValue}}\",\n        };\n        const result = transform(input, context, options);\n        expect(Object.getPrototypeOf(result), dangerousKey).toBe(Object.prototype);\n        descriptorIsSafe(result, dangerousKey, \"first\");\n        expect(log, dangerousKey).toEqual(options\n          ? [\"dangerous\", \"ordinaryKey\", \"dangerousValue\", \"ordinaryValue\"]\n          : [\"dangerous\", \"dangerousValue\", \"ordinaryKey\", \"ordinaryValue\"]);\n      }\n\n      const collisionInput: Record<string, unknown> = { \"{{first}}\": \"earlier\" };\n      defineOwn(collisionInput, dangerousKey, \"later\");\n      const collision = transform(collisionInput, { first: dangerousKey }, bodyMode);\n      expect((collision as Record<string, unknown>)[dangerousKey]).toBe(\"later\");\n      descriptorIsSafe(collision, dangerousKey, \"later\");\n    }\n  });\n\n  it(\"safe output error ordering: dangerous resolved keys preserve body and generic getter order\", () => {\n    for (const dangerousKey of [\"__proto__\", \"constructor\", \"prototype\"] as const) {\n      const makeContext = (log: string[]) => {\n        const context: Record<string, unknown> = {};\n        for (const [name, value, shouldThrow] of [\n          [\"dangerousKey\", dangerousKey, false],\n          [\"laterKey\", \"later\", false],\n          [\"dangerousValue\", \"unused\", true],\n        ] as const) {\n          Object.defineProperty(context, name, {\n            enumerable: true,\n            get() {\n              log.push(name);\n              if (shouldThrow) throw new Error(\"DANGEROUS_VALUE_SENTINEL\");\n              return value;\n            },\n          });\n        }\n        return context;\n      };\n      const input = {\n        \"{{dangerousKey}}\": \"{{dangerousValue}}\",\n        \"{{laterKey}}\": \"tail\",\n      };\n\n      const bodyLog: string[] = [];\n      expect(() => transform(input, makeContext(bodyLog), bodyMode), dangerousKey)\n        .toThrow(\"DANGEROUS_VALUE_SENTINEL\");\n      expect(bodyLog, dangerousKey).toEqual([\"dangerousKey\", \"laterKey\", \"dangerousValue\"]);\n\n      const genericLog: string[] = [];\n      expect(() => transform(input, makeContext(genericLog)), dangerousKey)\n        .toThrow(\"DANGEROUS_VALUE_SENTINEL\");\n      expect(genericLog, dangerousKey).toEqual([\"dangerousKey\", \"dangerousValue\"]);\n    }\n  });\n\n  it.each([\n    {\n      dangerousKey: \"constructor\",\n      testId: \"safe output getter/error variant: resolved constructor preserves exact body and generic order\",\n    },\n    {\n      dangerousKey: \"prototype\",\n      testId: \"safe output getter/error variant: resolved prototype preserves exact body and generic order\",\n    },\n  ])(\"$testId\", ({ dangerousKey }) => {\n    for (const options of [bodyMode, undefined]) {\n      const orderLog: string[] = [];\n      const orderContext: Record<string, unknown> = {};\n      for (const [name, value] of [\n        [\"dangerousKey\", dangerousKey],\n        [\"dangerousValue\", \"safe-value\"],\n        [\"laterKey\", \"later\"],\n        [\"laterValue\", \"tail\"],\n      ] as const) {\n        Object.defineProperty(orderContext, name, {\n          enumerable: true,\n          get() {\n            orderLog.push(name);\n            return value;\n          },\n        });\n      }\n      const result = transform({\n        \"{{dangerousKey}}\": \"{{dangerousValue}}\",\n        \"{{laterKey}}\": \"{{laterValue}}\",\n      }, orderContext, options);\n      expect(orderLog).toEqual(options\n        ? [\"dangerousKey\", \"laterKey\", \"dangerousValue\", \"laterValue\"]\n        : [\"dangerousKey\", \"dangerousValue\", \"laterKey\", \"laterValue\"]);\n      expect(Object.getPrototypeOf(result)).toBe(Object.prototype);\n      descriptorIsSafe(result, dangerousKey, \"safe-value\");\n\n      const errorLog: string[] = [];\n      const errorContext: Record<string, unknown> = {};\n      for (const [name, value, throws] of [\n        [\"dangerousKey\", dangerousKey, false],\n        [\"laterKey\", \"later\", false],\n        [\"dangerousValue\", \"unused\", true],\n      ] as const) {\n        Object.defineProperty(errorContext, name, {\n          enumerable: true,\n          get() {\n            errorLog.push(name);\n            if (throws) throw new Error(`${dangerousKey.toUpperCase()}_ERROR_SENTINEL`);\n            return value;\n          },\n        });\n      }\n      expect(() => transform({\n        \"{{dangerousKey}}\": \"{{dangerousValue}}\",\n        \"{{laterKey}}\": \"tail\",\n      }, errorContext, options)).toThrow(`${dangerousKey.toUpperCase()}_ERROR_SENTINEL`);\n      expect(errorLog).toEqual(options\n        ? [\"dangerousKey\", \"laterKey\", \"dangerousValue\"]\n        : [\"dangerousKey\", \"dangerousValue\"]);\n    }\n  });\n\n  it(\"safe output: null-prototype sources work and inherited/non-enumerable/symbol keys are excluded\", () => {\n    const inherited = { inherited: \"{{secret}}\" };\n    const source = Object.create(inherited) as Record<PropertyKey, unknown>;\n    defineOwn(source, \"type\", \"html\");\n    defineOwn(source, \"text\", \"{{secret}}\");\n    Object.defineProperty(source, \"hidden\", { value: \"hidden\", enumerable: false });\n    const symbol = Symbol(\"symbol\");\n    source[symbol] = \"symbol\";\n\n    const result = transform(source, { secret: \"LEAK\" }, bodyMode);\n    expect(Object.getPrototypeOf(result)).toBe(Object.prototype);\n    expect(result).toEqual({ type: \"html\", text: \"{{secret}}\" });\n    expect(Object.hasOwn(result, \"inherited\")).toBe(false);\n    expect(Object.hasOwn(result, \"hidden\")).toBe(false);\n    expect(Object.getOwnPropertySymbols(result)).toEqual([]);\n  });\n\n  it(\"unresolved expressions: body and generic modes return the exact unresolved strings\", () => {\n    const input = { type: \"label\", text: \"prefix {{missing}} suffix\", nested: { value: \"{{alsoMissing}}\" } };\n    const expected = {\n      type: \"label\",\n      text: \"prefix {{missing}} suffix\",\n      nested: { value: \"{{alsoMissing}}\" },\n    };\n    expect(transform(input, {})).toEqual(expected);\n    expect(transform(input, {}, bodyMode)).toEqual(expected);\n  });\n});\n\n\n===== tests/verify-coverage-map.mjs =====\n#!/usr/bin/env node\n\nimport { readFileSync, readdirSync } from \"node:fs\";\nimport { dirname, join } from \"node:path\";\nimport { fileURLToPath } from \"node:url\";\n\nconst testsDirectory = dirname(fileURLToPath(import.meta.url));\nconst coverageMap = JSON.parse(readFileSync(join(testsDirectory, \"coverage-map.json\"), \"utf8\"));\nconst testFiles = readdirSync(testsDirectory)\n  .filter((name) => name.endsWith(\".test.ts\"))\n  .sort();\n\nconst declarations = [];\nconst addMatches = (source, file, expression) => {\n  for (const match of source.matchAll(expression)) {\n    declarations.push({ title: JSON.parse(match[1]), file });\n  }\n};\n\nfor (const file of testFiles) {\n  const source = readFileSync(join(testsDirectory, file), \"utf8\");\n  // Literal it(\"title\", ...) declarations.\n  addMatches(source, file, /\\bit\\s*\\(\\s*(\"(?:\\\\.|[^\"\\\\])*\")/g);\n  // Concrete rows declared through it.each(...)(\"$testId\", ...).\n  addMatches(source, file, /\\btestId\\s*:\\s*(\"(?:\\\\.|[^\"\\\\])*\")/g);\n}\n\nconst byTitle = new Map();\nfor (const declaration of declarations) {\n  const matches = byTitle.get(declaration.title) ?? [];\n  matches.push(declaration.file);\n  byTitle.set(declaration.title, matches);\n}\n\nconst errors = [];\nfor (const [title, files] of byTitle) {\n  if (files.length !== 1) errors.push(`Vitest title is not globally unique: ${JSON.stringify(title)} (${files.join(\", \")})`);\n}\n\nlet executableItems = 0;\nlet executableReferences = 0;\nconst distinctReferences = new Set();\nconst referencedExternalIds = new Map();\n\nfor (const [dod, item] of Object.entries(coverageMap.definitionOfDone ?? {})) {\n  if (item.type === \"executable\") {\n    executableItems += 1;\n    if (!Array.isArray(item.tests) || item.tests.length === 0) {\n      errors.push(`${dod} is executable but has no exact test references`);\n      continue;\n    }\n    for (const title of item.tests) {\n      executableReferences += 1;\n      distinctReferences.add(title);\n      const matches = byTitle.get(title) ?? [];\n      if (matches.length !== 1) {\n        errors.push(`${dod} reference does not resolve exactly once: ${JSON.stringify(title)} (matches=${matches.length})`);\n      }\n    }\n  } else if (item.type === \"external\") {\n    if (Object.hasOwn(item, \"tests\")) errors.push(`${dod} is external and must not claim executable tests`);\n    if (item.status !== \"pending\") errors.push(`${dod} external evidence must be pending`);\n    if (!Array.isArray(item.externalEvidenceIds) || item.externalEvidenceIds.length === 0) {\n      errors.push(`${dod} is external but has no externalEvidenceIds`);\n    } else {\n      for (const id of item.externalEvidenceIds) {\n        const owners = referencedExternalIds.get(id) ?? [];\n        owners.push(dod);\n        referencedExternalIds.set(id, owners);\n      }\n    }\n  } else {\n    errors.push(`${dod} has unsupported coverage type ${JSON.stringify(item.type)}`);\n  }\n}\n\nconst externalEvidence = coverageMap.externalEvidence ?? [];\nconst evidenceById = new Map();\nfor (const item of externalEvidence) {\n  const matches = evidenceById.get(item.id) ?? [];\n  matches.push(item);\n  evidenceById.set(item.id, matches);\n  if (item.type !== \"external\" || item.status !== \"pending\") {\n    errors.push(`external evidence ${JSON.stringify(item.id)} must be typed external/pending`);\n  }\n}\nfor (const [id, owners] of referencedExternalIds) {\n  const matches = evidenceById.get(id) ?? [];\n  if (matches.length !== 1) errors.push(`external evidence reference ${JSON.stringify(id)} resolves ${matches.length} times`);\n  if (owners.length !== 1) errors.push(`external evidence ${JSON.stringify(id)} is owned by ${owners.length} DoD items`);\n}\nfor (const [id] of evidenceById) {\n  if (!referencedExternalIds.has(id)) errors.push(`unreferenced external evidence item ${JSON.stringify(id)}`);\n}\n\nif (errors.length > 0) {\n  console.error(`FAIL: coverage-map resolution found ${errors.length} error(s)`);\n  for (const error of errors) console.error(`- ${error}`);\n  process.exitCode = 1;\n} else {\n  console.log(\n    `PASS: ${executableReferences} executable coverage references (${distinctReferences.size} distinct) `\n      + `resolve uniquely across ${declarations.length} Vitest declarations; `\n      + `${executableItems} executable DoD items and ${externalEvidence.length} external pending items validated.`,\n  );\n}\n\n\n===== tests/web-action-state-safety.test.ts =====\n// @vitest-environment jsdom\n\nimport { afterEach, beforeEach, describe, expect, it, vi } from \"vitest\";\nimport { transform } from \"@jasonette/template-engine\";\nimport { executeAction, JasonetteRenderer } from \"@jasonette/web\";\n\nfunction createHarness(): { root: HTMLElement; renderer: InstanceType<typeof JasonetteRenderer>; state: any } {\n  const root = document.createElement(\"main\");\n  document.body.appendChild(root);\n  const renderer = new (JasonetteRenderer as any)(root) as InstanceType<typeof JasonetteRenderer>;\n  (renderer as any).renderDocument({ $jason: { head: {}, body: { sections: [] } } });\n  return { root, renderer, state: (renderer as any).getState() };\n}\n\nfunction defineOwn(target: object, key: string, value: unknown): void {\n  Object.defineProperty(target, key, {\n    value,\n    enumerable: true,\n    writable: true,\n    configurable: true,\n  });\n}\n\nfunction dangerousOptions(extra: Array<[string, unknown]> = [], nullPrototype = true): Record<string, unknown> {\n  const options = Object.create(nullPrototype ? null : Object.prototype) as Record<string, unknown>;\n  for (const [key, value] of [\n    ...extra,\n    [\"__proto__\", \"proto-value\"] as [string, unknown],\n    [\"constructor\", \"constructor-value\"] as [string, unknown],\n    [\"prototype\", \"prototype-value\"] as [string, unknown],\n  ]) defineOwn(options, key, value);\n  return options;\n}\n\nfunction expectDangerousDestination(destination: Record<string, unknown>): void {\n  expect(Object.getPrototypeOf(destination)).toBe(Object.prototype);\n  for (const [key, value] of [\n    [\"__proto__\", \"proto-value\"],\n    [\"constructor\", \"constructor-value\"],\n    [\"prototype\", \"prototype-value\"],\n  ] as const) {\n    const descriptor = Object.getOwnPropertyDescriptor(destination, key);\n    expect(descriptor).toEqual({ value, enumerable: true, writable: true, configurable: true });\n  }\n}\n\nbeforeEach(() => {\n  document.body.replaceChildren();\n});\n\nafterEach(() => {\n  document.body.replaceChildren();\n  vi.restoreAllMocks();\n  vi.unstubAllGlobals();\n});\n\ndescribe(\"black-box production action transforms and state sinks\", () => {\n  it(\"executeAction production vector: action options remain generic\", async () => {\n    const { state } = createHarness();\n    await executeAction(\n      { type: \"$set\", options: { probe: { type: \"html\", text: \"{{$jason.value}}\" } } } as any,\n      state,\n      { value: \"ACTION\" } as any,\n    );\n    expect(state.local.probe).toEqual({ type: \"html\", text: \"ACTION\" });\n  });\n\n  it(\"executeAction continuation vector: continuation options retain generic interpolation\", async () => {\n    const { state } = createHarness();\n    await executeAction({\n      type: \"$set\",\n      options: { firstStage: true },\n      success: {\n        type: \"$set\",\n        options: { continuationProbe: { type: \"html\", text: \"{{$jason.value}}\" } },\n      },\n    } as any, state, { value: \"ACTION\" } as any);\n    expect(state.local.continuationProbe.text).toBe(\"ACTION\");\n  });\n\n  it(\"executeAction embedded vector: body-raw action options interpolate during their second generic transform\", async () => {\n    const { state } = createHarness();\n    const body = transform(\n      {\n        action: {\n          type: \"$set\",\n          options: { probe: { type: \"html\", text: \"{{$jason.value}}\" } },\n        },\n      },\n      { $jason: { value: \"BODY\" } },\n      { preserveHtmlText: true },\n    ) as any;\n    expect(body.action.options.probe.text).toBe(\"{{$jason.value}}\");\n    await executeAction(body.action, state, { value: \"ACTION\" } as any);\n    expect(state.local.probe.text).toBe(\"ACTION\");\n  });\n\n  it.each([\n    { actionType: \"$set\", destination: \"local\", testId: \"safe state sink: $set defines inert own keys\" },\n    { actionType: \"$cache.set\", destination: \"cache\", testId: \"safe state sink: $cache.set defines inert own keys\" },\n    { actionType: \"$global.set\", destination: \"global\", testId: \"safe state sink: $global.set defines inert own keys\" },\n  ])(\"$testId\", async ({ actionType, destination }) => {\n    const { state } = createHarness();\n    await executeAction({ type: actionType, options: dangerousOptions() } as any, state);\n    expectDangerousDestination(state[destination]);\n    expect(({} as Record<string, unknown>)[\"proto-value\"]).toBeUndefined();\n  });\n\n  it(\"session inner storage: null-prototype options become a fresh safe ordinary session\", async () => {\n    const { state } = createHarness();\n    const options = dangerousOptions([[\"domain\", \"safe.example.com\"], [\"header\", { Authorization: \"own\" }]]);\n    await executeAction({ type: \"$session.set\", options } as any, state);\n\n    expect(Object.getPrototypeOf(state.sessions)).toBe(Object.prototype);\n    expect(Object.hasOwn(state.sessions, \"safe.example.com\")).toBe(true);\n    const stored = state.sessions[\"safe.example.com\"];\n    expect(stored).not.toBe(options);\n    expectDangerousDestination(stored);\n    expect(stored.header).toEqual({ Authorization: \"own\" });\n  });\n\n  it.each([\n    { domain: \"__proto__\", testId: \"session outer storage: normalized domain __proto__ is an inert reachable own key\" },\n    { domain: \"constructor\", testId: \"session outer storage: normalized domain constructor is an inert reachable own key\" },\n    { domain: \"prototype\", testId: \"session outer storage: normalized domain prototype is an inert reachable own key\" },\n  ])(\n    \"$testId\",\n    async ({ domain }) => {\n      const { state } = createHarness();\n      await executeAction({\n        type: \"$session.set\",\n        options: dangerousOptions([[\"domain\", domain], [\"marker\", domain]]),\n      } as any, state);\n\n      expect(Object.getPrototypeOf(state.sessions)).toBe(Object.prototype);\n      expect(Object.hasOwn(state.sessions, domain)).toBe(true);\n      expect(state.sessions[domain].marker).toBe(domain);\n      expect(Object.getPrototypeOf(state.sessions[domain])).toBe(Object.prototype);\n      const descriptor = Object.getOwnPropertyDescriptor(state.sessions, domain);\n      expect(descriptor).toMatchObject({ enumerable: true, writable: true, configurable: true });\n    },\n  );\n\n  it(\"session lookup control: own api.example.com decorates a network request\", async () => {\n    const { state } = createHarness();\n    state.sessions = {};\n    defineOwn(state.sessions, \"api.example.com\", {\n      header: { \"X-Own-Session\": \"OWN-SESSION-SENTINEL\" },\n    });\n\n    const fetchMock = vi.fn(async (_url: unknown, _init?: unknown) => ({\n      ok: true,\n      status: 200,\n      statusText: \"OK\",\n      headers: { forEach: () => undefined, get: () => \"application/json\" },\n      text: async () => \"{}\",\n      json: async () => ({}),\n    }));\n    vi.stubGlobal(\"fetch\", fetchMock);\n\n    await executeAction({\n      type: \"$network.request\",\n      options: { url: \"https://api.example.com/resource\", method: \"GET\" },\n    } as any, state);\n\n    expect(fetchMock).toHaveBeenCalledTimes(1);\n    const [, init] = fetchMock.mock.calls[0] as [unknown, RequestInit | undefined];\n    const normalizedHeaders = new Headers(init?.headers);\n    expect(normalizedHeaders.get(\"X-Own-Session\")).toBe(\"OWN-SESSION-SENTINEL\");\n  });\n\n  it(\"session lookup: inherited api.example.com does not decorate a network request\", async () => {\n    const { state } = createHarness();\n    const inheritedSession = {\n      header: { \"X-Inherited-Session\": \"must-not-appear\" },\n      headers: { \"X-Inherited-Headers\": \"must-not-appear\" },\n      body: \"INHERITED-BODY-SENTINEL\",\n    };\n    state.sessions = Object.create({ \"api.example.com\": inheritedSession });\n\n    const fetchMock = vi.fn(async (_url: unknown, _init?: unknown) => ({\n      ok: true,\n      status: 200,\n      statusText: \"OK\",\n      headers: { forEach: () => undefined, get: () => \"application/json\" },\n      text: async () => \"{}\",\n      json: async () => ({}),\n    }));\n    vi.stubGlobal(\"fetch\", fetchMock);\n\n    await executeAction({\n      type: \"$network.request\",\n      options: { url: \"https://api.example.com/resource\", method: \"GET\" },\n    } as any, state);\n\n    expect(fetchMock).toHaveBeenCalledTimes(1);\n    const [, init] = fetchMock.mock.calls[0] as [unknown, RequestInit | undefined];\n    const normalizedHeaders = new Headers(init?.headers);\n    expect(normalizedHeaders.get(\"X-Inherited-Session\")).toBeNull();\n    expect(normalizedHeaders.get(\"X-Inherited-Headers\")).toBeNull();\n    expect(init?.body).not.toBe(\"INHERITED-BODY-SENTINEL\");\n  });\n});\n\ndescribe(\"black-box own-only action and named-action registries\", () => {\n  it.each([\n    { name: \"toString\", testId: \"named trigger registry: own prototype-colliding entry toString executes\" },\n    { name: \"constructor\", testId: \"named trigger registry: own prototype-colliding entry constructor executes\" },\n    { name: \"__proto__\", testId: \"named trigger registry: own prototype-colliding entry __proto__ executes\" },\n  ])(\n    \"$testId\",\n    async ({ name }) => {\n      const { state } = createHarness();\n      state.actions = {};\n      defineOwn(state.actions, name, { type: \"$set\", options: { triggerMarker: name } });\n      await executeAction({ trigger: name } as any, state);\n      expect(state.local.triggerMarker).toBe(name);\n    },\n  );\n\n  it.each([\n    { name: \"toString\", testId: \"named trigger registry: inherited prototype-colliding entry toString is ignored\" },\n    { name: \"constructor\", testId: \"named trigger registry: inherited prototype-colliding entry constructor is ignored\" },\n    { name: \"__proto__\", testId: \"named trigger registry: inherited prototype-colliding entry __proto__ is ignored\" },\n  ])(\n    \"$testId\",\n    async ({ name }) => {\n      const { state } = createHarness();\n      const inherited: Record<string, unknown> = {};\n      defineOwn(inherited, name, { type: \"$set\", options: { inheritedTriggerRan: name } });\n      state.actions = Object.create(inherited);\n      const result = await executeAction({ trigger: name } as any, state);\n      expect(result).toBeUndefined();\n      expect(Object.hasOwn(state.local, \"inheritedTriggerRan\")).toBe(false);\n    },\n  );\n\n  it.each([\n    { name: \"toString\", testId: \"$lambda registry: own prototype-colliding entry toString executes\" },\n    { name: \"constructor\", testId: \"$lambda registry: own prototype-colliding entry constructor executes\" },\n    { name: \"__proto__\", testId: \"$lambda registry: own prototype-colliding entry __proto__ executes\" },\n  ])(\n    \"$testId\",\n    async ({ name }) => {\n      const { state } = createHarness();\n      state.actions = {};\n      defineOwn(state.actions, name, { type: \"$set\", options: { lambdaMarker: name } });\n      await executeAction({ type: \"$lambda\", options: { name } } as any, state);\n      expect(state.local.lambdaMarker).toBe(name);\n    },\n  );\n\n  it.each([\n    { name: \"toString\", testId: \"$lambda registry: inherited prototype-colliding entry toString is ignored\" },\n    { name: \"constructor\", testId: \"$lambda registry: inherited prototype-colliding entry constructor is ignored\" },\n    { name: \"__proto__\", testId: \"$lambda registry: inherited prototype-colliding entry __proto__ is ignored\" },\n  ])(\n    \"$testId\",\n    async ({ name }) => {\n      const { state } = createHarness();\n      const inherited: Record<string, unknown> = {};\n      defineOwn(inherited, name, { type: \"$set\", options: { inheritedLambdaRan: name } });\n      state.actions = Object.create(inherited);\n      const result = await executeAction({ type: \"$lambda\", options: { name } } as any, state);\n      expect(result).toBeUndefined();\n      expect(Object.hasOwn(state.local, \"inheritedLambdaRan\")).toBe(false);\n    },\n  );\n\n  it(\"named registry: own trigger and lambda action arrays execute\", async () => {\n    const { state } = createHarness();\n    state.actions = {};\n    defineOwn(state.actions, \"sequence\", [\n      { type: \"$set\", options: { first: 1 } },\n      { type: \"$set\", options: { second: 2 } },\n    ]);\n    defineOwn(state.actions, \"lambdaSequence\", [\n      { type: \"$set\", options: { lambdaFirst: 1 } },\n      { type: \"$set\", options: { lambdaSecond: 2 } },\n    ]);\n\n    await executeAction({ trigger: \"sequence\" } as any, state);\n    expect(state.local).toMatchObject({ first: 1, second: 2 });\n    await executeAction({ type: \"$lambda\", options: { name: \"lambdaSequence\" } } as any, state);\n    expect(state.local).toMatchObject({ lambdaFirst: 1, lambdaSecond: 2 });\n  });\n\n  it(\"named registry: inherited type falls through to an own trigger\", async () => {\n    const { state } = createHarness();\n    state.actions = {};\n    defineOwn(state.actions, \"fallback\", { type: \"$set\", options: { fallback: \"ran\" } });\n    const inheritedTypeAction = Object.create({ type: \"$set\" });\n    defineOwn(inheritedTypeAction, \"trigger\", \"fallback\");\n    defineOwn(inheritedTypeAction, \"options\", { inheritedTypeRan: true });\n\n    await executeAction(inheritedTypeAction, state);\n\n    expect(state.local.fallback).toBe(\"ran\");\n    expect(Object.hasOwn(state.local, \"inheritedTypeRan\")).toBe(false);\n  });\n\n  it(\"named registry: a non-string own type can still fall through to an own string trigger\", async () => {\n    const { state } = createHarness();\n    state.actions = { fallback: { type: \"$set\", options: { nonStringFallback: \"ran\" } } };\n    await executeAction({ type: 9, trigger: \"fallback\" } as any, state);\n    expect(state.local.nonStringFallback).toBe(\"ran\");\n  });\n\n  it(\"named registry: inherited/non-string triggers and own non-action entries are ignored\", async () => {\n    const { state } = createHarness();\n    state.actions = {\n      invalidString: \"not an action\",\n      invalidFunction: () => ({ type: \"$set\", options: { invalidRan: true } }),\n    };\n    const inheritedTrigger = Object.create({ trigger: \"invalidString\" });\n    for (const action of [\n      inheritedTrigger,\n      { trigger: 7 },\n      { trigger: \"invalidString\" },\n      { type: \"$lambda\", options: { name: \"invalidFunction\" } },\n    ]) {\n      expect(await executeAction(action as any, state)).toBeUndefined();\n    }\n    expect(Object.hasOwn(state.local, \"invalidRan\")).toBe(false);\n  });\n\n  it(\"action type registry: an inherited callable handler returns undefined and is never invoked\", async () => {\n    const { state } = createHarness();\n    const type = \"$redInheritedActionHandler\";\n    const inheritedTypeHandler = vi.fn(() => {\n      throw new Error(\"INHERITED_ACTION_HANDLER_SENTINEL\");\n    });\n    const previous = Object.getOwnPropertyDescriptor(Object.prototype, type);\n    Object.defineProperty(Object.prototype, type, {\n      value: inheritedTypeHandler,\n      enumerable: false,\n      writable: true,\n      configurable: true,\n    });\n\n    try {\n      const result = await executeAction({\n        type,\n        success: { type: \"$set\", options: { inheritedContinuationRan: true } },\n      } as any, state);\n\n      expect(result).toBeUndefined();\n      expect(Object.hasOwn(state.local, \"inheritedContinuationRan\")).toBe(false);\n      expect(inheritedTypeHandler).toHaveBeenCalledTimes(0);\n    } finally {\n      if (previous) Object.defineProperty(Object.prototype, type, previous);\n      else delete (Object.prototype as Record<string, unknown>)[type];\n    }\n  });\n\n  it(\"named action registry: an inherited callable is ignored by trigger and $lambda with zero invocations\", async () => {\n    const { state } = createHarness();\n    const name = \"redInheritedNamedAction\";\n    const inheritedNamedAction = vi.fn(() => {\n      throw new Error(\"INHERITED_NAMED_ACTION_SENTINEL\");\n    });\n    state.actions = Object.create({ [name]: inheritedNamedAction });\n\n    const triggerResult = await executeAction({\n      trigger: name,\n      success: { type: \"$set\", options: { inheritedTriggerContinuationRan: true } },\n    } as any, state);\n    const lambdaResult = await executeAction({\n      type: \"$lambda\",\n      options: { name },\n      success: { type: \"$set\", options: { inheritedLambdaContinuationRan: true } },\n    } as any, state);\n\n    expect(triggerResult).toBeUndefined();\n    expect(lambdaResult).toBeUndefined();\n    expect(Object.hasOwn(state.local, \"inheritedTriggerContinuationRan\")).toBe(false);\n    expect(Object.hasOwn(state.local, \"inheritedLambdaContinuationRan\")).toBe(false);\n    expect(inheritedNamedAction).toHaveBeenCalledTimes(0);\n  });\n\n  it.each([\n    { type: \"unknown-action\", testId: \"action type registry: own unknown type unknown-action returns undefined without continuation\" },\n    { type: \"__proto__\", testId: \"action type registry: own unknown type __proto__ returns undefined without continuation\" },\n    { type: \"constructor\", testId: \"action type registry: own unknown type constructor returns undefined without continuation\" },\n    { type: \"prototype\", testId: \"action type registry: own unknown type prototype returns undefined without continuation\" },\n    { type: \"toString\", testId: \"action type registry: own unknown type toString returns undefined without continuation\" },\n  ])(\n    \"$testId\",\n    async ({ type }) => {\n      const { state } = createHarness();\n      const result = await executeAction({\n        type,\n        success: { type: \"$set\", options: { continuationRan: type } },\n      } as any, state);\n      expect(result).toBeUndefined();\n      expect(Object.hasOwn(state.local, \"continuationRan\")).toBe(false);\n    },\n  );\n\n  it(\"action type registry: inherited, missing, and non-string types invoke no inherited handler\", async () => {\n    const { state } = createHarness();\n    const inherited = Object.create({ type: \"$set\" });\n    defineOwn(inherited, \"options\", { inheritedTypeRan: true });\n    for (const action of [inherited, {}, { type: null }, { type: {} }]) {\n      expect(await executeAction(action as any, state)).toBeUndefined();\n    }\n    expect(Object.hasOwn(state.local, \"inheritedTypeRan\")).toBe(false);\n  });\n});\n\n\n===== tests/web-component-safety.test.ts =====\n// @vitest-environment jsdom\n\nimport { afterEach, beforeEach, describe, expect, it, vi } from \"vitest\";\nimport { transform } from \"@jasonette/template-engine\";\nimport { renderComponent } from \"@jasonette/web\";\nimport { installIframeObserver } from \"./iframe-observer\";\n\nfunction render(component: unknown): HTMLElement {\n  return (renderComponent as unknown as (value: unknown) => HTMLElement)(component);\n}\n\nfunction ownRecord(entries: Array<[string, unknown]>, prototype: object | null = Object.prototype): Record<string, unknown> {\n  const result = Object.create(prototype) as Record<string, unknown>;\n  for (const [key, value] of entries) {\n    Object.defineProperty(result, key, { value, enumerable: true, writable: true, configurable: true });\n  }\n  return result;\n}\n\nbeforeEach(() => {\n  document.body.replaceChildren();\n});\n\nafterEach(() => {\n  document.body.replaceChildren();\n  vi.restoreAllMocks();\n});\n\ndescribe(\"black-box HTML component dispatch and source selection\", () => {\n  it(\"component dispatch: only own exact html enters the HTML renderer\", () => {\n    const ownHtml = render({ type: \"html\", text: \"<p>own</p>\" });\n    expect(ownHtml.querySelector(\"iframe\")).not.toBeNull();\n\n    const inheritedHtml = render(ownRecord([[\"text\", \"<p>inherited type</p>\"]], { type: \"html\" }));\n    expect(inheritedHtml.querySelector(\"iframe\")).toBeNull();\n    expect(inheritedHtml.classList.contains(\"jasonette-label\")).toBe(true);\n\n    for (const component of [{}, { type: 7 }, { type: null }, { type: {} }, { type: \"label\", text: \"label\" }]) {\n      const element = render(component);\n      expect(element.querySelector(\"iframe\")).toBeNull();\n      expect(element.classList.contains(\"jasonette-label\")).toBe(true);\n    }\n  });\n\n  it(\"component registry: an inherited callable renderer is ignored with exact unknown-component outcome\", () => {\n    const type = \"redInheritedComponentRenderer\";\n    const expectedUnknown = render({ type, text: \"must remain unknown\" });\n    const inheritedRenderer = vi.fn(() => {\n      throw new Error(\"INHERITED_COMPONENT_RENDERER_SENTINEL\");\n    });\n    const previous = Object.getOwnPropertyDescriptor(Object.prototype, type);\n    Object.defineProperty(Object.prototype, type, {\n      value: inheritedRenderer,\n      enumerable: false,\n      writable: true,\n      configurable: true,\n    });\n\n    try {\n      const element = render({ type, text: \"must remain unknown\" });\n      expect(element).toBeInstanceOf(HTMLElement);\n      expect(element.outerHTML).toBe(expectedUnknown.outerHTML);\n      expect(element.querySelector(\"iframe\")).toBeNull();\n      expect(element.classList.contains(\"jasonette-label\")).toBe(false);\n      expect(element.hidden).toBe(false);\n      expect(element.textContent).toBe(expectedUnknown.textContent);\n      expect(inheritedRenderer).toHaveBeenCalledTimes(0);\n    } finally {\n      if (previous) Object.defineProperty(Object.prototype, type, previous);\n      else delete (Object.prototype as Record<string, unknown>)[type];\n    }\n  });\n\n  it.each([\n    { type: \"unknown-component\", testId: \"component dispatch: own unknown type unknown-component is visible and never calls a prototype member\" },\n    { type: \"__proto__\", testId: \"component dispatch: own unknown type __proto__ is visible and never calls a prototype member\" },\n    { type: \"constructor\", testId: \"component dispatch: own unknown type constructor is visible and never calls a prototype member\" },\n    { type: \"prototype\", testId: \"component dispatch: own unknown type prototype is visible and never calls a prototype member\" },\n    { type: \"toString\", testId: \"component dispatch: own unknown type toString is visible and never calls a prototype member\" },\n  ])(\n    \"$testId\",\n    ({ type }) => {\n      const element = render({ type, text: \"sentinel\" });\n      expect(element).toBeInstanceOf(HTMLElement);\n      expect(element.querySelector(\"iframe\")).toBeNull();\n      expect(element.classList.contains(\"jasonette-label\")).toBe(false);\n      expect(element.hidden).toBe(false);\n      expect(element.textContent?.trim().length).toBeGreaterThan(0);\n    },\n  );\n\n  const sourceCases: Array<{\n    testId: string;\n    component: Record<string, unknown>;\n    kind: \"inline\" | \"url\" | \"none\";\n    value?: string;\n  }> = [\n    {\n      testId: \"component source matrix: text wins over URL\",\n      component: { type: \"html\", text: \"<p>inline</p>\", url: \"https://example.com/ignored\" },\n      kind: \"inline\",\n      value: \"<p>inline</p>\",\n    },\n    { testId: \"component source matrix: whitespace text is valid\", component: { type: \"html\", text: \" \", url: \"https://example.com/ignored\" }, kind: \"inline\", value: \" \" },\n    { testId: \"component source matrix: empty text falls back to URL\", component: { type: \"html\", text: \"\", url: \"https://example.com/url\" }, kind: \"url\", value: \"https://example.com/url\" },\n    { testId: \"component source matrix: non-string text falls back to URL\", component: { type: \"html\", text: 42, url: \"https://example.com/url\" }, kind: \"url\", value: \"https://example.com/url\" },\n    { testId: \"component source matrix: missing values select none\", component: { type: \"html\" }, kind: \"none\" },\n    { testId: \"component source matrix: empty values select none\", component: { type: \"html\", text: \"\", url: \"\" }, kind: \"none\" },\n    { testId: \"component source matrix: non-string values select none\", component: { type: \"html\", text: {}, url: 17 }, kind: \"none\" },\n    {\n      testId: \"component source matrix: inherited-only values select none\",\n      component: ownRecord([[\"type\", \"html\"]], { text: \"<p>inherited</p>\", url: \"https://example.com/inherited\" }),\n      kind: \"none\",\n    },\n    {\n      testId: \"component source matrix: inherited text plus own URL selects URL\",\n      component: ownRecord([[\"type\", \"html\"], [\"url\", \"https://example.com/own\"]], { text: \"<p>inherited</p>\" }),\n      kind: \"url\",\n      value: \"https://example.com/own\",\n    },\n    {\n      testId: \"component source matrix: inherited URL plus own text selects inline\",\n      component: ownRecord([[\"type\", \"html\"], [\"text\", \"<p>own</p>\"]], { url: \"https://example.com/inherited\" }),\n      kind: \"inline\",\n      value: \"<p>own</p>\",\n    },\n  ];\n\n  it.each(sourceCases)(\"$testId\", ({ component, kind, value }) => {\n    const wrapper = render(component);\n    const iframe = wrapper.querySelector(\"iframe\");\n    if (kind === \"none\") {\n      expect(iframe).toBeNull();\n      return;\n    }\n    expect(iframe).toBeInstanceOf(HTMLIFrameElement);\n    expect(iframe?.getAttribute(\"sandbox\")).toBe(\"allow-scripts\");\n    if (kind === \"inline\") {\n      expect(iframe?.getAttribute(\"srcdoc\")).toBe(value);\n      expect(iframe?.hasAttribute(\"srcdoc\")).toBe(true);\n      expect(iframe?.hasAttribute(\"src\")).toBe(false);\n    } else {\n      expect(iframe?.getAttribute(\"src\")).toBe(value);\n      expect(iframe?.hasAttribute(\"src\")).toBe(true);\n      expect(iframe?.hasAttribute(\"srcdoc\")).toBe(false);\n    }\n  });\n\n  const cssCases: Array<{ testId: string; component: Record<string, unknown>; expected: string }> = [\n    { testId: \"component CSS matrix: absent\", component: { type: \"html\", text: \"<p>x</p>\" }, expected: \"<p>x</p>\" },\n    { testId: \"component CSS matrix: empty\", component: { type: \"html\", text: \"<p>x</p>\", css: \"\" }, expected: \"<p>x</p>\" },\n    { testId: \"component CSS matrix: null\", component: { type: \"html\", text: \"<p>x</p>\", css: null }, expected: \"<p>x</p>\" },\n    { testId: \"component CSS matrix: undefined\", component: { type: \"html\", text: \"<p>x</p>\", css: undefined }, expected: \"<p>x</p>\" },\n    { testId: \"component CSS matrix: non-string\", component: { type: \"html\", text: \"<p>x</p>\", css: 12 }, expected: \"<p>x</p>\" },\n    {\n      testId: \"component CSS matrix: inherited\",\n      component: ownRecord([[\"type\", \"html\"], [\"text\", \"<p>x</p>\"]], { css: \"p{color:red}\" }),\n      expected: \"<p>x</p>\",\n    },\n    { testId: \"component CSS matrix: one space\", component: { type: \"html\", text: \"<p>x</p>\", css: \" \" }, expected: \"<style> </style><p>x</p>\" },\n    { testId: \"component CSS matrix: tab and newline\", component: { type: \"html\", text: \"<p>x</p>\", css: \"\\t\\n\" }, expected: \"<style>\\t\\n</style><p>x</p>\" },\n    { testId: \"component CSS matrix: mixed-case closer\", component: { type: \"html\", text: \"<p>x</p>\", css: \"</STYLE>\" }, expected: \"<style><\\\\/style></style><p>x</p>\" },\n    {\n      testId: \"component CSS matrix: repeated mixed-case closers\",\n      component: { type: \"html\", text: \"<p>x</p>\", css: \"a</style>b</StYlE>c\" },\n      expected: \"<style>a<\\\\/style>b<\\\\/style>c</style><p>x</p>\",\n    },\n  ];\n\n  it.each(cssCases)(\"$testId\", ({ component, expected }) => {\n    const iframe = render(component).querySelector(\"iframe\");\n    expect(iframe?.getAttribute(\"srcdoc\")).toBe(expected);\n    if (expected.includes(\"\\\\/style\")) {\n      expect(expected).toContain(\"\\\\\");\n      expect(Array.from(expected).some((character) => character.codePointAt(0) === 0x5c)).toBe(true);\n    }\n  });\n\n  it(\"component CSS matrix: arbitrary non-empty whitespace is retained code-unit-for-code-unit\", () => {\n    const css = \" \\t\\n\\r\\f \";\n    const iframe = render({ type: \"html\", text: \"<p>x</p>\", css }).querySelector(\"iframe\");\n    expect(iframe?.getAttribute(\"srcdoc\")).toBe(`<style>${css}</style><p>x</p>`);\n  });\n\n  it(\"component invalid HTML: srcdoc receives the authored string unchanged\", () => {\n    const invalid = \"<p><unclosed>&{{not_evaluated}}<script>\";\n    const iframe = render({ type: \"html\", text: invalid }).querySelector(\"iframe\");\n    expect(iframe?.getAttribute(\"srcdoc\")).toBe(invalid);\n  });\n\n  it(\"component non-string source and CSS sentinels are never coerced\", () => {\n    const coercionLog: string[] = [];\n    const sentinel = (name: string) => ({\n      [Symbol.toPrimitive]() {\n        coercionLog.push(`${name}:toPrimitive`);\n        throw new Error(`${name} must not be coerced`);\n      },\n      toString() {\n        coercionLog.push(`${name}:toString`);\n        throw new Error(`${name} must not be stringified`);\n      },\n      valueOf() {\n        coercionLog.push(`${name}:valueOf`);\n        throw new Error(`${name} must not use valueOf`);\n      },\n    });\n\n    const noSource = render({ type: \"html\", text: sentinel(\"text\"), url: sentinel(\"url\") });\n    expect(noSource.querySelector(\"iframe\")).toBeNull();\n    const inline = render({ type: \"html\", text: \"<p>own</p>\", css: sentinel(\"css\") });\n    expect(inline.querySelector(\"iframe\")?.getAttribute(\"srcdoc\")).toBe(\"<p>own</p>\");\n    expect(coercionLog).toEqual([]);\n  });\n\n  it(\"iframe observer self-test: Attr source and sandbox plus replaceChildren cannot evade tracing\", () => {\n    const observer = installIframeObserver();\n    try {\n      observer.run(\"component\", () => {\n        const parent = document.createElement(\"div\");\n        const iframe = document.createElement(\"iframe\");\n        const sandbox = document.createAttribute(\"sandbox\");\n        sandbox.value = \"allow-scripts\";\n        iframe.attributes.setNamedItem(sandbox);\n        const source = document.createAttribute(\"srcdoc\");\n        source.value = \"<p>self-test</p>\";\n        iframe.setAttributeNode(source);\n        parent.replaceChildren(iframe);\n      });\n      expect(observer.records).toHaveLength(1);\n      expect(observer.records[0].events).toEqual([\n        \"CREATE(component)\",\n        'SANDBOX(\"allow-scripts\")',\n        \"SOURCE(srcdoc)\",\n        \"APPEND\",\n        \"RETURN\",\n      ]);\n    } finally {\n      observer.restore();\n    }\n  });\n\n  it(\"iframe observer self-test: pre-existing DocumentFragment insertion is traced before later sandbox\", () => {\n    const fragment = document.createDocumentFragment();\n    const observer = installIframeObserver();\n    try {\n      observer.run(\"component\", () => {\n        const iframe = document.createElement(\"iframe\");\n        fragment.append(iframe);\n        iframe.setAttribute(\"sandbox\", \"allow-scripts\");\n        iframe.srcdoc = \"<p>late policy</p>\";\n      });\n      expect(observer.records).toHaveLength(1);\n      expect(observer.records[0].events).toEqual([\n        \"CREATE(component)\",\n        \"APPEND\",\n        'SANDBOX(\"allow-scripts\")',\n        \"SOURCE(srcdoc)\",\n        \"RETURN\",\n      ]);\n    } finally {\n      observer.restore();\n    }\n  });\n\n  it(\"iframe observer self-test: pre-existing detached Element insertion is traced before later sandbox\", () => {\n    const parent = document.createElement(\"section\");\n    const observer = installIframeObserver();\n    try {\n      observer.run(\"component\", () => {\n        const iframe = document.createElement(\"iframe\");\n        parent.appendChild(iframe);\n        iframe.setAttribute(\"sandbox\", \"allow-scripts\");\n        iframe.setAttribute(\"srcdoc\", \"<p>late element policy</p>\");\n      });\n      expect(observer.records).toHaveLength(1);\n      expect(observer.records[0].events).toEqual([\n        \"CREATE(component)\",\n        \"APPEND\",\n        'SANDBOX(\"allow-scripts\")',\n        \"SOURCE(srcdoc)\",\n        \"RETURN\",\n      ]);\n    } finally {\n      observer.restore();\n    }\n  });\n\n  it(\"iframe observer self-test: replacement removal property source and insertAdjacentElement are traced\", () => {\n    const observer = installIframeObserver();\n    try {\n      observer.run(\"component\", () => {\n        const parent = document.createElement(\"div\");\n        const marker = document.createElement(\"span\");\n        parent.append(marker);\n        const iframe = document.createElement(\"iframe\");\n        iframe.setAttribute(\"sandbox\", \"allow-scripts\");\n        const replacement = document.createAttribute(\"sandbox\");\n        replacement.value = \"allow-forms\";\n        iframe.attributes.setNamedItem(replacement);\n        iframe.toggleAttribute(\"sandbox\", false);\n        iframe.src = \"https://example.com/self-test\";\n        marker.insertAdjacentElement(\"afterend\", iframe);\n      });\n      expect(observer.records).toHaveLength(1);\n      expect(observer.records[0].events).toEqual([\n        \"CREATE(component)\",\n        'SANDBOX(\"allow-scripts\")',\n        'SANDBOX(\"allow-forms\")',\n        \"SANDBOX(null)\",\n        \"SOURCE(src)\",\n        \"APPEND\",\n        \"RETURN\",\n      ]);\n    } finally {\n      observer.restore();\n    }\n  });\n\n  it.each([\n    { testId: \"component trace: inline has exact finite creation order\", component: { type: \"html\", text: \"<p>inline</p>\" }, attribute: \"srcdoc\" },\n    { testId: \"component trace: URL has exact finite creation order\", component: { type: \"html\", url: \"https://example.com/page\" }, attribute: \"src\" },\n  ])(\"$testId\", ({ component, attribute }) => {\n    const observer = installIframeObserver();\n    try {\n      const wrapper = observer.run(\"component\", () => render(component));\n      expect(observer.records).toHaveLength(1);\n      expect(observer.records[0].events).toEqual([\n        \"CREATE(component)\",\n        'SANDBOX(\"allow-scripts\")',\n        `SOURCE(${attribute})`,\n        \"APPEND\",\n        \"RETURN\",\n      ]);\n      expect(observer.records[0].events.filter((event) => event.startsWith(\"SANDBOX\"))).toEqual(['SANDBOX(\"allow-scripts\")']);\n      expect(observer.records[0].iframe.getAttribute(\"sandbox\")).toBe(\"allow-scripts\");\n      expect(wrapper.isConnected).toBe(false);\n    } finally {\n      observer.restore();\n    }\n  });\n\n  it(\"component topology: detached wrapper owns an exact class/data/style iframe\", () => {\n    const wrapper = render({ type: \"html\", text: \"<p>x</p>\", style: { width: 320, height: 120 } });\n    const iframe = wrapper.querySelector(\"iframe\") as HTMLIFrameElement;\n    expect(wrapper.isConnected).toBe(false);\n    expect(wrapper.classList.contains(\"jasonette-html\")).toBe(true);\n    expect(wrapper.getAttribute(\"data-jasonette-type\")).toBe(\"html\");\n    expect(iframe.parentElement).toBe(wrapper);\n    expect(iframe.style.width).toBe(\"100%\");\n    expect(iframe.style.border).toBe(\"none\");\n    expect(iframe.getAttribute(\"sandbox\")).toBe(\"allow-scripts\");\n    expect(iframe.hasAttribute(\"srcdoc\")).toBe(true);\n    expect(iframe.hasAttribute(\"src\")).toBe(false);\n  });\n\n  it(\"component no-source trace: invalid own and inherited-only sources create no iframe record\", () => {\n    const observer = installIframeObserver();\n    try {\n      const inheritedOnly = ownRecord([[\"type\", \"html\"]], { text: \"<p>inherited</p>\" });\n      const first = observer.run(\"component\", () => render({ type: \"html\", text: \"\", url: 9 }));\n      const second = observer.run(\"component\", () => render(inheritedOnly));\n      expect(first.querySelector(\"iframe\")).toBeNull();\n      expect(second.querySelector(\"iframe\")).toBeNull();\n      expect(observer.records).toEqual([]);\n    } finally {\n      observer.restore();\n    }\n  });\n\n  it(\"transform-to-render: inherited text, URL, and CSS are excluded while mixed own fields win\", () => {\n    const inheritedOnly = ownRecord([[\"type\", \"html\"]], {\n      text: \"<p>inherited</p>\",\n      url: \"https://example.com/inherited\",\n      css: \"p{color:red}\",\n    });\n    const inheritedTextOwnUrl = ownRecord(\n      [[\"type\", \"html\"], [\"url\", \"https://example.com/own\"]],\n      { text: \"<p>inherited</p>\" },\n    );\n    const inheritedUrlOwnText = ownRecord(\n      [[\"type\", \"html\"], [\"text\", \"<p>own</p>\"]],\n      { url: \"https://example.com/inherited\" },\n    );\n    const inheritedCssOwnText = ownRecord(\n      [[\"type\", \"html\"], [\"text\", \"<p>own</p>\"]],\n      { css: \"p{color:red}\" },\n    );\n\n    const transformed = [inheritedOnly, inheritedTextOwnUrl, inheritedUrlOwnText, inheritedCssOwnText]\n      .map((entry) => transform(entry, {}, { preserveHtmlText: true }));\n    expect(render(transformed[0]).querySelector(\"iframe\")).toBeNull();\n    expect(render(transformed[1]).querySelector(\"iframe\")?.getAttribute(\"src\")).toBe(\"https://example.com/own\");\n    expect(render(transformed[2]).querySelector(\"iframe\")?.getAttribute(\"srcdoc\")).toBe(\"<p>own</p>\");\n    expect(render(transformed[3]).querySelector(\"iframe\")?.getAttribute(\"srcdoc\")).toBe(\"<p>own</p>\");\n  });\n});\n\n\n===== tests/web-renderer-safety.test.ts =====\n// @vitest-environment jsdom\n\nimport { afterEach, beforeEach, describe, expect, it } from \"vitest\";\nimport { transform } from \"@jasonette/template-engine\";\nimport { executeAction, JasonetteRenderer } from \"@jasonette/web\";\nimport { installIframeObserver } from \"./iframe-observer\";\n\nfunction harness(): { root: HTMLElement; renderer: any } {\n  const root = document.createElement(\"main\");\n  document.body.appendChild(root);\n  const renderer = new (JasonetteRenderer as any)(root);\n  return { root, renderer };\n}\n\nfunction staticDocument(body: unknown): unknown {\n  return { $jason: { head: {}, body } };\n}\n\nfunction templateDocument(body: unknown, data: Record<string, unknown>): unknown {\n  return { $jason: { head: { data, templates: { body } } } };\n}\n\nfunction ownRecord(entries: Array<[string, unknown]>, prototype: object | null = Object.prototype): Record<string, unknown> {\n  const record = Object.create(prototype) as Record<string, unknown>;\n  for (const [key, value] of entries) {\n    Object.defineProperty(record, key, { value, enumerable: true, writable: true, configurable: true });\n  }\n  return record;\n}\n\nfunction bodyFor(path: \"canonical\" | \"legacy\", background: unknown): Record<string, unknown> {\n  return path === \"canonical\"\n    ? { background, sections: [] }\n    : { style: { background }, sections: [] };\n}\n\nfunction backgroundIframe(root: HTMLElement): HTMLIFrameElement | null {\n  return root.querySelector(\"iframe.jasonette-background-web\");\n}\n\nfunction throwingCoercionSentinel(name: string, calls: string[]): object {\n  return {\n    [Symbol.toPrimitive]() {\n      calls.push(`${name}:toPrimitive`);\n      throw new Error(`${name} must not be coerced`);\n    },\n    toString() {\n      calls.push(`${name}:toString`);\n      throw new Error(`${name} must not be stringified`);\n    },\n    valueOf() {\n      calls.push(`${name}:valueOf`);\n      throw new Error(`${name} must not use valueOf`);\n    },\n  };\n}\n\nfunction assertExactBackground(\n  iframe: HTMLIFrameElement,\n  kind: \"inline\" | \"url\",\n  expected: string,\n): void {\n  expect(iframe.classList.contains(\"jasonette-background-web\")).toBe(true);\n  expect(iframe.getAttribute(\"aria-hidden\")).toBe(\"true\");\n  expect(iframe.getAttribute(\"sandbox\")).toBe(\"allow-scripts\");\n  if (kind === \"inline\") {\n    expect(iframe.getAttribute(\"srcdoc\")).toBe(expected);\n    expect(iframe.hasAttribute(\"srcdoc\")).toBe(true);\n    expect(iframe.hasAttribute(\"src\")).toBe(false);\n  } else {\n    expect(iframe.getAttribute(\"src\")).toBe(expected);\n    expect(iframe.hasAttribute(\"src\")).toBe(true);\n    expect(iframe.hasAttribute(\"srcdoc\")).toBe(false);\n  }\n}\n\nbeforeEach(() => {\n  document.body.replaceChildren();\n});\n\nafterEach(() => {\n  document.body.replaceChildren();\n});\n\ndescribe(\"black-box HTML background source, policy, and precedence\", () => {\n  it.each([\n    {\n      testId: \"background locations: static canonical inline CSS\",\n      document: staticDocument({ background: { type: \"html\", text: \"<p>static</p>\", css: \"p{color:red}\" }, sections: [] }),\n      kind: \"inline\" as const,\n      expected: \"<style>p{color:red}</style><p>static</p>\",\n    },\n    {\n      testId: \"background locations: static legacy URL\",\n      document: staticDocument({ style: { background: { type: \"html\", url: \"https://example.com/bg.html\" } }, sections: [] }),\n      kind: \"url\" as const,\n      expected: \"https://example.com/bg.html\",\n    },\n    {\n      testId: \"background locations: templated canonical raw text and transformed CSS\",\n      document: templateDocument(\n        { background: { type: \"html\", text: \"<p>{{$jason.secret}}</p>\", css: \"p{color:{{$jason.color}}}\" }, sections: [] },\n        { secret: \"LEAK\", color: \"red\" },\n      ),\n      kind: \"inline\" as const,\n      expected: \"<style>p{color:red}</style><p>{{$jason.secret}}</p>\",\n    },\n    {\n      testId: \"background locations: templated legacy dynamic URL\",\n      document: templateDocument(\n        { style: { background: { type: \"html\", url: \"{{$jason.url}}\" } }, sections: [] },\n        { url: \"https://example.com/dynamic.html\" },\n      ),\n      kind: \"url\" as const,\n      expected: \"https://example.com/dynamic.html\",\n    },\n  ])(\"$testId\", ({ document: authored, kind, expected }) => {\n    const { root, renderer } = harness();\n    renderer.renderDocument(authored);\n    const iframe = backgroundIframe(root);\n    expect(iframe).not.toBeNull();\n    assertExactBackground(iframe as HTMLIFrameElement, kind, expected);\n  });\n\n  it.each([\n    { path: \"canonical\" as const, testId: \"background no-source: canonical static and templated locations create no iframe\" },\n    { path: \"legacy\" as const, testId: \"background no-source: legacy static and templated locations create no iframe\" },\n  ])(\"$testId\", ({ path }) => {\n    for (const authored of [\n      staticDocument(bodyFor(path, { type: \"html\" })),\n      templateDocument(bodyFor(path, { type: \"html\" }), { value: \"unused\" }),\n    ]) {\n      const { root, renderer } = harness();\n      const observer = installIframeObserver();\n      try {\n        observer.run(\"background\", () => renderer.renderDocument(authored));\n        expect(backgroundIframe(root)).toBeNull();\n        expect(observer.records).toEqual([]);\n      } finally {\n        observer.restore();\n      }\n    }\n  });\n\n  it.each([\n    {\n      path: \"canonical\" as const,\n      testId: \"background coercion sentinels: canonical path never coerces text URL or CSS\",\n    },\n    {\n      path: \"legacy\" as const,\n      testId: \"background coercion sentinels: legacy path never coerces text URL or CSS\",\n    },\n  ])(\"$testId\", ({ path }) => {\n    const calls: string[] = [];\n    const text = throwingCoercionSentinel(`${path}:text`, calls);\n    const url = throwingCoercionSentinel(`${path}:url`, calls);\n    const css = throwingCoercionSentinel(`${path}:css`, calls);\n    const observer = installIframeObserver();\n    try {\n      const noSourceHarness = harness();\n      observer.run(\"background\", () => {\n        noSourceHarness.renderer.renderDocument(staticDocument(bodyFor(path, {\n          type: \"html\",\n          text,\n          url,\n        })));\n      });\n      expect(backgroundIframe(noSourceHarness.root)).toBeNull();\n      expect(observer.records).toEqual([]);\n\n      const cssHarness = harness();\n      observer.run(\"background\", () => {\n        cssHarness.renderer.renderDocument(staticDocument(bodyFor(path, {\n          type: \"html\",\n          text: \"<p>own</p>\",\n          css,\n        })));\n      });\n      const iframe = backgroundIframe(cssHarness.root) as HTMLIFrameElement;\n      assertExactBackground(iframe, \"inline\", \"<p>own</p>\");\n      expect(observer.records).toHaveLength(1);\n      expect(observer.records[0].events).toEqual([\n        \"CREATE(background)\",\n        'SANDBOX(\"allow-scripts\")',\n        \"SOURCE(srcdoc)\",\n        \"APPEND\",\n        \"RETURN\",\n      ]);\n      expect(calls).toEqual([]);\n    } finally {\n      observer.restore();\n    }\n  });\n\n  const matrixCases: Array<{\n    name: string;\n    value: () => Record<string, unknown>;\n    kind: \"inline\" | \"url\" | \"none\";\n    expected?: string;\n  }> = [\n    { name: \"inline\", value: () => ({ type: \"html\", text: \"<p>inline</p>\" }), kind: \"inline\", expected: \"<p>inline</p>\" },\n    { name: \"URL\", value: () => ({ type: \"html\", url: \"https://example.com/url\" }), kind: \"url\", expected: \"https://example.com/url\" },\n    { name: \"dual source text wins\", value: () => ({ type: \"html\", text: \"<p>dual</p>\", url: \"https://example.com/ignored\" }), kind: \"inline\", expected: \"<p>dual</p>\" },\n    { name: \"whitespace text\", value: () => ({ type: \"html\", text: \" \", url: \"https://example.com/ignored\" }), kind: \"inline\", expected: \" \" },\n    { name: \"empty text URL fallback\", value: () => ({ type: \"html\", text: \"\", url: \"https://example.com/url\" }), kind: \"url\", expected: \"https://example.com/url\" },\n    { name: \"non-string text URL fallback\", value: () => ({ type: \"html\", text: 9, url: \"https://example.com/url\" }), kind: \"url\", expected: \"https://example.com/url\" },\n    { name: \"empty text and URL\", value: () => ({ type: \"html\", text: \"\", url: \"\" }), kind: \"none\" },\n    { name: \"object text and non-string URL\", value: () => ({ type: \"html\", text: {}, url: 17 }), kind: \"none\" },\n    { name: \"empty CSS\", value: () => ({ type: \"html\", text: \"<p>x</p>\", css: \"\" }), kind: \"inline\", expected: \"<p>x</p>\" },\n    { name: \"null CSS\", value: () => ({ type: \"html\", text: \"<p>x</p>\", css: null }), kind: \"inline\", expected: \"<p>x</p>\" },\n    { name: \"undefined CSS\", value: () => ({ type: \"html\", text: \"<p>x</p>\", css: undefined }), kind: \"inline\", expected: \"<p>x</p>\" },\n    { name: \"non-string CSS\", value: () => ({ type: \"html\", text: \"<p>x</p>\", css: 12 }), kind: \"inline\", expected: \"<p>x</p>\" },\n    { name: \"space CSS\", value: () => ({ type: \"html\", text: \"<p>x</p>\", css: \" \" }), kind: \"inline\", expected: \"<style> </style><p>x</p>\" },\n    { name: \"tab newline CSS\", value: () => ({ type: \"html\", text: \"<p>x</p>\", css: \"\\t\\n\" }), kind: \"inline\", expected: \"<style>\\t\\n</style><p>x</p>\" },\n    { name: \"arbitrary whitespace CSS\", value: () => ({ type: \"html\", text: \"<p>x</p>\", css: \" \\t\\n\\r\\f \" }), kind: \"inline\", expected: \"<style> \\t\\n\\r\\f </style><p>x</p>\" },\n    { name: \"mixed-case CSS escape\", value: () => ({ type: \"html\", text: \"<p>x</p>\", css: \"</STYLE>\" }), kind: \"inline\", expected: \"<style><\\\\/style></style><p>x</p>\" },\n    { name: \"repeated CSS escape\", value: () => ({ type: \"html\", text: \"<p>x</p>\", css: \"a</style>b</StYlE>c\" }), kind: \"inline\", expected: \"<style>a<\\\\/style>b<\\\\/style>c</style><p>x</p>\" },\n    { name: \"invalid HTML unchanged\", value: () => ({ type: \"html\", text: \"<p><unclosed>&raw\" }), kind: \"inline\", expected: \"<p><unclosed>&raw\" },\n    {\n      name: \"inherited-only fields\",\n      value: () => ownRecord([[\"type\", \"html\"]], { text: \"<p>inherited</p>\", url: \"https://example.com/inherited\", css: \"x\" }),\n      kind: \"none\",\n    },\n    {\n      name: \"inherited text own URL\",\n      value: () => ownRecord([[\"type\", \"html\"], [\"url\", \"https://example.com/own\"]], { text: \"<p>inherited</p>\" }),\n      kind: \"url\",\n      expected: \"https://example.com/own\",\n    },\n    {\n      name: \"inherited URL own text\",\n      value: () => ownRecord([[\"type\", \"html\"], [\"text\", \"<p>own</p>\"]], { url: \"https://example.com/inherited\" }),\n      kind: \"inline\",\n      expected: \"<p>own</p>\",\n    },\n    {\n      name: \"inherited CSS own text\",\n      value: () => ownRecord([[\"type\", \"html\"], [\"text\", \"<p>own</p>\"]], { css: \"p{color:red}\" }),\n      kind: \"inline\",\n      expected: \"<p>own</p>\",\n    },\n    { name: \"empty and non-string sources\", value: () => ({ type: \"html\", text: \"\", url: 12 }), kind: \"none\" },\n    { name: \"no source\", value: () => ({ type: \"html\" }), kind: \"none\" },\n    { name: \"missing type\", value: () => ({ text: \"<p>x</p>\" }), kind: \"none\" },\n    { name: \"non-HTML type\", value: () => ({ type: \"label\", text: \"<p>x</p>\" }), kind: \"none\" },\n    { name: \"prototype type __proto__\", value: () => ({ type: \"__proto__\", text: \"<p>x</p>\" }), kind: \"none\" },\n    { name: \"prototype type constructor\", value: () => ({ type: \"constructor\", text: \"<p>x</p>\" }), kind: \"none\" },\n    { name: \"prototype type prototype\", value: () => ({ type: \"prototype\", text: \"<p>x</p>\" }), kind: \"none\" },\n    { name: \"prototype type toString\", value: () => ({ type: \"toString\", text: \"<p>x</p>\" }), kind: \"none\" },\n    { name: \"non-string type\", value: () => ({ type: 4, text: \"<p>x</p>\" }), kind: \"none\" },\n    {\n      name: \"inherited HTML type\",\n      value: () => ownRecord([[\"text\", \"<p>x</p>\"]], { type: \"html\" }),\n      kind: \"none\",\n    },\n  ];\n  const runFullMatrix = (path: \"canonical\" | \"legacy\"): void => {\n    for (const { name, value, kind, expected } of matrixCases) {\n      const { root, renderer } = harness();\n      const observer = installIframeObserver();\n      try {\n        observer.run(\n          \"background\",\n          () => renderer.renderDocument(staticDocument(bodyFor(path, value()))),\n        );\n        const iframe = backgroundIframe(root);\n        if (kind === \"none\") {\n          expect(iframe, `${path} ${name}`).toBeNull();\n          expect(observer.records, `${path} ${name} trace`).toEqual([]);\n          continue;\n        }\n        expect(iframe, `${path} ${name}`).not.toBeNull();\n        assertExactBackground(iframe as HTMLIFrameElement, kind, expected as string);\n        const attribute = kind === \"inline\" ? \"srcdoc\" : \"src\";\n        expect(observer.records, `${path} ${name} trace`).toHaveLength(1);\n        expect(observer.records[0].events, `${path} ${name} event order`).toEqual([\n          \"CREATE(background)\",\n          'SANDBOX(\"allow-scripts\")',\n          `SOURCE(${attribute})`,\n          \"APPEND\",\n          \"RETURN\",\n        ]);\n        expect(observer.records[0].events.filter((event) => event.startsWith(\"SANDBOX\"))).toHaveLength(1);\n      } finally {\n        observer.restore();\n        root.remove();\n      }\n    }\n  };\n\n  it(\"background full matrix: canonical path covers every type source inherited-field and CSS vector\", () => {\n    runFullMatrix(\"canonical\");\n  });\n\n  it(\"background full matrix: legacy path covers every type source inherited-field and CSS vector\", () => {\n    runFullMatrix(\"legacy\");\n  });\n\n  it.each([\n    { testId: \"background precedence: canonical false blocks fallback\", canonical: false, expected: \"none\" },\n    { testId: \"background precedence: canonical empty object blocks fallback\", canonical: {}, expected: \"none\" },\n    { testId: \"background precedence: canonical HTML no source blocks fallback\", canonical: { type: \"html\" }, expected: \"none\" },\n    { testId: \"background precedence: canonical null uses legacy\", canonical: null, expected: \"legacy\" },\n    { testId: \"background precedence: canonical undefined uses legacy\", canonical: undefined, expected: \"legacy\" },\n  ])(\"$testId\", ({ canonical, expected }) => {\n    const { root, renderer } = harness();\n    const body = ownRecord([\n      [\"background\", canonical],\n      [\"style\", { background: { type: \"html\", text: \"<p>legacy</p>\" } }],\n      [\"sections\", []],\n    ]);\n    const observer = installIframeObserver();\n    try {\n      observer.run(\"background\", () => renderer.renderDocument(staticDocument(body)));\n      const iframe = backgroundIframe(root);\n      if (expected === \"none\") {\n        expect(iframe).toBeNull();\n        expect(observer.records).toEqual([]);\n      } else {\n        expect(iframe?.getAttribute(\"srcdoc\")).toBe(\"<p>legacy</p>\");\n        expect(observer.records).toHaveLength(1);\n        expect(observer.records[0].events).toEqual([\n          \"CREATE(background)\",\n          'SANDBOX(\"allow-scripts\")',\n          \"SOURCE(srcdoc)\",\n          \"APPEND\",\n          \"RETURN\",\n        ]);\n      }\n    } finally {\n      observer.restore();\n    }\n  });\n\n  const expectObservedLegacyFallback = (body: Record<string, unknown>): void => {\n    const { root, renderer } = harness();\n    const observer = installIframeObserver();\n    try {\n      observer.run(\"background\", () => renderer.renderDocument(staticDocument(body)));\n      expect(backgroundIframe(root)?.getAttribute(\"srcdoc\")).toBe(\"<p>legacy</p>\");\n      expect(observer.records).toHaveLength(1);\n      expect(observer.records[0].events).toEqual([\n        \"CREATE(background)\",\n        'SANDBOX(\"allow-scripts\")',\n        \"SOURCE(srcdoc)\",\n        \"APPEND\",\n        \"RETURN\",\n      ]);\n    } finally {\n      observer.restore();\n    }\n  };\n\n  const expectObservedNoBackground = (body: Record<string, unknown>): void => {\n    const { root, renderer } = harness();\n    const observer = installIframeObserver();\n    try {\n      observer.run(\"background\", () => renderer.renderDocument(staticDocument(body)));\n      expect(backgroundIframe(root)).toBeNull();\n      expect(observer.records).toEqual([]);\n    } finally {\n      observer.restore();\n    }\n  };\n\n  it(\"background precedence: missing canonical value uses an own legacy fallback\", () => {\n    expectObservedLegacyFallback({\n      style: { background: { type: \"html\", text: \"<p>legacy</p>\" } },\n      sections: [],\n    });\n  });\n\n  it(\"background precedence: inherited canonical value uses an own legacy fallback\", () => {\n    expectObservedLegacyFallback(ownRecord(\n      [[\"style\", { background: { type: \"html\", text: \"<p>legacy</p>\" } }], [\"sections\", []]],\n      { background: { type: \"html\", text: \"<p>inherited canonical</p>\" } },\n    ));\n  });\n\n  it(\"background precedence: inherited canonical path without fallback has no transient iframe\", () => {\n    expectObservedNoBackground(ownRecord([[\"sections\", []]], {\n      background: { type: \"html\", text: \"<p>inherited canonical</p>\" },\n    }));\n  });\n\n  it(\"background precedence: inherited style path segment has no transient iframe\", () => {\n    expectObservedNoBackground(ownRecord([[\"sections\", []]], {\n      style: { background: { type: \"html\", text: \"<p>inherited style</p>\" } },\n    }));\n  });\n\n  it(\"background precedence: inherited legacy background segment has no transient iframe\", () => {\n    expectObservedNoBackground({\n      style: ownRecord([], { background: { type: \"html\", text: \"<p>inherited legacy</p>\" } }),\n      sections: [],\n    });\n  });\n\n  it.each([\n    { testId: \"background trace: inline has exact finite creation order\", background: { type: \"html\", text: \"<p>inline</p>\" }, attribute: \"srcdoc\" },\n    { testId: \"background trace: URL has exact finite creation order\", background: { type: \"html\", url: \"https://example.com/background\" }, attribute: \"src\" },\n  ])(\"$testId\", ({ background, attribute }) => {\n    const { renderer } = harness();\n    const observer = installIframeObserver();\n    try {\n      observer.run(\n        \"background\",\n        () => renderer.renderDocument(staticDocument({ background, sections: [] })),\n      );\n      expect(observer.records).toHaveLength(1);\n      expect(observer.records[0].events).toEqual([\n        \"CREATE(background)\",\n        'SANDBOX(\"allow-scripts\")',\n        `SOURCE(${attribute})`,\n        \"APPEND\",\n        \"RETURN\",\n      ]);\n    } finally {\n      observer.restore();\n    }\n  });\n\n  it(\"background topology: iframe is a direct root child before foreground content\", () => {\n    const { root, renderer } = harness();\n    renderer.renderDocument(staticDocument({\n      background: { type: \"html\", text: \"<p>background</p>\" },\n      sections: [{ items: [{ type: \"label\", text: \"foreground\" }] }],\n    }));\n    const iframe = backgroundIframe(root) as HTMLIFrameElement;\n    const foreground = root.querySelector(\".jasonette-label\") as HTMLElement;\n    expect(iframe.parentElement).toBe(root);\n    expect(iframe.className).toBe(\"jasonette-background-web\");\n    expect(iframe.getAttribute(\"aria-hidden\")).toBe(\"true\");\n    expect(iframe.compareDocumentPosition(foreground) & Node.DOCUMENT_POSITION_FOLLOWING).not.toBe(0);\n  });\n});\n\ndescribe(\"black-box renderer body-mode integration and liveness\", () => {\n  const bodyTemplate = {\n    sections: [{\n      items: [\n        {\n          type: \"{{$jason.kind}}\",\n          text: \"<script>window.__html_boundary = \\\"{{$jason.secret}}\\\"</script>\",\n          style: { height: \"{{$jason.height}}\" },\n        },\n        { type: \"label\", text: \"{{$jason.label}}\" },\n      ],\n    }],\n  };\n\n  it(\"integration smoke: exact initial and actual $render paths preserve raw HTML and remain live\", async () => {\n    const { root, renderer } = harness();\n    const observer = installIframeObserver();\n    try {\n      observer.run(\"component\", () => renderer.renderDocument(templateDocument(bodyTemplate, {\n        kind: \"html\",\n        secret: \"LEAK\",\n        height: 40,\n        label: \"first\",\n      })));\n\n      const firstIframe = root.querySelector(\".jasonette-html iframe\") as HTMLIFrameElement;\n      const firstWrapper = root.querySelector(\".jasonette-html\") as HTMLElement;\n      const firstLabel = root.querySelector(\".jasonette-label\") as HTMLElement;\n      expect(firstIframe).toBeInstanceOf(HTMLIFrameElement);\n      expect(firstIframe.getAttribute(\"srcdoc\")).toContain('{{$jason.secret}}');\n      expect(firstIframe.getAttribute(\"srcdoc\")).toContain('<script>window.__html_boundary = \"{{$jason.secret}}\"</script>');\n      expect(firstWrapper.style.height).toBe(\"40px\");\n      expect(firstLabel.textContent).toBe(\"first\");\n      expect(firstIframe.getAttribute(\"sandbox\")).toBe(\"allow-scripts\");\n      expect(firstIframe.hasAttribute(\"src\")).toBe(false);\n\n      await observer.runAsync(\"component\", () => executeAction({\n        type: \"$render\",\n        options: { data: { kind: \"html\", secret: \"SECOND\", height: 80, label: \"second\" } },\n      } as any, renderer.getState()));\n\n      const secondIframe = root.querySelector(\".jasonette-html iframe\") as HTMLIFrameElement;\n      expect(secondIframe).not.toBe(firstIframe);\n      expect((root.querySelector(\".jasonette-html\") as HTMLElement).style.height).toBe(\"80px\");\n      expect(root.querySelector(\".jasonette-label\")?.textContent).toBe(\"second\");\n      expect(secondIframe.getAttribute(\"srcdoc\")).toContain(\"{{$jason.secret}}\");\n      expect(secondIframe.getAttribute(\"srcdoc\")).not.toContain(\"SECOND\");\n      expect(firstIframe.isConnected).toBe(false);\n      expect(firstIframe.getAttribute(\"sandbox\")).toBe(\"allow-scripts\");\n      expect(secondIframe.getAttribute(\"sandbox\")).toBe(\"allow-scripts\");\n\n      const generic = transform(\n        { type: \"html\", text: \"{{secret}}\" },\n        { secret: \"GENERIC\" },\n      );\n      expect(generic.text).toBe(\"GENERIC\");\n\n      await executeAction({\n        type: \"$set\",\n        options: { probe: { type: \"html\", text: \"{{$jason.value}}\" } },\n      } as any, renderer.getState(), { value: \"ACTION\" } as any);\n      expect(renderer.getState().local.probe.text).toBe(\"ACTION\");\n\n      expect(observer.records).toHaveLength(2);\n      for (const record of observer.records) {\n        expect(record.events).toEqual([\n          \"CREATE(component)\",\n          'SANDBOX(\"allow-scripts\")',\n          \"SOURCE(srcdoc)\",\n          \"APPEND\",\n          \"RETURN\",\n        ]);\n      }\n    } finally {\n      observer.restore();\n    }\n  });\n\n  it(\"integration smoke: exact production executeAction vector yields ACTION\", async () => {\n    const { renderer } = harness();\n    renderer.renderDocument(templateDocument(bodyTemplate, {\n      kind: \"html\",\n      secret: \"LEAK\",\n      height: 40,\n      label: \"first\",\n    }));\n    await executeAction({\n      type: \"$set\",\n      options: { probe: { type: \"html\", text: \"{{$jason.value}}\" } },\n    } as any, renderer.getState(), { value: \"ACTION\" } as any);\n    expect(renderer.getState().local.probe.text).toBe(\"ACTION\");\n  });\n\n  it(\"renderer liveness: actual $render replaces background and retains old/new sandbox endpoints\", async () => {\n    const { root, renderer } = harness();\n    const template = {\n      background: { type: \"html\", url: \"{{$jason.url}}\" },\n      sections: [{ items: [{ type: \"label\", text: \"{{$jason.label}}\" }] }],\n    };\n    renderer.renderDocument(templateDocument(template, { url: \"https://example.com/first\", label: \"first\" }));\n    const first = backgroundIframe(root) as HTMLIFrameElement;\n    expect(first.getAttribute(\"src\")).toBe(\"https://example.com/first\");\n    expect(first.getAttribute(\"sandbox\")).toBe(\"allow-scripts\");\n\n    await executeAction({\n      type: \"$render\",\n      options: { data: { url: \"https://example.com/second\", label: \"second\" } },\n    } as any, renderer.getState());\n\n    const second = backgroundIframe(root) as HTMLIFrameElement;\n    expect(second).not.toBe(first);\n    expect(second.getAttribute(\"src\")).toBe(\"https://example.com/second\");\n    expect(first.isConnected).toBe(false);\n    expect(first.getAttribute(\"sandbox\")).toBe(\"allow-scripts\");\n    expect(second.getAttribute(\"sandbox\")).toBe(\"allow-scripts\");\n    expect(root.querySelector(\".jasonette-label\")?.textContent).toBe(\"second\");\n  });\n\n  it(\"renderer content: component/background authored HTML stays raw while CSS and ordinary siblings interpolate\", () => {\n    const { root, renderer } = harness();\n    renderer.renderDocument(templateDocument({\n      background: {\n        type: \"html\",\n        text: \"<script>background('{{$jason.secret}}')</script>\",\n        css: \"body{color:{{$jason.color}}}\",\n      },\n      sections: [{ items: [\n        {\n          type: \"html\",\n          text: \"<script>component('{{$jason.secret}}')</script>\",\n          style: { height: \"{{$jason.height}}\" },\n        },\n        { type: \"label\", text: \"{{$jason.label}}\" },\n      ] }],\n    }, { secret: \"LEAK\", color: \"red\", height: 44, label: \"ordinary\" }));\n\n    const background = backgroundIframe(root) as HTMLIFrameElement;\n    const component = root.querySelector(\".jasonette-html iframe\") as HTMLIFrameElement;\n    expect(background.getAttribute(\"srcdoc\")).toBe(\n      \"<style>body{color:red}</style><script>background('{{$jason.secret}}')</script>\",\n    );\n    expect(component.getAttribute(\"srcdoc\")).toBe(\"<script>component('{{$jason.secret}}')</script>\");\n    expect((root.querySelector(\".jasonette-html\") as HTMLElement).style.height).toBe(\"44px\");\n    expect(root.querySelector(\".jasonette-label\")?.textContent).toBe(\"ordinary\");\n  });\n});\n"
    }
  ],
  "withheld_context": [
    {
      "label": "green_workspace",
      "kind": "green_workspace_path",
      "sha256": "cd0aa9856147b6c5b4ff2b7dfee5da20aa38253099ef1b4a64aced233c9afe29",
      "content": "withheld",
      "samples": [
        "/tmp/foundry-019f4bec-43dc-742b-80b0-e42b7d46ebdd-green"
      ]
    },
    {
      "label": "implementation",
      "kind": "implementation_code",
      "sha256": "de7d1b721a1e0632b7cf04edf5032c8ecffa9f9a08492152b926f1a5a7e765d7",
      "content": "withheld",
      "samples": [
        "packages/template-engine/src/transformer.ts",
        "const renderers: Record<string, ComponentRenderer> = {};",
        "Object.assign(state.local, opts);"
      ]
    }
  ],
  "redactions": []
}
