Feature: Initial render and actual render-action integration
  Public-package black-box evidence only; no browser-enforcement claim is made.

  Scenario Outline: <title>
    Given <setup>
    When <action>
    Then <expected>

    Examples: structural render and rerender locations
      | title | setup | action | expected |
      | production body pipeline: body root propagates HTML text mode | the body root owns style.background HTML with data-red-location="body-root", raw {{$jason.secret}} text, dynamic CSS height, and a section containing only label "body-root-{{$jason.label}}"; initial data are secret="FIRST-LEAK", label="first", height=31, and location-qualified URL; $render data are secret="SECOND-LEAK", label="second", height=62, and location-qualified URL | render the authored document, then execute actual $render with replacement data | on initial render and $render, the sole matching jasonette-background-web iframe is a direct renderer-root child, has only srcdoc with data-red-location="body-root", literal {{$jason.secret}}, and CSS heights 31 then 62; no jasonette-html wrapper exists; exact foreground labels are body-root-first then body-root-second |
      | production body pipeline: header propagates HTML text mode | body.header.items owns location-marked inline HTML, URL HTML, and label components while body.sections is empty; initial data are secret="FIRST-LEAK", label="first", height=31, and location-qualified URL; $render data are secret="SECOND-LEAK", label="second", height=62, and location-qualified URL | render the authored document, then execute actual $render with replacement data | on initial render and $render, exactly one component iframe selected by srcdoc data-red-location="header" retains literal {{$jason.secret}} and has wrapper heights 31px then 62px; exactly one exclusive URL iframe changes from "https://example.com/header-first.html" to "https://example.com/header-second.html"; exact labels change from "header-first" to "header-second" |
      | production body pipeline: footer propagates HTML text mode | body.footer.items owns location-marked inline HTML, URL HTML, and label components while body.sections is empty; initial data are secret="FIRST-LEAK", label="first", height=31, and location-qualified URL; $render data are secret="SECOND-LEAK", label="second", height=62, and location-qualified URL | render the authored document, then execute actual $render with replacement data | on initial render and $render, exactly one component iframe selected by srcdoc data-red-location="footer" retains literal {{$jason.secret}} and has wrapper heights 31px then 62px; exactly one exclusive URL iframe changes from "https://example.com/footer-first.html" to "https://example.com/footer-second.html"; exact labels change from "footer-first" to "footer-second" |
      | production body pipeline: section propagates HTML text mode | body.sections[0].items directly owns location-marked inline HTML, URL HTML, and label components; initial data are secret="FIRST-LEAK", label="first", height=31, and location-qualified URL; $render data are secret="SECOND-LEAK", label="second", height=62, and location-qualified URL | render the authored document, then execute actual $render with replacement data | on initial render and $render, exactly one component iframe selected by srcdoc data-red-location="section" retains literal {{$jason.secret}} and has wrapper heights 31px then 62px; exactly one exclusive URL iframe changes from "https://example.com/section-first.html" to "https://example.com/section-second.html"; exact labels change from "section-first" to "section-second" |
      | production body pipeline: layout propagates HTML text mode | body.sections[0].items[0] is a vertical layout whose items own location-marked inline HTML, URL HTML, and label components; initial data are secret="FIRST-LEAK", label="first", height=31, and location-qualified URL; $render data are secret="SECOND-LEAK", label="second", height=62, and location-qualified URL | render the authored document, then execute actual $render with replacement data | on initial render and $render, exactly one component iframe selected by srcdoc data-red-location="layout" retains literal {{$jason.secret}} and has wrapper heights 31px then 62px; exactly one exclusive URL iframe changes from "https://example.com/layout-first.html" to "https://example.com/layout-second.html"; exact labels change from "layout-first" to "layout-second" |
      | production body pipeline: layer propagates HTML text mode | body.layers[0] is a vertical layer whose items own location-marked inline HTML, URL HTML, and label components, separate from a foreground section; initial data are secret="FIRST-LEAK", label="first", height=31, and location-qualified URL; $render data are secret="SECOND-LEAK", label="second", height=62, and location-qualified URL | render the authored document, then execute actual $render with replacement data | on initial render and $render, exactly one component iframe selected by srcdoc data-red-location="layer" retains literal {{$jason.secret}} and has wrapper heights 31px then 62px; exactly one exclusive URL iframe changes from "https://example.com/layer-first.html" to "https://example.com/layer-second.html"; exact labels change from "layer-first" to "layer-second" |
      | production body pipeline: background propagates HTML text mode | the body root directly owns canonical background HTML with data-red-location="background", raw {{$jason.secret}} text, and dynamic CSS height, plus a foreground section; initial data are secret="FIRST-LEAK", label="first", height=31, and location-qualified URL; $render data are secret="SECOND-LEAK", label="second", height=62, and location-qualified URL | render the authored document, then execute actual $render with replacement data | on initial render and $render, the sole matching jasonette-background-web iframe is a direct renderer-root child, has only srcdoc with data-red-location="background", literal {{$jason.secret}}, and CSS heights 31 then 62; no jasonette-html wrapper exists; exact foreground labels are background-first then background-second |
      | production body pipeline: nested arrays propagate HTML text mode | body.sections[0].items has three array levels around location-marked inline HTML, URL HTML, and label components; initial data are secret="FIRST-LEAK", label="first", height=31, and location-qualified URL; $render data are secret="SECOND-LEAK", label="second", height=62, and location-qualified URL | render the authored document, then execute actual $render with replacement data | on initial render and $render, exactly one component iframe selected by srcdoc data-red-location="nested-array" retains literal {{$jason.secret}} and has wrapper heights 31px then 62px; exactly one exclusive URL iframe changes from "https://example.com/nested-array-first.html" to "https://example.com/nested-array-second.html"; exact labels change from "nested-array-first" to "nested-array-second" |
      | production body pipeline: directives propagate HTML text mode | body.sections[0].items contains an enabled #if whose result is location-marked inline HTML, URL HTML, and label components; initial data are secret="FIRST-LEAK", label="first", height=31, and location-qualified URL; $render data are secret="SECOND-LEAK", label="second", height=62, and location-qualified URL | render the authored document, then execute actual $render with replacement data | on initial render and $render, exactly one component iframe selected by srcdoc data-red-location="directive" retains literal {{$jason.secret}} and has wrapper heights 31px then 62px; exactly one exclusive URL iframe changes from "https://example.com/directive-first.html" to "https://example.com/directive-second.html"; exact labels change from "directive-first" to "directive-second" |

  Scenario Outline: <title>
    Given <setup>
    When <action>
    Then <expected>

    Examples: rendered control activation and replacement workflows
      | title | setup | action | expected |
      | production body pipeline: embedded action propagates HTML text mode | a label control template owns action {type:"{{$jason.actionType}}", options:{actionObjectReached:"{{$jason.label}}"}} with actionType="$set"; initial data are secret="FIRST-LEAK", label="first", height=31, and location-qualified URL; $render data are secret="SECOND-LEAK", label="second", height=62, and location-qualified URL | render the authored document, activate its exact rendered control, execute actual $render with replacement data, then activate the exact replacement control | clicking the exact rendered controls run-action-first and run-action-second after their respective renders reaches the transformed action object and sets local.actionObjectReached to "first" then "second" |
      | production body pipeline: embedded action options propagate HTML text mode | a label control template owns a literal $set action whose options.actionOptionsProbe is {type:"html", text:"{{$jason.secret}}", style:{height:"{{$jason.height}}"}}; initial data are secret="FIRST-LEAK", label="first", height=31, and location-qualified URL; $render data are secret="SECOND-LEAK", label="second", height=62, and location-qualified URL | render the authored document, activate its exact rendered control, execute actual $render with replacement data, then activate the exact replacement control | clicking the exact rendered controls run-action-options-first and run-action-options-second stores options.actionOptionsProbe with type="html", literal text {{$jason.secret}}, and style heights 31 then 62 |
      | production body pipeline: embedded action payload propagates HTML text mode | a label control template owns a literal $set action whose options.actionPayloadProbe.payload is {type:"html", text:"{{$jason.secret}}", style:{height:"{{$jason.height}}"}}; initial data are secret="FIRST-LEAK", label="first", height=31, and location-qualified URL; $render data are secret="SECOND-LEAK", label="second", height=62, and location-qualified URL | render the authored document, activate its exact rendered control, execute actual $render with replacement data, then activate the exact replacement control | clicking the exact rendered controls run-action-payload-first and run-action-payload-second stores options.actionPayloadProbe.payload with type="html", literal text {{$jason.secret}}, and style heights 31 then 62 |

  Scenario: integration smoke: initial render actual $render generic action and all iframe traces stay live
    Given HTML template raw script with secret "LEAK", height 40, label "first"; then $render data secret "SECOND", height 80, label "second"; then generic transform and $set contexts
    When render initially, execute actual $render, generic transform, and $set
    Then first then replacement iframes retain raw {{$jason.secret}}, heights are 40px then 80px, labels first then second, old iframe disconnects, both sandbox endpoints remain, each trace is [CREATE(component), SANDBOX("allow-scripts"), SOURCE("srcdoc"), APPEND, RETURN] with RETURN from wrapper data-jasonette-type="html" completion and no later iframe event through the containing public render/action return, generic text is GENERIC, and $set text is ACTION

  Scenario: Jasonpedia fixture renders actual iframe srcdoc with authored CSS and Nexus content
    Given the public fixture support/fixtures/jasonpedia-html-index.json
    When render the fixture through the public renderer API
    Then actual iframe srcdoc contains "img{width: 100%;}" and "Nexus devices", sandbox is "allow-scripts", and events are [CREATE(component), SANDBOX("allow-scripts"), SOURCE("srcdoc"), APPEND, RETURN] with RETURN from wrapper data-jasonette-type="html" completion and no later iframe event through public render return

  Scenario: rendered HTML keeps authored script and CSS while an ordinary sibling interpolates
    Given background and component HTML contain {{$jason.secret}}, CSS uses color red and height 7, and label context is "ordinary"
    When render the templated document
    Then background srcdoc is "<style>body{color:red}</style><script>window.raw='{{$jason.secret}}'</script>", component srcdoc is "<style>p{height:7px}</style><p>{{$jason.secret}}</p>", label is "ordinary"; background events are [CREATE(background), SANDBOX("allow-scripts"), SOURCE("srcdoc"), APPEND, RETURN] and component events are [CREATE(component), SANDBOX("allow-scripts"), SOURCE("srcdoc"), APPEND, RETURN] with RETURN from wrapper data-jasonette-type="html" completion and no later iframe event through public render return

  Scenario: actual $render replaces a background iframe while old and new sandbox endpoints remain exact
    Given templated background URL starts "https://example.com/first-background.html" and $render data changes it to "https://example.com/second-background.html"
    When render initially then execute actual $render
    Then new iframe differs from disconnected old iframe, both retain sandbox="allow-scripts", new iframe has only src="https://example.com/second-background.html", and each trace is [CREATE(background), SANDBOX("allow-scripts"), SOURCE("src"), APPEND, RETURN]

  Scenario: emitted iframe policy is exact and makes no browser-enforcement assertion
    Given HTML integration component with context kind="html", secret="LEAK", height=40, label="first"
    When render the document under the finite observer
    Then emitted sandbox tokens equal only ["allow-scripts"] and events are [CREATE(component), SANDBOX("allow-scripts"), SOURCE("srcdoc"), APPEND, RETURN] with RETURN from wrapper data-jasonette-type="html" completion and no later iframe event through public render return; no browser-enforcement outcome is asserted

