Feature: Finite iframe security observer self-tests
  Public-package black-box evidence only; no browser-enforcement claim is made.

  Scenario: component RETURN observer self-test marks only after direct renderComponent returns
    Given direct component {type:"html", text:"<p>timing</p>"} and a markComponentReturn interception
    When render through the direct component boundary
    Then before the one return mark events are [CREATE(component), SANDBOX("allow-scripts"), SOURCE("srcdoc"), APPEND]; afterward events are [CREATE(component), SANDBOX("allow-scripts"), SOURCE("srcdoc"), APPEND, RETURN]

  Scenario: integrated component marker completes before public return and later iframe events are rejected
    Given an integrated component observer, wrapper, iframe, source value, and a configured late-mutation value
    When inside the containing public boundary set sandbox and source, append the iframe, mark data-jasonette-type="html" completion, then mutate the source late
    Then trace equals [CREATE(component), SANDBOX("allow-scripts"), SOURCE("srcdoc"), APPEND, RETURN, SOURCE("srcdoc")] and the boundary rejects "iframe event occurred after integrated component completion"

  Scenario Outline: <title>
    Given <setup>
    When <action>
    Then <expected>

    Examples: concrete insertion_observer_cases vectors
      | title | setup | action | expected |
      | observer insertion self-test: detects Element appendChild before sandbox | a tracked unsandboxed iframe and insertion route "Element appendChild" | insert the iframe using Element appendChild | the sole trace is ["CREATE(self-test)","APPEND"] and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer insertion self-test: detects DocumentFragment appendChild before sandbox | a tracked unsandboxed iframe and insertion route "DocumentFragment appendChild" | insert the iframe using DocumentFragment appendChild | the sole trace is ["CREATE(self-test)","APPEND"] and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer insertion self-test: detects insertBefore before sandbox | a tracked unsandboxed iframe and insertion route "insertBefore" | insert the iframe using insertBefore | the sole trace is ["CREATE(self-test)","APPEND"] and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer insertion self-test: detects replaceChild before sandbox | a tracked unsandboxed iframe and insertion route "replaceChild" | insert the iframe using replaceChild | the sole trace is ["CREATE(self-test)","APPEND"] and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer insertion self-test: detects Element append before sandbox | a tracked unsandboxed iframe and insertion route "Element append" | insert the iframe using Element append | the sole trace is ["CREATE(self-test)","APPEND"] and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer insertion self-test: detects Element prepend before sandbox | a tracked unsandboxed iframe and insertion route "Element prepend" | insert the iframe using Element prepend | the sole trace is ["CREATE(self-test)","APPEND"] and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer insertion self-test: detects replaceChildren before sandbox | a tracked unsandboxed iframe and insertion route "replaceChildren" | insert the iframe using replaceChildren | the sole trace is ["CREATE(self-test)","APPEND"] and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer insertion self-test: detects before before sandbox | a tracked unsandboxed iframe and insertion route "before" | insert the iframe using before | the sole trace is ["CREATE(self-test)","APPEND"] and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer insertion self-test: detects after before sandbox | a tracked unsandboxed iframe and insertion route "after" | insert the iframe using after | the sole trace is ["CREATE(self-test)","APPEND"] and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer insertion self-test: detects replaceWith before sandbox | a tracked unsandboxed iframe and insertion route "replaceWith" | insert the iframe using replaceWith | the sole trace is ["CREATE(self-test)","APPEND"] and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer insertion self-test: detects insertAdjacentElement before sandbox | a tracked unsandboxed iframe and insertion route "insertAdjacentElement" | insert the iframe using insertAdjacentElement | the sole trace is ["CREATE(self-test)","APPEND"] and safety assertion throws "source/insertion occurred before exact sandbox" |

  Scenario Outline: <title>
    Given <setup>
    When <action>
    Then <expected>

    Examples: concrete observer_creation_cases vectors
      | title | setup | action | expected |
      | observer creation self-test: detects createElementNS exactly | observer kind "self-test" and XHTML namespace createElementNS("iframe") | create an iframe using createElementNS | one trace equals [CREATE(self-test)] and passes the no-source-before-sandbox assertion |
      | observer creation self-test: detects Element innerHTML parser exactly | observer kind "self-test", route "Element innerHTML", and markup <iframe sandbox="allow-scripts" srcdoc="<p>a</p>"></iframe> | create an iframe using Element innerHTML | one trace equals [CREATE(self-test), SANDBOX("allow-scripts"), SOURCE("srcdoc"), APPEND] and passes the safety assertion |
      | observer creation self-test: detects template fragment parser exactly | observer kind "self-test", route "template innerHTML", and markup <iframe sandbox="allow-scripts" srcdoc="<p>a</p>"></iframe> | create an iframe using template innerHTML | one trace equals [CREATE(self-test), SANDBOX("allow-scripts"), SOURCE("srcdoc"), APPEND] and passes the safety assertion |
      | observer creation self-test: detects insertAdjacentHTML parser exactly | observer kind "self-test", route "insertAdjacentHTML", and markup <iframe sandbox="allow-scripts" srcdoc="<p>a</p>"></iframe> | create an iframe using insertAdjacentHTML | one trace equals [CREATE(self-test), SANDBOX("allow-scripts"), SOURCE("srcdoc"), APPEND] and passes the safety assertion |
      | observer creation self-test: detects Range contextual fragment exactly | observer kind "self-test", route "Range contextual fragment", and markup <iframe sandbox="allow-scripts" srcdoc="<p>a</p>"></iframe> | create an iframe using Range contextual fragment | one trace equals [CREATE(self-test), SANDBOX("allow-scripts"), SOURCE("srcdoc"), APPEND] and passes the safety assertion |
      | observer creation self-test: detects DOMParser document exactly | observer kind "self-test", route "DOMParser", and markup <iframe sandbox="allow-scripts" srcdoc="<p>a</p>"></iframe> | create an iframe using DOMParser | one trace equals [CREATE(self-test), SANDBOX("allow-scripts"), SOURCE("srcdoc"), APPEND] and passes the safety assertion |
      | observer parser self-test: rejects source before sandbox in authored parser order | observer kind "self-test" and parser markup <iframe srcdoc="<p>a</p>" sandbox="allow-scripts"></iframe> | create an iframe using source-first innerHTML | one trace equals [CREATE(self-test), SOURCE("srcdoc"), SANDBOX("allow-scripts"), APPEND] and safety assertion throws "source/insertion occurred before exact sandbox" |

  Scenario Outline: <title>
    Given <setup>
    When <action>
    Then <expected>

    Examples: concrete sandbox_observer_cases vectors
      | title | setup | action | expected |
      | observer sandbox self-test: detects setAttribute mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "setAttribute" | apply sandbox mutation sequence: setAttribute("sandbox","allow-scripts") | the sole post-reset event is SANDBOX("allow-scripts") |
      | observer sandbox self-test: detects setAttributeNS mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "setAttributeNS" | apply sandbox mutation sequence: setAttributeNS(null,"sandbox","allow-scripts") | the sole post-reset event is SANDBOX("allow-scripts") |
      | observer sandbox self-test: detects removeAttribute mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "removeAttribute" | apply sandbox mutation sequence: prepare sandbox="allow-scripts", reset events, then removeAttribute("sandbox") | the sole post-reset event is SANDBOX(null) |
      | observer sandbox self-test: detects removeAttributeNS mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "removeAttributeNS" | apply sandbox mutation sequence: prepare sandbox="allow-scripts", reset events, then removeAttributeNS(null,"sandbox") | the sole post-reset event is SANDBOX(null) |
      | observer sandbox self-test: detects toggleAttribute mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "toggleAttribute" | apply sandbox mutation sequence: toggleAttribute("sandbox",true) | the sole post-reset event is SANDBOX("") |
      | observer sandbox self-test: detects setAttributeNode mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "setAttributeNode" | apply sandbox mutation sequence: setAttributeNode of sandbox="allow-scripts" | the sole post-reset event is SANDBOX("allow-scripts") |
      | observer sandbox self-test: detects setAttributeNodeNS mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "setAttributeNodeNS" | apply sandbox mutation sequence: setAttributeNodeNS of sandbox="allow-scripts" | the sole post-reset event is SANDBOX("allow-scripts") |
      | observer sandbox self-test: detects removeAttributeNode mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "removeAttributeNode" | apply sandbox mutation sequence: prepare sandbox="allow-scripts", reset events, then removeAttributeNode | the sole post-reset event is SANDBOX(null) |
      | observer sandbox self-test: detects NamedNodeMap setNamedItem mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "NamedNodeMap setNamedItem" | apply sandbox mutation sequence: attributes.setNamedItem sandbox="allow-scripts" | the sole post-reset event is SANDBOX("allow-scripts") |
      | observer sandbox self-test: detects NamedNodeMap setNamedItemNS mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "NamedNodeMap setNamedItemNS" | apply sandbox mutation sequence: attributes.setNamedItemNS sandbox="allow-scripts" | the sole post-reset event is SANDBOX("allow-scripts") |
      | observer sandbox self-test: detects NamedNodeMap removeNamedItem mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "NamedNodeMap removeNamedItem" | apply sandbox mutation sequence: prepare sandbox="allow-scripts", reset events, then attributes.removeNamedItem | the sole post-reset event is SANDBOX(null) |
      | observer sandbox self-test: detects NamedNodeMap removeNamedItemNS mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "NamedNodeMap removeNamedItemNS" | apply sandbox mutation sequence: prepare sandbox="allow-scripts", reset events, then attributes.removeNamedItemNS | the sole post-reset event is SANDBOX(null) |
      | observer sandbox self-test: detects Attr value mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "Attr value" | apply sandbox mutation sequence: change prepared sandbox Attr from "allow-forms" to "allow-scripts" after reset | the sole post-reset event is SANDBOX("allow-scripts") |
      | observer sandbox self-test: detects Attr nodeValue mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "Attr nodeValue" | apply sandbox mutation sequence: change prepared sandbox Attr.nodeValue from "allow-forms" to "allow-scripts" after reset | the sole post-reset event is SANDBOX("allow-scripts") |
      | observer sandbox self-test: detects Attr textContent mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "Attr textContent" | apply sandbox mutation sequence: change prepared sandbox Attr.textContent from "allow-forms" to "allow-scripts" after reset | the sole post-reset event is SANDBOX("allow-scripts") |
      | observer sandbox self-test: detects sandbox value mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "sandbox value" | apply sandbox mutation sequence: set sandbox.value="allow-scripts" | the sole post-reset event is SANDBOX("allow-scripts") |
      | observer sandbox self-test: detects sandbox add mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "sandbox add" | apply sandbox mutation sequence: sandbox.add("allow-scripts") | the sole post-reset event is SANDBOX("allow-scripts") |
      | observer sandbox self-test: detects sandbox remove mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "sandbox remove" | apply sandbox mutation sequence: add allow-scripts, reset events, then sandbox.remove("allow-scripts") | the sole post-reset event is SANDBOX("") |
      | observer sandbox self-test: detects sandbox toggle mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "sandbox toggle" | apply sandbox mutation sequence: sandbox.toggle("allow-scripts") | the sole post-reset event is SANDBOX("allow-scripts") |
      | observer sandbox self-test: detects sandbox replace mutation | a tracked iframe, resettable event log, and configured sandbox mutation route "sandbox replace" | apply sandbox mutation sequence: add allow-forms, reset events, then sandbox.replace("allow-forms","allow-scripts") | the sole post-reset event is SANDBOX("allow-scripts") |

  Scenario Outline: <title>
    Given <setup>
    When <action>
    Then <expected>

    Examples: concrete source_observer_cases vectors
      | title | setup | action | expected |
      | observer source self-test: detects src property mutation | a tracked unsandboxed iframe, resettable event log, and configured source mutation route "src property" | apply source mutation sequence: set src="https://example.com/a" | the sole post-reset event is SOURCE("src") and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer source self-test: detects srcdoc property mutation | a tracked unsandboxed iframe, resettable event log, and configured source mutation route "srcdoc property" | apply source mutation sequence: set srcdoc="<p>a</p>" | the sole post-reset event is SOURCE("srcdoc") and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer source self-test: detects setAttribute src mutation | a tracked unsandboxed iframe, resettable event log, and configured source mutation route "setAttribute src" | apply source mutation sequence: setAttribute src="https://example.com/a" | the sole post-reset event is SOURCE("src") and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer source self-test: detects setAttribute srcdoc mutation | a tracked unsandboxed iframe, resettable event log, and configured source mutation route "setAttribute srcdoc" | apply source mutation sequence: setAttribute srcdoc="<p>a</p>" | the sole post-reset event is SOURCE("srcdoc") and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer source self-test: detects setAttributeNS src mutation | a tracked unsandboxed iframe, resettable event log, and configured source mutation route "setAttributeNS src" | apply source mutation sequence: setAttributeNS src="https://example.com/a" | the sole post-reset event is SOURCE("src") and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer source self-test: detects removeAttribute src mutation | a tracked unsandboxed iframe, resettable event log, and configured source mutation route "removeAttribute src" | apply source mutation sequence: prepare src="https://example.com/a", reset, then remove it | the sole post-reset event is SOURCE("src") and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer source self-test: detects removeAttributeNS srcdoc mutation | a tracked unsandboxed iframe, resettable event log, and configured source mutation route "removeAttributeNS srcdoc" | apply source mutation sequence: prepare srcdoc="<p>a</p>", reset, then remove it with namespace null | the sole post-reset event is SOURCE("srcdoc") and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer source self-test: detects toggleAttribute srcdoc mutation | a tracked unsandboxed iframe, resettable event log, and configured source mutation route "toggleAttribute srcdoc" | apply source mutation sequence: toggleAttribute("srcdoc",true) | the sole post-reset event is SOURCE("srcdoc") and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer source self-test: detects setAttributeNode srcdoc mutation | a tracked unsandboxed iframe, resettable event log, and configured source mutation route "setAttributeNode srcdoc" | apply source mutation sequence: setAttributeNode srcdoc="<p>a</p>" | the sole post-reset event is SOURCE("srcdoc") and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer source self-test: detects setAttributeNodeNS src mutation | a tracked unsandboxed iframe, resettable event log, and configured source mutation route "setAttributeNodeNS src" | apply source mutation sequence: setAttributeNodeNS src="https://example.com/a" | the sole post-reset event is SOURCE("src") and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer source self-test: detects removeAttributeNode srcdoc mutation | a tracked unsandboxed iframe, resettable event log, and configured source mutation route "removeAttributeNode srcdoc" | apply source mutation sequence: prepare srcdoc="<p>a</p>", reset, then remove its Attr | the sole post-reset event is SOURCE("srcdoc") and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer source self-test: detects NamedNodeMap setNamedItem src mutation | a tracked unsandboxed iframe, resettable event log, and configured source mutation route "NamedNodeMap setNamedItem src" | apply source mutation sequence: attributes.setNamedItem src="https://example.com/a" | the sole post-reset event is SOURCE("src") and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer source self-test: detects NamedNodeMap setNamedItemNS srcdoc mutation | a tracked unsandboxed iframe, resettable event log, and configured source mutation route "NamedNodeMap setNamedItemNS srcdoc" | apply source mutation sequence: attributes.setNamedItemNS srcdoc="<p>a</p>" | the sole post-reset event is SOURCE("srcdoc") and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer source self-test: detects NamedNodeMap removeNamedItem src mutation | a tracked unsandboxed iframe, resettable event log, and configured source mutation route "NamedNodeMap removeNamedItem src" | apply source mutation sequence: prepare src="https://example.com/a", reset, then removeNamedItem | the sole post-reset event is SOURCE("src") and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer source self-test: detects NamedNodeMap removeNamedItemNS srcdoc mutation | a tracked unsandboxed iframe, resettable event log, and configured source mutation route "NamedNodeMap removeNamedItemNS srcdoc" | apply source mutation sequence: prepare srcdoc="<p>a</p>", reset, then removeNamedItemNS | the sole post-reset event is SOURCE("srcdoc") and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer source self-test: detects Attr value srcdoc mutation | a tracked unsandboxed iframe, resettable event log, and configured source mutation route "Attr value srcdoc" | apply source mutation sequence: change srcdoc Attr.value from "<p>before</p>" to "<p>after</p>" after reset | the sole post-reset event is SOURCE("srcdoc") and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer source self-test: detects Attr nodeValue src mutation | a tracked unsandboxed iframe, resettable event log, and configured source mutation route "Attr nodeValue src" | apply source mutation sequence: change src Attr.nodeValue from "https://example.com/before" to "https://example.com/after" after reset | the sole post-reset event is SOURCE("src") and safety assertion throws "source/insertion occurred before exact sandbox" |
      | observer source self-test: detects Attr textContent srcdoc mutation | a tracked unsandboxed iframe, resettable event log, and configured source mutation route "Attr textContent srcdoc" | apply source mutation sequence: change srcdoc Attr.textContent from "<p>before</p>" to "<p>after</p>" after reset | the sole post-reset event is SOURCE("srcdoc") and safety assertion throws "source/insertion occurred before exact sandbox" |

